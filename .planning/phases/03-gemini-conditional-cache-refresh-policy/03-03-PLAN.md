---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/tui/app.rs
  - src/tui/widgets/hp_row.rs
  - src/tui/mod.rs
autonomous: true
requirements:
  - TUI-03

must_haves:
  truths:
    - "User sees a yellow HP bar row with '(stale Ns ago)' suffix when an adapter fails transiently — the row does not blank out (D-69, D-70)"
    - "Stale rows are visually distinct: every span uses Color::Yellow instead of the normal green/red accent (D-69)"
    - "RowState::StaleOk { state: ProviderState, stale_age_secs: u64 } variant exists in src/tui/app.rs (D-70)"
    - "AppState::apply_results accepts Vec<(ProviderId, RowOutcome)> and translates Stale to StaleOk (D-70)"
    - "ProviderState struct is NOT modified — no new stale fields (D-70)"
    - "src/cli/render_text.rs and src/cli/render_json.rs are NOT modified (D-68)"
    - "tokio::time::interval(Duration::from_secs(15)) in src/tui/mod.rs is NOT changed (D-74)"
    - "No Timestamp::now() call added under src/tui/widgets/ (BL-01)"
    - "TUI panic-safe restore and non-TTY refusal tests still pass (Phase 1 TUI-04, TUI-05)"
    - "The SCAFFOLD adapter added by Plan 03-02 Task 3 is fully removed from src/tui/mod.rs"
  artifacts:
    - path: "src/tui/app.rs"
      provides: "RowState::StaleOk variant + updated apply_results"
      contains: "StaleOk { state: ProviderState, stale_age_secs: u64 }"
    - path: "src/tui/widgets/hp_row.rs"
      provides: "build_stale_ok_line + match arm in build_line"
      contains: "build_stale_ok_line"
    - path: "src/tui/mod.rs"
      provides: "tui_loop fetch tick passes RowOutcome to apply_results (SCAFFOLD removed)"
      contains: "apply_results(results)"
  key_links:
    - from: "src/tui/mod.rs"
      to: "src/tui/app.rs"
      via: "tui_loop calls apply_results with Vec<(ProviderId, RowOutcome)>"
      pattern: "apply_results"
    - from: "src/tui/app.rs"
      to: "src/tui/widgets/hp_row.rs"
      via: "ui::draw matches on RowState::StaleOk and calls render()"
      pattern: "StaleOk"
---

<objective>
Wire the engine's RowOutcome::Stale into the TUI render path. Three coordinated changes: (1+2) add RowState::StaleOk variant AND build_stale_ok_line in an atomic commit to keep the Rust match exhaustive at all times, (3) update tui_loop in mod.rs to pass RowOutcome directly to the updated apply_results and remove the SCAFFOLD adapter that Plan 03-02 Task 3 added.

Purpose: After this plan, `AHB tui` shows a yellow "(stale Ns ago)" row when an adapter fails transiently and cache has a prior successful state. CLI behavior is unchanged (D-68). The SCAFFOLD marker "SCAFFOLD: removed in Plan 03-03" in src/tui/mod.rs is deleted; no scaffold remains.

Output: Updated src/tui/app.rs, src/tui/widgets/hp_row.rs, src/tui/mod.rs.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-RESEARCH.md

<interfaces>
<!-- Key types extracted from codebase 2026-05-25 -->

From src/tui/app.rs (lines 21-31) — current RowState (Phase 03 adds StaleOk):
```rust
pub enum RowState {
    Ok(ProviderState),
    SchemaDrift { id: ProviderId },
    Err { id: ProviderId, message: String },
}
```

From src/tui/app.rs (lines 63-78) — current apply_results signature (MUST CHANGE):
```rust
pub fn apply_results(
    &mut self,
    results: Vec<(ProviderId, Result<ProviderState, ProviderError>)>,
)
```

From src/tui/widgets/hp_row.rs (lines 66-72) — build_line match (ADD StaleOk arm):
```rust
pub fn build_line(row: &RowState, now: &jiff::Timestamp) -> Line<'static> {
    match row {
        RowState::Ok(state) => build_ok_line(state, now),
        RowState::SchemaDrift { id } => build_schema_drift_line(*id),
        RowState::Err { id, message } => build_err_line(*id, message),
    }
}
```

From src/tui/widgets/hp_row.rs (lines 74-104) — build_ok_line (DO NOT CHANGE — build_stale_ok_line is a sibling):
Key pattern: Span::styled("\u{2588}".repeat(filled), Style::default().fg(accent)) per span.
build_stale_ok_line must use Color::Yellow for ALL spans, NOT call build_ok_line internally.

From src/tui/mod.rs (lines 144-147) — fetch tick arm (state after Plan 03-02 SCAFFOLD):
```rust
_ = fetch_tick.tick() => {
    let outcomes = engine.refresh_all(jiff::Timestamp::now()).await;
    // SCAFFOLD: removed in Plan 03-03
    let legacy: Vec<(ProviderId, Result<ProviderState, ProviderError>)> = outcomes
        .into_iter()
        .map(|(id, outcome)| match outcome {
            RowOutcome::Fresh(state) | RowOutcome::Stale { state, .. } => (id, Ok(state)),
            RowOutcome::Failed(err) => (id, Err(err)),
        })
        .collect();
    app.apply_results(legacy);
}
```
After this plan: the SCAFFOLD block is removed and outcomes passed directly to apply_results.

From 03-CONTEXT.md § D-69 — stale row format:
"claude  ██████░░░░ 60% • resets in 2h00m  (stale 32s ago)" — TWO spaces before (stale, integer seconds.
Whole row Yellow override. --ascii: bar chars change but suffix stays.
--color=never: remove Yellow but keep suffix text.

From 03-RESEARCH.md § Q6 — Yellow override:
Build each Span with Style::default().fg(Color::Yellow) directly.
Do NOT use Paragraph base style alone — per-Span styling takes precedence in ratatui 0.30.

From 03-CONTEXT.md § D-70 — RowState::StaleOk fields:
{ state: ProviderState, stale_age_secs: u64 }
stale_age_secs is pre-computed by Engine (in RowOutcome::Stale), so apply_results just passes it through.

From engine/cache.rs (Plan 02) — RowOutcome:
pub enum RowOutcome {
    Fresh(ProviderState),
    Stale { state: ProviderState, stale_age_secs: u64 },
    Failed(ProviderError),
}

apply_results translation table (Q5 from RESEARCH):
| RowOutcome::Fresh(state)       | RowState::Ok(state)             |
| RowOutcome::Stale{state,secs}  | RowState::StaleOk{state,secs}   |
| RowOutcome::Failed(SchemaDrift)| RowState::SchemaDrift{id}        |
| RowOutcome::Failed(other)      | RowState::Err{id, message}       |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RowState::StaleOk to app.rs AND build_stale_ok_line to hp_row.rs in one atomic change</name>
  <read_first>
    - src/tui/app.rs full file: current RowState enum (lines 21-31) + apply_results (lines 63-78) + unit tests (lines 98-183)
    - src/tui/widgets/hp_row.rs full file: build_ok_line (lines 74-104) and build_line (lines 66-72) — understand Span construction pattern
    - 03-CONTEXT.md § D-70: StaleOk field names { state, stale_age_secs }
    - 03-CONTEXT.md § D-69: exact format "  (stale Ns ago)" suffix, Yellow override on ALL spans
    - 03-RESEARCH.md § Q5: translation table Fresh/Stale/Failed → RowState variants
    - 03-RESEARCH.md § Q6: per-Span Color::Yellow override (not Paragraph base style); Modifier::DIM skipped for v1
    - src/engine/cache.rs (created in Plan 02): RowOutcome enum definition
  </read_first>
  <files>src/tui/app.rs, src/tui/widgets/hp_row.rs</files>
  <behavior>
    app.rs behavioral tests:
    - Test `apply_results_translates_fresh_to_ok`: RowOutcome::Fresh(state) → RowState::Ok(state)
    - Test `apply_results_translates_stale_to_stale_ok`: RowOutcome::Stale { state, stale_age_secs: 47 } → RowState::StaleOk { state, stale_age_secs: 47 }
    - Test `apply_results_passes_stale_age_secs_unchanged`: stale_age_secs = 0, 15, 3600 — all pass through as-is
    - Test `apply_results_translates_schema_drift_to_schema_drift`: RowOutcome::Failed(ProviderError::SchemaDrift{..}) → RowState::SchemaDrift { id }
    - Test `apply_results_translates_other_failed_to_err`: RowOutcome::Failed(ProviderError::Unavailable{..}) → RowState::Err { id, message }
    - Existing test `apply_results_translates_ok_schema_drift_and_err` must be UPDATED to use RowOutcome input

    hp_row.rs behavioral tests:
    - Test `build_stale_ok_line_uses_yellow_for_all_spans`: all styled Spans have fg == Color::Yellow (no Green or Red)
    - Test `build_stale_ok_line_includes_stale_suffix`: concatenated text contains "(stale 32s ago)" for stale_age_secs = 32
    - Test `build_stale_ok_line_two_spaces_before_suffix`: text contains "  (stale" (two spaces before open paren, D-69)
    - Test `build_stale_ok_line_zero_secs`: stale_age_secs = 0 → "(stale 0s ago)"
    - Test `build_line_dispatches_stale_ok_to_stale_line`: build_line with RowState::StaleOk dispatches to build_stale_ok_line
  </behavior>
  <action>
    IMPORTANT: These two files must be changed in the SAME task because adding RowState::StaleOk to app.rs makes the match in hp_row.rs::build_line non-exhaustive. Changing them atomically ensures `cargo test --lib tui` compiles and passes in one shot.

    === Changes to src/tui/app.rs ===

    1. In RowState enum, add after the Err variant:
       ```rust
       /// Last fetch failed transiently; serving cached data. Rendered as yellow bar
       /// with "(stale Ns ago)" suffix per D-69. stale_age_secs is pre-computed by
       /// Engine so the renderer never touches wall clock (BL-01).
       StaleOk { state: ProviderState, stale_age_secs: u64 },
       ```

    2. Rewrite apply_results signature:
       ```rust
       pub fn apply_results(
           &mut self,
           results: Vec<(ProviderId, crate::engine::cache::RowOutcome)>,
       )
       ```
       Body replaces the match arm:
       - RowOutcome::Fresh(state) → RowState::Ok(state)
       - RowOutcome::Stale { state, stale_age_secs } → RowState::StaleOk { state, stale_age_secs }
       - RowOutcome::Failed(ProviderError::SchemaDrift { .. }) → RowState::SchemaDrift { id }
       - RowOutcome::Failed(other) → RowState::Err { id, message: other.to_string() }

    3. Update existing test `apply_results_translates_ok_schema_drift_and_err` to use RowOutcome input (input type changes; assertions stay the same shape).

    4. Add all new app.rs behavioral tests listed in &lt;behavior&gt;.

    NOTE on ProviderState not modified: the StaleOk variant carries a cloned ProviderState — the struct itself gains no new fields. ProviderState remains a pure DTO per D-70.

    === Changes to src/tui/widgets/hp_row.rs ===

    5. Add `use crate::tui::app::RowState::StaleOk;` in the match import area (or use full path in the match arm).

    6. In build_line, add match arm before or after RowState::Ok:
       ```rust
       RowState::StaleOk { state, stale_age_secs } => build_stale_ok_line(state, *stale_age_secs, now),
       ```
       This arm MUST be added in the same commit as the variant addition to app.rs — doing so atomically keeps the match exhaustive throughout.

    7. Add function `fn build_stale_ok_line(state: &ProviderState, stale_age_secs: u64, now: &jiff::Timestamp) -> Line<'static>`:
       - Mirror build_ok_line's pct / filled / empty / countdown / label logic EXACTLY (same math).
       - Replace ALL Span::styled colors with `Style::default().fg(Color::Yellow)`.
       - Do NOT call build_ok_line internally — build_stale_ok_line is a sibling (per RESEARCH Q6: ratatui 0.30 per-Span style takes precedence; wrapping would require overriding each span after the fact, which is fragile).
       - Append at the end: `Span::raw("  ")` followed by `Span::raw(format!("(stale {stale_age_secs}s ago)"))`. Both un-styled (raw) — the suffix itself doesn't need color treatment; it's clear text.

       Full span list (mirroring build_ok_line):
       - Span::raw(format!("{label}  "))
       - Span::styled("\u{2588}".repeat(filled), Style::default().fg(Color::Yellow))  // filled cells Yellow
       - Span::styled("\u{2591}".repeat(empty), Style::default().fg(Color::Yellow))   // empty cells Yellow (not DarkGray)
       - Span::raw(format!(" {pct_int}% "))
       - Span::styled("\u{2022}", Style::default().fg(Color::Yellow))                  // bullet Yellow
       - Span::raw(format!(" resets in {countdown}"))
       - Span::raw("  ")
       - Span::raw(format!("(stale {stale_age_secs}s ago)"))

    8. Add unit tests for all 5 hp_row.rs behaviors listed in &lt;behavior&gt;.

    NOTE: The `_ascii` parameter is NOT passed to build_stale_ok_line for now — TUI always uses Unicode. D-69 says "--ascii mode: bar chars change but suffix stays"; since the TUI widget's ascii param is currently ignored by build_ok_line too (`_ascii: bool`), keep the same convention.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test --lib tui 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --lib tui::app::tests::apply_results_translates_fresh_to_ok exits 0
    - cargo test --lib tui::app::tests::apply_results_translates_stale_to_stale_ok exits 0
    - cargo test --lib tui::app::tests::apply_results_passes_stale_age_secs_unchanged exits 0
    - cargo test --lib tui::app::tests::apply_results_translates_schema_drift_to_schema_drift exits 0
    - cargo test --lib tui::app::tests::apply_results_translates_other_failed_to_err exits 0
    - cargo test --lib tui::widgets::hp_row::tests::build_stale_ok_line_uses_yellow_for_all_spans exits 0
    - cargo test --lib tui::widgets::hp_row::tests::build_stale_ok_line_includes_stale_suffix exits 0
    - cargo test --lib tui::widgets::hp_row::tests::build_stale_ok_line_two_spaces_before_suffix exits 0
    - cargo test --lib tui::widgets::hp_row::tests::build_stale_ok_line_zero_secs exits 0
    - cargo test --lib tui::widgets::hp_row::tests::build_line_dispatches_stale_ok_to_stale_line exits 0
    - grep -F "StaleOk" src/tui/app.rs returns >= 1 match in RowState enum
    - grep -F "build_stale_ok_line" src/tui/widgets/hp_row.rs returns >= 1 match in build_line arm + function def
    - grep -c "Color::Yellow" src/tui/widgets/hp_row.rs returns >= 5 (all spans in build_stale_ok_line)
    - cargo build --lib exits 0 (no non-exhaustive match errors)
  </acceptance_criteria>
  <done>All app.rs unit tests pass (existing + new). All hp_row.rs tests pass (existing + 5 new). RowState::StaleOk variant exists. apply_results signature changed to RowOutcome input. build_line has StaleOk arm. cargo build --lib clean.</done>
</task>

<task type="auto">
  <name>Task 2: Remove SCAFFOLD from tui/mod.rs, wire RowOutcome directly to apply_results, and run full regression suite</name>
  <read_first>
    - src/tui/mod.rs full file: tui_loop (lines 107-158), specifically the fetch tick arm and the priming fetch — look for the SCAFFOLD blocks added by Plan 03-02 Task 3
    - After Task 1 of this plan: app.apply_results now accepts Vec&lt;(ProviderId, RowOutcome)&gt; directly
    - 03-CONTEXT.md § D-74: tokio::time::interval(Duration::from_secs(15)) at line 124 MUST NOT CHANGE
    - 03-CONTEXT.md § D-74: authorized Timestamp::now() callsites in tui_loop — lines 108, 112, 120, 145, 153 — these are unchanged
  </read_first>
  <files>src/tui/mod.rs</files>
  <action>
    1. In src/tui/mod.rs, search for the `// SCAFFOLD: removed in Plan 03-03` comment marker using grep to locate both SCAFFOLD adapter blocks (priming fetch and fetch tick arm). Remove BOTH SCAFFOLD blocks entirely:
       - Delete the `let legacy: Vec<...>` binding and its `into_iter().map(...)` adapter
       - Delete the `app.apply_results(legacy)` call (which used the legacy binding)
       - Rename `outcomes` back to `results` (or keep `outcomes` — either name compiles fine after apply_results accepts RowOutcome directly)
       - Replace with the direct call: `app.apply_results(results);` (or `app.apply_results(outcomes);`)

    2. The RowOutcome import (`use crate::engine::cache::RowOutcome;`) that Plan 03-02 added to tui/mod.rs may still be needed if the variable has type RowOutcome — keep it if cargo warns it is used, remove it only if cargo warns "unused import". Do not guess: let the compiler decide after the SCAFFOLD removal.

    3. Verify that `tokio::time::interval(Duration::from_secs(15))` is UNCHANGED. Do not touch this line.

    4. Verify that NO new Timestamp::now() call is added.

    5. If src/tui/ui.rs needs a StaleOk match arm added (to draw the row via hp_row::render), add it now:
       - Read src/tui/ui.rs to confirm how it iterates app.rows and calls hp_row::render
       - If ui::draw has an exhaustive match on RowState that currently doesn't include StaleOk, add the arm: `RowState::StaleOk { .. } => { hp_row::render(area, buf, row, false, &app.now); }` (same as Ok — the render function dispatches to build_line which now handles StaleOk)

    6. Run the full test suite to confirm no Phase 1/2 regressions:
       - cargo build (full binary)
       - cargo test --lib tui
       - cargo test tui_panic_safe_restore
       - cargo test tui_non_tty_refusal
       - cargo test cli_walking_skeleton
       - cargo test exit_codes
       - cargo test json_format_round_trip
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo build 2>&1 | tail -20 && cargo test --lib tui 2>&1 | tail -20 && cargo test tui_panic_safe_restore 2>&1 | tail -10 && cargo test tui_non_tty_refusal 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo build (full binary) exits 0 with no errors
    - cargo test --lib tui exits 0 (all TUI lib tests pass)
    - cargo test tui_panic_safe_restore exits 0 (TUI-04 regression)
    - cargo test tui_non_tty_refusal exits 0 (TUI-05 regression)
    - grep -F "SCAFFOLD: removed in Plan 03-03" src/tui/mod.rs returns 0 matches (scaffold fully removed)
    - grep -c "tokio::time::interval(Duration::from_secs(15))" src/tui/mod.rs returns 1 (D-74 unchanged)
    - grep -rn "Timestamp::now()" src/tui/widgets/ returns 0 matches (BL-01)
    - grep -F "stale" src/cli/render_text.rs returns 0 matches (D-68 CLI not modified)
    - grep -F "stale" src/cli/render_json.rs returns 0 matches (D-68)
    - grep -F "stale_age_secs" src/model.rs returns 0 matches (ProviderState not modified, D-70)
  </acceptance_criteria>
  <done>cargo build (full binary) passes clean. All TUI lib tests pass. tui_panic_safe_restore and tui_non_tty_refusal integration tests pass (Phase 1 TUI-04 + TUI-05 regression). SCAFFOLD fully removed: grep -F "SCAFFOLD: removed in Plan 03-03" src/tui/mod.rs returns 0 matches. D-74 regression: grep confirms tokio::time::interval(Duration::from_secs(15)) unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RowOutcome::Stale → TUI render | Stale state from in-memory cache; state was previously validated by the adapter |
| TUI render → terminal | Yellow ANSI color codes; rendered only in real TTY context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | RowState::StaleOk displayed in TUI | accept | State was previously returned by the adapter and displayed; stale_age_secs is a derived duration (no secrets); no new information is surfaced beyond what was already shown |
| T-03-03-02 | Tampering | ProviderState modified to carry stale concept | mitigate | D-70 explicitly prohibits this; StaleOk is a TUI-layer concern only; ProviderState is unchanged; JSON schema unaffected |
| T-03-03-03 | Denial of Service | build_stale_ok_line panic on empty windows | mitigate | Mirrors build_ok_line's existing guard: if state.windows.is_empty() return build_err_line fallback (same defensive pattern) |
| T-03-03-04 | Tampering | CLI render acquiring stale path | mitigate | CLI only calls run_compact/detailed/json which use outcome_to_result(Stale) → unreachable!(); D-68 enforced |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan |
</threat_model>

<verification>
1. `cargo build` (full binary including TUI) passes clean after both tasks
2. `cargo test --lib tui` passes (app.rs + hp_row.rs + ui.rs unit tests)
3. `cargo test tui_panic_safe_restore` passes (TUI-04 regression)
4. `cargo test tui_non_tty_refusal` passes (TUI-05 regression)
5. `grep -F "SCAFFOLD: removed in Plan 03-03" src/tui/mod.rs` returns 0 matches (scaffold removed)
6. `grep -c "tokio::time::interval(Duration::from_secs(15))" src/tui/mod.rs` returns 1 (D-74)
7. `grep -rn "Timestamp::now()" src/tui/widgets/` returns 0 matches (BL-01)
8. `grep -F "StaleOk" src/tui/app.rs` returns hits in RowState enum + apply_results
9. `grep -F "build_stale_ok_line" src/tui/widgets/hp_row.rs` returns hits in build_line + function def
10. `grep -F "Color::Yellow" src/tui/widgets/hp_row.rs` returns ≥5 matches in build_stale_ok_line (all spans)
11. Negative: `grep -F "stale" src/cli/render_text.rs` returns 0 matches (D-68 CLI not modified)
12. Negative: `grep -F "stale" src/cli/render_json.rs` returns 0 matches (D-68)
13. Negative: `grep -F "stale_age_secs" src/model.rs` returns 0 matches (ProviderState not modified, D-70)
</verification>

<success_criteria>
- RowState::StaleOk { state: ProviderState, stale_age_secs: u64 } exists and is matched by build_line.
- build_stale_ok_line produces a Line where every styled Span uses Color::Yellow and the text includes "  (stale Ns ago)".
- apply_results correctly translates RowOutcome::Stale to RowState::StaleOk passing stale_age_secs through.
- tui_loop compiles with SCAFFOLD fully removed; RowOutcome passed directly to apply_results.
- Existing TUI integration tests pass.
- ProviderState, CLI render paths, and JSON schema are unmodified.
- Tasks 1+2 merged into one atomic task to prevent non-exhaustive match compile error between individual task completions.
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-03-SUMMARY.md` when done.
</output>
