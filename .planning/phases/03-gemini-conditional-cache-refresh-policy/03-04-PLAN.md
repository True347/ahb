---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "04"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/provider/gemini.rs
  - src/templates/default-config.toml
  - README.md
autonomous: true
requirements:
  - ADP-05

must_haves:
  truths:
    - "User enabling [providers.gemini] enabled = true sees a clear, actionable error message pointing to README §Gemini status instead of a cryptic internal error (D-65)"
    - "README.md contains an explicit warning section explaining the Gemini deferral and the ToS risk of web-scraping gemini.google.com/usage (D-65, SC-2)"
    - "No --experimental-gemini CLI flag introduced (D-63)"
    - "GeminiUnimplementedProvider::fetch returns Err(Unavailable) with reason exactly 'Gemini adapter deferred to v2 — see README §Gemini status' (D-65)"
    - "src/templates/default-config.toml Gemini comment reads '# Gemini CLI subscription — deferred to v2 stub (see README §Gemini status)' (D-64)"
    - "format_error_row_colored still renders Gemini Unavailable row correctly (Phase 1 SEC-03 sanitizer rule still applies)"
  artifacts:
    - path: "src/provider/gemini.rs"
      provides: "Updated error reason string"
      contains: "Gemini adapter deferred to v2 — see README §Gemini status"
    - path: "src/templates/default-config.toml"
      provides: "Updated Gemini comment"
      contains: "deferred to v2 stub"
    - path: "README.md"
      provides: "Gemini adapter status section"
      contains: "## Gemini adapter status — deferred to v2"
  key_links:
    - from: "src/provider/gemini.rs"
      to: "README.md"
      via: "Error reason字面 aligns with README section title"
      pattern: "Gemini adapter deferred to v2"
---

<objective>
Finalize the Gemini NO-GO stub: update the error reason string, default config comment, and README section so all three sources say the same thing (D-63, D-64, D-65). This is the smallest plan in Phase 3 — pure literal and doc updates, zero logic changes.

Purpose: ADP-05 NO-GO path per GEMINI_SPIKE.md decision. User enabling `[providers.gemini] enabled = true` sees a clear, actionable message pointing to README §Gemini status.

Note on wave: This plan is in Wave 2 (depends_on 03-01) rather than Wave 1 to avoid a parallel file conflict on src/provider/gemini.rs — Plan 03-01 Task 2 also touches that file to add DEFAULT_REFRESH_INTERVAL_SECS. Running sequentially eliminates the need for merge coordination.

Output: Updated gemini.rs (reason string + test assertions), default-config.toml (comment), README.md (new section).
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/research/GEMINI_SPIKE.md

<interfaces>
<!-- Key code extracts from codebase 2026-05-25 -->

From src/provider/gemini.rs (lines 44-49) — current error reason to replace:
```rust
Err(ProviderError::Unavailable {
    reason: "Gemini provider is not yet implemented (Phase 3) — set \
             [providers.gemini].enabled = false to suppress this row"
        .into(),
})
```

From src/provider/gemini.rs (lines 69-78) — test assertions that MUST be updated:
```rust
ProviderError::Unavailable { reason } => {
    assert!(
        reason.contains("not yet implemented"),   // MUST CHANGE
        "reason should explain Phase 3 status: {reason}"
    );
    assert!(
        reason.contains("enabled = false"),        // MUST CHANGE
        "reason should include next-step hint per UI-SPEC: {reason}"
    );
}
```

From src/templates/default-config.toml (line 12) — current comment to replace:
```toml
enabled = false  # Gemini CLI subscription — not yet implemented (Phase 3)
```

From 03-RESEARCH.md § Pitfall 14: format_error_row_colored must still render Gemini Unavailable correctly after reason string change. The render logic matches on ProviderError variant (not reason string substring), so this is safe — but verify.

From GEMINI_SPIKE.md § Kill criteria (lines 144-151): five conditions; reference in README.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update GeminiUnimplementedProvider error reason and fix test assertions</name>
  <read_first>
    - src/provider/gemini.rs full file: understand current reason string (lines 44-48) and test assertions (lines 69-78)
    - 03-CONTEXT.md § D-65: exact locked reason string + format_one_line sanitizer compliance
    - 03-RESEARCH.md § Pitfall 12: DO NOT change test structure, only assertion strings
    - 01-CONTEXT.md § format_one_line sanitizer rule: short sentence, no line breaks in the reason string
  </read_first>
  <files>src/provider/gemini.rs</files>
  <behavior>
    - Test `gemini_placeholder_returns_unavailable` (UPDATED): fetch() returns ProviderError::Unavailable whose reason contains "Gemini adapter deferred to v2" (new substring assertion)
    - Test `gemini_placeholder_returns_unavailable` (UPDATED): reason also contains "README §Gemini status" (second new substring assertion)
    - Test `gemini_placeholder_id_is_gemini`: unchanged — still passes
    - New test `gemini_error_reason_does_not_contain_old_literals`: assert reason does NOT contain "not yet implemented" and does NOT contain "enabled = false" (regression guard against accidental revert)
  </behavior>
  <action>
    1. In GeminiUnimplementedProvider::fetch (lines 44-48), replace the entire reason string with the D-65 LOCKED literal:
       `"Gemini adapter deferred to v2 — see README §Gemini status".into()`
       This is a single-line string. Do NOT use a line continuation (backslash) — the new literal is short enough to fit.

    2. In the test `gemini_placeholder_returns_unavailable` (lines 69-78), replace BOTH assert! calls:
       - First: `assert!(reason.contains("Gemini adapter deferred to v2"), "reason: {reason}")`
       - Second: `assert!(reason.contains("README §Gemini status"), "reason: {reason}")`

    3. Add a new test `gemini_error_reason_does_not_contain_old_literals` that calls provider.fetch() and asserts:
       - `!reason.contains("not yet implemented")`
       - `!reason.contains("enabled = false")`
       This prevents silent revert to Phase 2 wording during future edits.

    4. DEFAULT_REFRESH_INTERVAL_SECS = 15 was added by Plan 01 Task 2 (depends_on 03-01 ensures this is already present). Do NOT re-add it; verify it exists and leave it untouched.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test gemini 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --lib provider::gemini::tests::gemini_placeholder_returns_unavailable exits 0
    - cargo test --lib provider::gemini::tests::gemini_placeholder_id_is_gemini exits 0
    - cargo test --lib provider::gemini::tests::gemini_error_reason_does_not_contain_old_literals exits 0
    - grep -v '^//' src/provider/gemini.rs | grep -c "not yet implemented" returns 0
    - grep -F "Gemini adapter deferred to v2 — see README §Gemini status" src/provider/gemini.rs returns >= 1 match
  </acceptance_criteria>
  <done>All gemini tests pass. `cargo test gemini` green. `grep -n "not yet implemented" src/provider/gemini.rs` returns 0 matches. `grep -n "Gemini adapter deferred to v2" src/provider/gemini.rs` returns 1+ matches.</done>
</task>

<task type="auto">
  <name>Task 2: Update default-config.toml Gemini comment and add README §Gemini status section</name>
  <read_first>
    - src/templates/default-config.toml: current Gemini comment (line 12) to replace per D-64
    - 03-CONTEXT.md § D-64: exact new comment literal
    - 03-CONTEXT.md § D-65: README section structure — title + two paragraphs + ToS warning
    - GEMINI_SPIKE.md § Phase 3 hand-off (lines 152-161): verbatim README section draft (use this as the source for paragraph content)
    - GEMINI_SPIKE.md § Kill criteria (lines 144-151): reference these 5 conditions in the v2 trigger paragraph
    - ROADMAP.md Phase 3 SC-2: "README.md contains an explicit warning section about... the deferral note"
  </read_first>
  <files>
    src/templates/default-config.toml,
    README.md
  </files>
  <action>
    1. In src/templates/default-config.toml, replace line 12:
       FROM: `enabled = false  # Gemini CLI subscription — not yet implemented (Phase 3)`
       TO:   `enabled = false  # Gemini CLI subscription — deferred to v2 stub (see README §Gemini status)`
       (per D-64 exactly; do NOT add refresh_interval to the template per RESEARCH Pitfall 13)

    2. In README.md, append a new section at the end (or before any "Contributing" / "License" section if those exist). The section must have:

       Title (H2): `## Gemini adapter status — deferred to v2`

       Paragraph 1 (NO-GO reasons, three bullet points or short sentences):
       The Gemini adapter is deferred to v2. Phase 0's go/no-go spike (see `.planning/research/GEMINI_SPIKE.md`) determined that `gemini-cli 0.41.2` does not expose a non-interactive, stable, parseable stats endpoint:
       - The local `/stats` slash command is REPL-only; `gemini -p "/stats"` forwards the literal string to the LLM as a chat prompt.
       - `--output-format json` activates the full `LocalAgentExecutor` agent loop instead of emitting a thin metadata envelope.
       - No probe produced quota or session-reset fields in a parseable format.

       Paragraph 2 (v2 trigger + ToS warning):
       The v2 adapter will be re-spiked when one of the conditions in `GEMINI_SPIKE.md § Kill criteria` is met (e.g., gemini-cli exposes a non-interactive `/stats` entry point, or a stable stats endpoint with documented schema becomes available).

       ToS warning sentence (SC-2 binding): Web-scraping `gemini.google.com/usage` carries account-ban risk and is permanently out of scope for AHB; see `PITFALLS.md § Pitfall 1` for details.

    The section must NOT exceed ~15 lines of markdown. Keep it concise per D-65 "兩段、八行左右".
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && grep -F "deferred to v2 stub" src/templates/default-config.toml && grep -F "## Gemini adapter status — deferred to v2" README.md && grep -F "gemini.google.com/usage" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -F "deferred to v2 stub (see README §Gemini status)" src/templates/default-config.toml returns 1 match
    - grep -F "## Gemini adapter status — deferred to v2" README.md returns 1 match
    - grep -F "gemini.google.com/usage" README.md returns 1 match (ToS warning)
    - grep -F "Kill criteria" README.md returns >= 1 match (v2 trigger reference)
    - cargo build exits 0 (include_str! picks up updated template without errors)
    - cargo test gemini exits 0 (no regression from template change)
  </acceptance_criteria>
  <done>All three grep commands return matches. `cargo build --lib` still clean (embedded template updated via include_str!). `cargo test gemini` still passes (separate from template change, but confirm no compile error).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Error reason string → TUI/CLI render | The reason string passes through format_error_row_colored; must not contain control chars or newlines |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | Gemini error reason string | accept | New reason "Gemini adapter deferred to v2 — see README §Gemini status" contains zero Gemini credentials, API keys, or secrets — vacuously passes SEC-03 grep (no secrets to disclose; D-65) |
| T-03-04-02 | Tampering | README §Gemini status ToS section | accept | Documentation-only; no code behavior change; content accurately reflects GEMINI_SPIKE.md findings |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan |
</threat_model>

<verification>
1. `cargo test gemini` passes (updated test assertions + new regression test)
2. `grep -n "not yet implemented" src/provider/gemini.rs` returns 0 matches
3. `grep -F "Gemini adapter deferred to v2 — see README §Gemini status" src/provider/gemini.rs` returns 1 match (reason string)
4. `grep -F "deferred to v2 stub (see README §Gemini status)" src/templates/default-config.toml` returns 1 match (D-64)
5. `grep -F "## Gemini adapter status — deferred to v2" README.md` returns 1 match (D-65)
6. `grep -F "gemini.google.com/usage" README.md` returns 1 match (ToS warning, SC-2)
7. `grep -F "Kill criteria" README.md` returns 1 match (v2 trigger reference)
8. `cargo build` compiles clean (include_str! picks up updated template)
9. Negative: `grep -rn "\-\-experimental-gemini" src/ README.md` returns 0 matches (D-63)
</verification>

<success_criteria>
- GeminiUnimplementedProvider::fetch error reason is exactly "Gemini adapter deferred to v2 — see README §Gemini status".
- All gemini tests pass including new regression test.
- default-config.toml Gemini comment updated per D-64.
- README §Gemini status section present with two-paragraph structure + ToS warning per D-65 + SC-2.
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-04-SUMMARY.md` when done.
</output>
