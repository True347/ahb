---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - Cargo.toml
  - src/engine/mod.rs
  - src/engine/cache.rs
  - src/cli/mod.rs
  - src/tui/mod.rs
autonomous: true
requirements:
  - TUI-03
  - ADP-05

must_haves:
  truths:
    - "TUI continues showing last-known provider state with a '(stale Ns ago)' suffix when a provider's fetch returns a transient error, instead of blanking the row (D-71 three-state)"
    - "AHB skips fetching a provider that was refreshed within its TTL window — repeated TUI ticks do not hammer an adapter that hasn't expired yet (D-71 path a)"
    - "Engine::refresh_all returns Vec<(ProviderId, RowOutcome)> where RowOutcome is Fresh|Stale|Failed (Q5)"
    - "Engine::new populates refresh_intervals: HashMap<ProviderId, Duration> clamping values < 5s to 5s with tracing::warn! (D-72)"
    - "Non-transient errors (Unavailable, SchemaDrift, Internal, Unconfigured) always produce RowOutcome::Failed regardless of cache (Q2 is_transient table)"
    - "CLI dispatch (run_compact / run_detailed / run_json) converts RowOutcome to Result<ProviderState, ProviderError> — RowOutcome::Stale maps to unreachable! because CLI cache is always empty (D-66, D-73)"
    - "tokio::time::interval(Duration::from_secs(15)) in src/tui/mod.rs is NOT changed (D-74)"
    - "JSON schema_version stays 1 (D-68)"
    - "No Timestamp::now() call added under src/provider/ or src/tui/widgets/ (BL-01)"
    - "The full binary compiles cleanly after this plan (SCAFFOLD adapter in tui/mod.rs bridges the RowOutcome→Result type gap until Plan 03-03 removes it)"
  artifacts:
    - path: "src/engine/cache.rs"
      provides: "CacheEntry, RowOutcome, is_transient helper"
      contains: "pub enum RowOutcome"
    - path: "src/engine/mod.rs"
      provides: "Engine with cache + refresh_intervals fields; Engine::refresh_all returns RowOutcome"
      contains: "moka::sync::Cache"
    - path: "Cargo.toml"
      provides: "moka dependency"
      contains: "moka = { version = \"0.12\""
    - path: "src/tui/mod.rs"
      provides: "SCAFFOLD adapter converting RowOutcome to Result at apply_results call site"
      contains: "SCAFFOLD: removed in Plan 03-03"
  key_links:
    - from: "src/engine/mod.rs"
      to: "src/engine/cache.rs"
      via: "Engine owns Cache<ProviderId, CacheEntry>"
      pattern: "CacheEntry"
    - from: "src/engine/mod.rs"
      to: "src/cli/mod.rs"
      via: "run_compact/detailed/json consume Vec<(ProviderId, RowOutcome)>"
      pattern: "RowOutcome"
    - from: "src/engine/mod.rs"
      to: "src/tui/mod.rs"
      via: "tui_loop calls engine.refresh_all(now).await and passes RowOutcome to apply_results (Plan 03)"
      pattern: "RowOutcome"
---

<objective>
Add `moka` dependency and implement the cache + refresh-interval engine layer. `Engine::refresh_all` return type changes from `Vec<(ProviderId, Result<ProviderState, ProviderError>)>` to `Vec<(ProviderId, RowOutcome)>`. CLI dispatch is updated to translate RowOutcome back to Result (CLI cache is always empty so Stale is unreachable). TUI wiring (Plan 03) and stale row rendering (Plan 03) are NOT done here.

Purpose: The architectural change that gates all stale-on-error behavior. After this plan, `cargo test` (including existing engine_row_order, cli_walking_skeleton, exit_codes, json_format_round_trip) passes green and CLI behavior is byte-identical to Phase 2. The whole binary compiles cleanly via a SCAFFOLD adapter in tui/mod.rs that Plan 03-03 removes.

Output: New src/engine/cache.rs, updated src/engine/mod.rs (2 new fields + refresh_all rewrite), updated src/cli/mod.rs (RowOutcome translation), updated Cargo.toml, updated src/tui/mod.rs (SCAFFOLD adapter — removed in Plan 03-03).
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-RESEARCH.md

<interfaces>
<!-- Key types extracted from codebase 2026-05-25 -->

From src/engine/mod.rs (lines 29-33) — current Engine struct:
```rust
pub struct Engine {
    providers: Vec<Arc<dyn Provider>>,
    secrets: Arc<Secrets>,
    per_provider_timeout: Duration,
}
```

From src/engine/mod.rs (lines 101-117) — current refresh_all:
```rust
pub async fn refresh_all(
    &self,
    now: jiff::Timestamp,
) -> Vec<(ProviderId, Result<ProviderState, ProviderError>)> {
    let mut results = fanout::refresh_all_inner(...).await;
    results.sort_by_key(|(id, _)| Self::sort_key(*id));
    results
}
```

From src/engine/fanout.rs (lines 34-100) — refresh_all_inner signature (DO NOT CHANGE):
```rust
pub async fn refresh_all_inner(
    providers: &[Arc<dyn Provider>],
    now: jiff::Timestamp,
    secrets: Arc<Secrets>,
    per_provider_timeout: Duration,
) -> Vec<(ProviderId, Result<ProviderState, ProviderError>)>
```

From src/model.rs (ProviderError variants — confirmed from lines 110-141):
```rust
pub enum ProviderError {
    Unconfigured,
    Unavailable { reason: String },
    SchemaDrift { missing: Vec<String> },
    Network { source: NetworkErr },
    RateLimited { retry_after: Option<jiff::Span> },
    Internal { source: anyhow::Error },
}
```

From src/cli/mod.rs — run_compact / run_detailed / run_json all call:
```rust
let results = engine.refresh_all(now).await;
// then iterate &results as (ProviderId, Result<ProviderState, ProviderError>)
```

Phase 3 changes: these functions must convert RowOutcome → Result before iterating.
DispatchOutcome::from_results signature: `from_results<T, E>(results: &[(ProviderId, Result<T, E>)]) -> Self`
Must be updated or a new conversion helper must exist.

From 03-CONTEXT.md § D-67: CacheEntry = { state: ProviderState, fetched_at: jiff::Timestamp }
From 03-RESEARCH.md § Q1: moka::sync::Cache — cache.get() returns Option&lt;V&gt; (a clone, not &V); CacheEntry must impl Clone.
ProviderState already derives Clone (verified src/model.rs line 76).

From 03-RESEARCH.md § Q2 — is_transient mapping:
| Variant     | is_transient |
|-------------|-------------|
| Unconfigured | false |
| Unavailable  | false |
| SchemaDrift  | false |
| Network      | true |
| RateLimited  | true |
| Internal     | false |

From 03-RESEARCH.md § Q4 — Engine::new owns cache internally. No cache injection point.

From 03-RESEARCH.md § Q10 — moka Cargo.toml entry:
moka = { version = "0.12", default-features = false, features = ["sync"] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add moka dep to Cargo.toml and create src/engine/cache.rs</name>
  <read_first>
    - Cargo.toml: existing dependency structure and comment style (Phase N comment pattern)
    - 03-RESEARCH.md § Q10: exact Cargo.toml entry with features = ["sync"]
    - 03-RESEARCH.md § Q1: moka::sync::Cache API — no TTL, max_capacity(8)
    - 03-CONTEXT.md § D-67: CacheEntry fields
    - 03-CONTEXT.md § D-66: in-memory only, never disk
    - 03-RESEARCH.md § Q2: is_transient exact variant mapping
    - src/model.rs: ProviderError variant names (confirm spelling before writing matches!)
  </read_first>
  <files>Cargo.toml, src/engine/cache.rs</files>
  <action>
    1. In Cargo.toml, add under the last Phase 2 production dependency (before [dev-dependencies]):
       ```
       # Phase 3 — moka stale-on-error cache (D-66, D-67). sync feature only; no TTL set
       # (manual stale semantics per D-71 — moka auto-eviction disabled).
       moka = { version = "0.12", default-features = false, features = ["sync"] }
       ```

    2. Create src/engine/cache.rs with:

       Module-level deny/warn matching other engine files:
       `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]`
       `#![warn(clippy::pedantic)]`

       Import: `use crate::model::{ProviderError, ProviderState};`

       CacheEntry struct (must derive Clone because moka::Cache::get returns a clone):
       ```rust
       #[derive(Debug, Clone)]
       pub struct CacheEntry {
           pub state: ProviderState,
           pub fetched_at: jiff::Timestamp,
       }
       ```

       RowOutcome enum (pub, used by Engine and CLI dispatch):
       ```rust
       #[derive(Debug)]
       pub enum RowOutcome {
           /// Provider data is fresh (either from a successful fetch or within TTL).
           Fresh(ProviderState),
           /// Last fetch failed transiently; serving cached data with stale age in seconds.
           Stale { state: ProviderState, stale_age_secs: u64 },
           /// Adapter returned a non-transient error (or no cache available for transient).
           Failed(ProviderError),
       }
       ```

       is_transient helper (pub(crate), used by Engine::refresh_all):
       ```rust
       pub(crate) fn is_transient(err: &ProviderError) -> bool {
           matches!(err, ProviderError::Network { .. } | ProviderError::RateLimited { .. })
       }
       ```
       Doc-comment: "Returns true only for Network and RateLimited errors — the two variants that represent recoverable transient conditions. Unavailable covers both Gemini stub (permanent) and adapter timeouts (structural) and does NOT trigger stale fallback (see 03-RESEARCH.md Q2)."

       Unit tests in `#[cfg(test)] mod tests`:
       - `is_transient_network_is_true`: `assert!(is_transient(&ProviderError::Network { source: NetworkErr("x".into()) }))`
       - `is_transient_rate_limited_is_true`: `assert!(is_transient(&ProviderError::RateLimited { retry_after: None }))`
       - `is_transient_unavailable_is_false`: `assert!(!is_transient(&ProviderError::Unavailable { reason: "x".into() }))`
       - `is_transient_schema_drift_is_false`
       - `is_transient_internal_is_false`: use `ProviderError::Internal { source: anyhow::anyhow!("x") }`
       - `is_transient_unconfigured_is_false`

    3. Declare the new module in src/engine/mod.rs: add `pub mod cache;` and `pub use cache::{CacheEntry, RowOutcome};` near the top.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test --lib engine::cache 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --lib engine::cache::tests::is_transient_network_is_true exits 0
    - cargo test --lib engine::cache::tests::is_transient_rate_limited_is_true exits 0
    - cargo test --lib engine::cache::tests::is_transient_unavailable_is_false exits 0
    - cargo test --lib engine::cache::tests::is_transient_schema_drift_is_false exits 0
    - cargo test --lib engine::cache::tests::is_transient_internal_is_false exits 0
    - cargo test --lib engine::cache::tests::is_transient_unconfigured_is_false exits 0
    - cargo build --lib exits 0
    - grep -F "moka" Cargo.lock returns >= 1 match (moka appears in dependency tree)
  </acceptance_criteria>
  <done>6 is_transient unit tests pass. cargo build --lib clean. moka appears in Cargo.lock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Engine struct and Engine::refresh_all with TTL filter + cache read/write</name>
  <read_first>
    - src/engine/mod.rs full file: current Engine struct (lines 29-33), Engine::new (lines 39-88), refresh_all (lines 101-117), sort_key (lines 119-129)
    - 03-CONTEXT.md § D-71: three-state timeline (TTL-hit → Fresh, TTL-expired+success → Fresh+update, TTL-expired+transient+cache → Stale, no-cache+fail → Failed)
    - 03-CONTEXT.md § D-72: clamp ≥5s with tracing::warn! format "refresh_interval for {id} clamped to 5s (was {raw}s)"
    - 03-CONTEXT.md § D-66: CLI path — cache is never used; stale can only appear in TUI
    - 03-RESEARCH.md § Q3: Option A — pre-filter + skip fanout for TTL-hit providers (recommended); emit Fresh from cache without calling fanout
    - 03-RESEARCH.md § Q4: Engine internal owns cache; no injection
    - 03-RESEARCH.md § Pitfall 16: if ALL providers are within TTL, still emit one RowOutcome::Fresh per provider — do NOT return empty Vec
    - src/provider/claude/mod.rs: confirms DEFAULT_REFRESH_INTERVAL_SECS = 15 (added by Plan 01)
    - 03-RESEARCH.md § Q8: cache fetched_at = state.fetched_at (already in ProviderState from FetchCtx); do NOT call jiff::Timestamp::now() in cache write
  </read_first>
  <files>src/engine/mod.rs</files>
  <behavior>
    - Test `engine_caches_successful_fetch`: create Engine with MockProvider, call refresh_all(now) twice; second call (if TTL not elapsed) returns Fresh from cache without hitting fanout.
    - Test `engine_returns_stale_on_transient_error_with_cache_hit`: cache has CacheEntry; Engine receives Network error from fanout → RowOutcome::Stale with correct stale_age_secs.
    - Test `engine_returns_failed_on_non_transient_error_regardless_of_cache`: cache has CacheEntry; Engine receives Unavailable error from fanout → RowOutcome::Failed (not Stale).
    - Test `refresh_interval_clamps_to_five_seconds_minimum`: Engine::new with config refresh_interval = Some(2) → stored interval is 5s (not 2s). (Cannot easily assert warn was emitted in unit test; document that integration test catches this.)
    - Test `engine_row_order_preserved_with_row_outcome`: sort_by_key still applies to RowOutcome vec.
  </behavior>
  <action>
    In src/engine/mod.rs:

    1. Add imports at the top:
       - `use std::collections::HashMap;`
       - `use moka::sync::Cache;`
       - `use crate::engine::cache::{CacheEntry, RowOutcome, is_transient};`
       - `use crate::provider::{claude, codex, gemini, mock};` (for DEFAULT_REFRESH_INTERVAL_SECS constants)

    2. Add two fields to Engine struct after `per_provider_timeout`:
       ```rust
       cache: Cache<ProviderId, CacheEntry>,
       refresh_intervals: HashMap<ProviderId, Duration>,
       ```

    3. In Engine::new, after the existing providers vec construction:
       a. Build `refresh_intervals: HashMap<ProviderId, Duration>` by iterating over enabled providers. For each ProviderId, read `cfg.providers.<id>.refresh_interval` (Option&lt;u64&gt; from Plan 01). If None, use the provider module's DEFAULT_REFRESH_INTERVAL_SECS. If Some(raw) and raw < 5, clamp to 5 and emit `tracing::warn!(provider = %id_str, raw = raw, "refresh_interval clamped to 5s")` where id_str is a display-friendly string (e.g., "claude", "codex"). Store `Duration::from_secs(clamped)`.

       b. Build cache: `Cache::builder().max_capacity(8).build()`. DO NOT set time_to_live or time_to_idle (D-66 / 03-RESEARCH Pitfall 1).

       c. Store both in Self { ..., cache, refresh_intervals }.

    4. Rewrite Engine::refresh_all signature and body:
       ```rust
       pub async fn refresh_all(&self, now: jiff::Timestamp) -> Vec<(ProviderId, RowOutcome)>
       ```

       Body (Option A — pre-filter + skip fanout for TTL-hit providers):
       a. Partition self.providers into two groups:
          - `needs_fetch`: providers whose TTL has elapsed (or no cache entry yet, treated as elapsed)
          - `from_cache`: providers within TTL — emit RowOutcome::Fresh(entry.state.clone()) directly

       b. TTL check for provider p: `refresh_intervals.get(&p.id())` gives the interval Duration. TTL elapsed = `now >= last_fetch_at + interval`. last_fetch_at = `cache.get(&p.id()).map(|e| e.fetched_at)`. If no cache entry, treat as elapsed (must fetch).

       c. For `needs_fetch` providers, call `fanout::refresh_all_inner(needs_fetch_slice, now, ...)`.

       d. For each fanout result (id, result):
          - Ok(state): update cache `cache.insert(id, CacheEntry { state: state.clone(), fetched_at: state.fetched_at })`; push `(id, RowOutcome::Fresh(state))`
          - Err(e) if `is_transient(&e)`: check cache. If hit: compute stale_age_secs = `(now.since((Unit::Second, entry.fetched_at))).ok().and_then(|s| i64::try_from(s.get_seconds()).ok()).and_then(|s| u64::try_from(s.max(0)).ok()).unwrap_or(0)` (pattern from RESEARCH External API Snippets). Push `(id, RowOutcome::Stale { state: entry.state.clone(), stale_age_secs })`. If no cache hit: push `(id, RowOutcome::Failed(e))`.
          - Err(e) non-transient: push `(id, RowOutcome::Failed(e))` regardless of cache.

       e. Combine from_cache results + fanout results into one Vec.

       f. Sort by Self::sort_key (unchanged — sort_key operates on ProviderId which we still have as tuple.0).

    5. Update the unit test that constructs Engine directly (in mod tests) — Engine::new signature unchanged externally but now takes config's refresh_interval. The existing tests use `Config::default()` which has `refresh_interval: None` → uses defaults. Confirm existing tests still compile.

    IMPORTANT: Do NOT change src/engine/fanout.rs at all. PITFALL: Do NOT call jiff::Timestamp::now() inside cache write — use state.fetched_at (already stamped by FetchCtx::now in the provider).
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test --lib engine 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --lib engine::tests::engine_caches_successful_fetch exits 0
    - cargo test --lib engine::tests::engine_returns_stale_on_transient_error_with_cache_hit exits 0
    - cargo test --lib engine::tests::engine_returns_failed_on_non_transient_error_regardless_of_cache exits 0
    - cargo test --lib engine::tests::refresh_interval_clamps_to_five_seconds_minimum exits 0
    - cargo test --lib engine::tests::engine_row_order_preserved_with_row_outcome exits 0
    - grep -v '^//' src/engine/mod.rs | grep -c 'moka::sync::Cache' returns >= 1
    - grep -rn "time_to_live\|time_to_idle" src/engine/ returns 0 matches
    - cargo build --lib exits 0
  </acceptance_criteria>
  <done>All engine unit tests pass including the 5 new behavioral tests. cargo build --lib clean. Engine::refresh_all returns Vec&lt;(ProviderId, RowOutcome)&gt;.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLI dispatch to translate RowOutcome, add SCAFFOLD adapter in tui/mod.rs, and verify full binary compiles</name>
  <read_first>
    - src/cli/mod.rs: run_compact (lines 145-180), run_detailed (lines 195-250), run_json path — all call engine.refresh_all and iterate results
    - src/cli/mod.rs: DispatchOutcome::from_results signature (line 45) — takes &[(ProviderId, Result&lt;T, E&gt;)] — will need a conversion or wrapper
    - 03-CONTEXT.md § D-66: CLI cache永遠空 → RowOutcome::Stale is IMPOSSIBLE in CLI path; map to unreachable!()
    - 03-CONTEXT.md § D-68: CLI render_text.rs and render_json.rs do NOT change — only the dispatch layer adapts
    - 03-RESEARCH.md § Slice 2 last bullet: "CLI cache always empty → never Stale; safe to map Fresh → Ok, Failed → Err, Stale → unreachable!()"
    - tests/cli_walking_skeleton.rs + tests/exit_codes.rs + tests/json_format_round_trip.rs: these must pass without any change
    - src/tui/mod.rs: tui_loop fetch tick arm — calls engine.refresh_all then app.apply_results
    - src/tui/app.rs: AppState::apply_results current signature (Result-typed) — Plan 03-03 will change this; for now, the SCAFFOLD adapter bridges the gap
  </read_first>
  <files>src/cli/mod.rs, src/tui/mod.rs</files>
  <action>
    === Changes to src/cli/mod.rs ===

    1. Add import: `use crate::engine::cache::RowOutcome;`

    2. Add a private helper function `outcome_to_result(outcome: RowOutcome) -> Result<ProviderState, ProviderError>` that maps:
       - `RowOutcome::Fresh(state)` → `Ok(state)`
       - `RowOutcome::Stale { .. }` → `unreachable!("CLI cache is always empty — RowOutcome::Stale cannot appear in CLI dispatch (D-66 + D-73)")`
       - `RowOutcome::Failed(e)` → `Err(e)`

    3. In run_compact: after `let results = engine.refresh_all(now).await;`, add:
       `let results: Vec<(ProviderId, Result<ProviderState, ProviderError>)> = results.into_iter().map(|(id, o)| (id, outcome_to_result(o))).collect();`
       Then the rest of run_compact's body is unchanged.

    4. In run_detailed: same pattern — convert results immediately after refresh_all.

    5. In run_json (wherever it calls engine.refresh_all): same pattern.

    6. DispatchOutcome::from_results already takes `Result<T, E>` generically — no change needed after the conversion.

    7. Do NOT modify src/cli/render_text.rs or src/cli/render_json.rs in this task.

    === Changes to src/tui/mod.rs (SCAFFOLD — removed in Plan 03-03) ===

    8. In src/tui/mod.rs, at both call sites that consume `engine.refresh_all(...).await` and pass results to `app.apply_results(...)`, add a temporary adapter that converts `Vec<(ProviderId, RowOutcome)>` back to the legacy `Vec<(ProviderId, Result<ProviderState, ProviderError>)>` shape expected by the current `AppState::apply_results`. Pattern:

       ```rust
       // SCAFFOLD: removed in Plan 03-03 when apply_results is updated to accept RowOutcome directly
       let legacy: Vec<(ProviderId, Result<ProviderState, ProviderError>)> = outcomes
           .into_iter()
           .map(|(id, outcome)| match outcome {
               RowOutcome::Fresh(state) | RowOutcome::Stale { state, .. } => (id, Ok(state)),
               RowOutcome::Failed(err) => (id, Err(err)),
           })
           .collect();
       app.apply_results(legacy);
       ```

       Apply this pattern at the priming fetch (lines ~112-113) AND at the fetch tick arm (lines ~144-147). In both places: rename the `results` binding to `outcomes` before the SCAFFOLD block so the adapter can consume it, then pass `legacy` to `apply_results`.

       Add the import `use crate::engine::cache::RowOutcome;` at the top of src/tui/mod.rs.

       The `// SCAFFOLD: removed in Plan 03-03` comment marker MUST appear at the start of the adapter block so Plan 03-03 Task 2 can locate and remove it with a grep.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo build 2>&1 | tail -20 && cargo test --lib cli 2>&1 | tail -20 && cargo test cli_walking_skeleton 2>&1 | tail -10 && cargo test exit_codes 2>&1 | tail -10 && cargo test json_format_round_trip 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - cargo build (full binary including TUI module) exits 0
    - cargo test --lib cli exits 0 (all CLI lib unit tests pass)
    - cargo test cli_walking_skeleton exits 0 (CLI output byte-identical to Phase 2)
    - cargo test exit_codes exits 0 (Gemini stub still exits 1 in Gemini-only config)
    - cargo test json_format_round_trip exits 0 (schema_version stays 1, D-68)
    - grep -v '^//' src/cli/render_text.rs | grep -c "stale" returns 0 (CLI render not modified, D-68)
    - grep -v '^//' src/cli/render_json.rs | grep -c "stale" returns 0 (D-68)
    - grep -F "SCAFFOLD: removed in Plan 03-03" src/tui/mod.rs returns >= 1 match (scaffold marker present)
  </acceptance_criteria>
  <done>Full binary (cargo build) compiles cleanly. CLI lib tests pass. cli_walking_skeleton, exit_codes, json_format_round_trip integration tests pass (CLI behavior is byte-identical to Phase 2). No stale path is reachable from CLI. SCAFFOLD adapter in tui/mod.rs keeps the binary compilable until Plan 03-03 removes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fanout results → cache write | ProviderState from adapter; fetched_at must come from FetchCtx::now not wall clock |
| cache read → RowOutcome::Stale | Stale state is user-visible; must not leak secrets or stale age beyond what state already contains |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | moka cache contents | accept | Cache is pure in-memory (D-66), never serialized to disk or --json output (D-68); no secret values are stored in ProviderState (ProviderState is a display DTO) |
| T-03-02-02 | Denial of Service | refresh_interval = 1s (below clamp) | mitigate | Clamp to ≥5s in Engine::new with tracing::warn!; D-72 binding |
| T-03-02-03 | Tampering | is_transient widens to include Unavailable | mitigate | is_transient is a private(crate) helper with 6 explicit unit tests; Unavailable returns false explicitly per Q2; widening requires test changes |
| T-03-02-04 | Elevation of Privilege | unreachable!() in CLI RowOutcome::Stale arm | accept | Stale requires TUI-long-lived cache which CLI never builds; the unreachable!() is a correctness assertion, not a user-triggered path |
| T-03-02-SC | Tampering | npm/pip/cargo installs | mitigate | moka 0.12.15 from crates.io; provenance: github.com/moka-rs/moka — verify at implementation time via `cargo search moka` |
</threat_model>

<verification>
1. `cargo test --lib engine` passes (Task 2 unit tests + Task 1 is_transient tests)
2. `cargo test refresh_interval_config_parse` still passes (Plan 01 not broken)
3. `cargo build` (full binary) exits 0 (SCAFFOLD adapter in tui/mod.rs keeps binary compilable)
4. `cargo test cli_walking_skeleton` passes — CLI output byte-identical to Phase 2 (D-68 D-66)
5. `cargo test exit_codes` passes — Gemini stub still exit 1 in Gemini-only config
6. `cargo test json_format_round_trip` passes — schema_version stays 1 (D-68)
7. `grep -c "tokio::time::interval(Duration::from_secs(15))" src/tui/mod.rs` returns 1 (D-74 — not changed)
8. `grep -rn "Timestamp::now()" src/provider/ src/tui/widgets/` returns 0 matches (BL-01)
9. `grep -rn "time_to_live\|time_to_idle" src/engine/` returns 0 matches (D-66 / RESEARCH Pitfall 1)
10. `grep -F "SCAFFOLD: removed in Plan 03-03" src/tui/mod.rs` returns >= 1 match
11. Negative: `grep -F "schema_version.*2" tests/json_format_round_trip.rs` returns 0 matches (D-68)
</verification>

<success_criteria>
- moka 0.12 added to Cargo.toml with features = ["sync"].
- src/engine/cache.rs exists with CacheEntry, RowOutcome, is_transient (6 unit tests green).
- Engine::refresh_all returns Vec&lt;(ProviderId, RowOutcome)&gt; with TTL filter + cache read/write.
- Engine::new clamps refresh_interval &lt; 5s to 5s with tracing::warn!.
- CLI dispatch converts RowOutcome → Result; Phase 2 integration tests all pass.
- SCAFFOLD adapter in src/tui/mod.rs bridges the type gap so the full binary compiles; marked with "SCAFFOLD: removed in Plan 03-03".
- No new Timestamp::now() callsites under src/provider/ or src/tui/widgets/ (BL-01).
- moka cache has no TTL/TTI configured (manual stale semantics per D-71).
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-02-SUMMARY.md` when done.
</output>
