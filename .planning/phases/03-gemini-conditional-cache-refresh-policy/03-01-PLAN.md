---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.rs
  - src/provider/claude/mod.rs
  - src/provider/codex/mod.rs
  - src/provider/gemini.rs
  - src/provider/mock.rs
  - tests/refresh_interval_config_parse.rs
autonomous: true
requirements:
  - CFG-03
  - TUI-03

must_haves:
  truths:
    - "User can set refresh_interval = 30 in config for any provider and AHB respects it next refresh cycle (D-72)"
    - "A typo like refresh_intervall = 10 produces a warning but does not crash AHB (D-38 forward-compat)"
    - "Values below 5 seconds are silently clamped to 5s with a warning — AHB never polls faster than 5s per provider (D-72 safety floor)"
    - "Absent refresh_interval means each provider uses its own 15s default — no config entry required for normal use (D-72)"
    - "No --experimental-gemini CLI flag introduced (D-63)"
  artifacts:
    - path: "src/config.rs"
      provides: "ProviderConfig with refresh_interval: Option<u64>"
      contains: "refresh_interval: Option<u64>"
    - path: "src/provider/claude/mod.rs"
      provides: "DEFAULT_REFRESH_INTERVAL_SECS = 15"
      contains: "pub const DEFAULT_REFRESH_INTERVAL_SECS"
    - path: "src/provider/codex/mod.rs"
      provides: "DEFAULT_REFRESH_INTERVAL_SECS = 15"
      contains: "pub const DEFAULT_REFRESH_INTERVAL_SECS"
    - path: "src/provider/gemini.rs"
      provides: "DEFAULT_REFRESH_INTERVAL_SECS = 15"
      contains: "pub const DEFAULT_REFRESH_INTERVAL_SECS"
    - path: "src/provider/mock.rs"
      provides: "DEFAULT_REFRESH_INTERVAL_SECS = 15"
      contains: "pub const DEFAULT_REFRESH_INTERVAL_SECS"
    - path: "tests/refresh_interval_config_parse.rs"
      provides: "parse roundtrip + clamp + unknown-key tests"
  key_links:
    - from: "src/config.rs"
      to: "src/engine/mod.rs"
      via: "Engine::new reads cfg.providers.<id>.refresh_interval"
      pattern: "refresh_interval"
    - from: "src/provider/claude/mod.rs"
      to: "src/engine/mod.rs"
      via: "Engine::new uses claude::DEFAULT_REFRESH_INTERVAL_SECS as fallback"
      pattern: "DEFAULT_REFRESH_INTERVAL_SECS"
---

<objective>
Add `refresh_interval: Option<u64>` to ProviderConfig (CFG-03) and publish `DEFAULT_REFRESH_INTERVAL_SECS = 15` from each provider module. Engine wiring happens in Plan 02 — this plan only establishes the config schema and constants so Plan 02 can read them without circular dependency.

Purpose: Establish the config schema extension that Plan 02 (cache + engine rewrite) will consume. After this plan, `cargo test refresh_interval_config_parse` is green and the engine is behaviorally unchanged (no cache logic yet).

Output: Updated src/config.rs, four provider-module constants, new integration test.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts extracted from codebase 2026-05-25. -->

From src/config.rs (lines 26-36):
```rust
const KNOWN_PROVIDER_FIELD_KEYS: &[&str] = &["enabled"];

#[derive(Debug, Clone, Default, Deserialize)]
pub struct ProviderConfig {
    #[serde(default)]
    pub enabled: bool,
}
```

From src/config.rs (line 30 region) — warn_unknown_keys walks the table using KNOWN_PROVIDER_FIELD_KEYS as the allow-list.

From src/provider/claude/mod.rs (line 29):
```rust
pub use window::CLAUDE_5H_TOKEN_LIMIT;
```
No DEFAULT_REFRESH_INTERVAL_SECS yet — add in this plan.

From src/provider/mock.rs — no pub const yet; add DEFAULT_REFRESH_INTERVAL_SECS.

From src/provider/gemini.rs (line 32) — GeminiUnimplementedProvider unit struct; add DEFAULT_REFRESH_INTERVAL_SECS at module level.

Engine::new signature (src/engine/mod.rs line 41):
```rust
pub fn new(cfg: Config, secrets: Secrets) -> Self
```
Engine does NOT read refresh_interval yet — that is Plan 02's job.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ProviderConfig with refresh_interval and update KNOWN_PROVIDER_FIELD_KEYS</name>
  <read_first>
    - src/config.rs lines 26-36: current KNOWN_PROVIDER_FIELD_KEYS + ProviderConfig struct
    - 03-CONTEXT.md § D-72: clamp ≥5s + warn format + no upper bound
    - 03-CONTEXT.md § D-38 (Phase 1): forward-compat unknown-key warn contract
    - 03-RESEARCH.md § Q9: rationale for Option&lt;u64&gt; type (u64 for Duration::from_secs, Option for "absent = use default")
  </read_first>
  <files>src/config.rs</files>
  <behavior>
    - Test: parse TOML `[providers.claude]\nenabled = true\nrefresh_interval = 30` → ProviderConfig { enabled: true, refresh_interval: Some(30) }
    - Test: parse TOML `[providers.claude]\nenabled = true` (no refresh_interval) → ProviderConfig { enabled: true, refresh_interval: None }
    - Test: parse TOML `[providers.claude]\nenabled = true\nrefresh_interval = 0` → ProviderConfig { enabled: true, refresh_interval: Some(0) } (clamp is engine's job, parse accepts u64 0)
    - Test: unknown key `refresh_intervall = 10` still triggers warn path (KNOWN_PROVIDER_FIELD_KEYS does NOT include the typo)
    - Test: `"refresh_interval"` IS in KNOWN_PROVIDER_FIELD_KEYS so it does NOT trigger the unknown-key warn
  </behavior>
  <action>
    In src/config.rs:

    1. Change line 30 from `&["enabled"]` to `&["enabled", "refresh_interval"]` for KNOWN_PROVIDER_FIELD_KEYS.

    2. Add `refresh_interval: Option<u64>` field to ProviderConfig after `enabled: bool`, decorated with `#[serde(default)]`. The field type is Option&lt;u64&gt; because u64 maps directly to Duration::from_secs, and Option means "absent = use per-provider default" with no need for a sentinel value. Negative TOML integers are automatically rejected by serde's u64 deserialization — no extra validation needed at the parse layer (D-72).

    3. Do NOT add `effective_refresh_interval` to config.rs — clamping logic lives in Engine::new (Plan 02). Config is a pure DTO.

    4. Add unit tests in the existing `#[cfg(test)] mod tests` block:
       - `provider_config_refresh_interval_parses_value`: parse TOML with refresh_interval = 30 → Some(30)
       - `provider_config_refresh_interval_absent_is_none`: parse TOML without refresh_interval → None
       - `provider_config_known_key_refresh_interval_does_not_warn`: use the existing unknown-key-warn test pattern to confirm "refresh_interval" is in KNOWN_PROVIDER_FIELD_KEYS (grep the array directly rather than running the warn walker; the integration test file covers the live warn path)
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test --lib config 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -v '^//' src/config.rs | grep -c 'refresh_interval: Option<u64>' returns >= 1
    - grep -v '^//' src/config.rs | grep -c '"refresh_interval"' returns >= 1 (within KNOWN_PROVIDER_FIELD_KEYS)
    - cargo test --lib config::tests::provider_config_refresh_interval_parses_value exits 0
    - cargo test --lib config::tests::provider_config_refresh_interval_absent_is_none exits 0
    - cargo test --lib config::tests::provider_config_known_key_refresh_interval_does_not_warn exits 0
    - cargo build --lib exits 0 with no errors
  </acceptance_criteria>
  <done>All config unit tests pass. ProviderConfig has refresh_interval: Option&lt;u64&gt;. KNOWN_PROVIDER_FIELD_KEYS contains "refresh_interval". cargo build --lib compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add DEFAULT_REFRESH_INTERVAL_SECS to each provider module</name>
  <read_first>
    - src/provider/claude/mod.rs line 29: existing pub use / pub const pattern
    - src/provider/codex/mod.rs: check existing pub const pattern
    - src/provider/mock.rs: no pub const today
    - src/provider/gemini.rs lines 32-49: GeminiUnimplementedProvider struct
    - 03-CONTEXT.md § D-72: all four providers default to 15s; gemini's value has no practical effect (stub never caches)
  </read_first>
  <files>
    src/provider/claude/mod.rs,
    src/provider/codex/mod.rs,
    src/provider/gemini.rs,
    src/provider/mock.rs
  </files>
  <action>
    In each of the four provider modules, add at the top of the public API region (after any existing pub use / pub const lines):

    ```
    pub const DEFAULT_REFRESH_INTERVAL_SECS: u64 = 15;
    ```

    With a doc-comment above it:
    ```
    /// Default per-provider refresh interval (D-72). Engine::new uses this when
    /// [providers.&lt;id&gt;] refresh_interval is absent from the config.
    ```

    For src/provider/gemini.rs: add above GeminiUnimplementedProvider. Note in a code comment: "This value has no practical effect — the stub always returns Err(Unavailable) and the cache is never populated, so the interval never triggers a real skip."

    Do NOT add this constant to src/provider/mod.rs or any shared location — each provider owns its own default (D-72 comment: "建議放在各 provider module 內"). Engine::new (Plan 02) will import each module's constant independently.

    Do NOT modify ProviderConfig, Engine, or any CLI dispatch path in this task.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo build --lib 2>&1 | tail -10 && grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/ | grep "pub const"</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --lib exits 0
    - grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/claude/mod.rs | grep -c "pub const" returns 1
    - grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/codex/mod.rs | grep -c "pub const" returns 1
    - grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/gemini.rs | grep -c "pub const" returns 1
    - grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/mock.rs | grep -c "pub const" returns 1
    - grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/ | grep -c "pub const" returns exactly 4
  </acceptance_criteria>
  <done>All four provider modules export `pub const DEFAULT_REFRESH_INTERVAL_SECS: u64 = 15`. `cargo build --lib` is clean. grep shows 4 matches (claude/mod.rs, codex/mod.rs, gemini.rs, mock.rs).</done>
</task>

<task type="auto">
  <name>Task 3: Integration test for refresh_interval config parsing and clamp behavior</name>
  <read_first>
    - src/config.rs: load_or_init + KNOWN_PROVIDER_FIELD_KEYS + warn_unknown_keys — understand how the unknown-key warn path fires
    - tests/ directory: look at existing integration test shape (e.g., tests/no_walltime_in_adapter.rs, tests/cli_walking_skeleton.rs) for file structure conventions
    - 03-RESEARCH.md § Q9 and § Test Surface for exact test names
  </read_first>
  <files>tests/refresh_interval_config_parse.rs</files>
  <action>
    Create tests/refresh_interval_config_parse.rs with these test functions:

    1. `refresh_interval_parses_from_toml`: write a tempfile config with `[providers.claude]\nenabled = true\nrefresh_interval = 30` and call `config::load_or_init`. Assert ProviderConfig.refresh_interval == Some(30).

    2. `refresh_interval_absent_deserializes_to_none`: config without refresh_interval → ProviderConfig.refresh_interval == None.

    3. `refresh_interval_zero_accepted_by_parser`: config with refresh_interval = 0 → ProviderConfig.refresh_interval == Some(0). (Clamping is Engine's job; parser accepts it.)

    4. `refresh_interval_large_value_accepted`: config with refresh_interval = 86400 (24h in seconds) → Some(86400). No upper bound in parser (D-72 "上限不設").

    5. `refresh_interval_typo_key_does_not_panic`: config with `refresh_intervall = 10` (double-l typo) — assert the load returns Ok (warn-and-ignore, no error). This test cannot easily intercept the tracing::warn! in integration context; asserting Ok(LoadOutcome::Loaded) is sufficient to prove the forward-compat path holds.

    Use `tempfile::NamedTempFile` for temp config files; use `ahb::config::load_or_init` (public API).

    Test names are the exact function names listed in 03-RESEARCH.md § Test Surface ("refresh_interval_config_parse.rs" block) so the `cargo test refresh_interval_config_parse` filter hits all five.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test refresh_interval_config_parse 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - cargo test refresh_interval_config_parse::refresh_interval_parses_from_toml exits 0
    - cargo test refresh_interval_config_parse::refresh_interval_absent_deserializes_to_none exits 0
    - cargo test refresh_interval_config_parse::refresh_interval_zero_accepted_by_parser exits 0
    - cargo test refresh_interval_config_parse::refresh_interval_large_value_accepted exits 0
    - cargo test refresh_interval_config_parse::refresh_interval_typo_key_does_not_panic exits 0
    - cargo test (full suite) exits 0 — no existing tests regressed
  </acceptance_criteria>
  <done>All five integration tests pass under `cargo test refresh_interval_config_parse`. No existing tests regress (verify with `cargo test` full suite).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file → ProviderConfig | User-editable TOML; serde parses into typed structs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | refresh_interval TOML value | mitigate | serde u64 deserialization automatically rejects negative integers and non-numeric values; no extra validation needed at parse layer |
| T-03-01-02 | Denial of Service | refresh_interval = 1 (below 5s clamp) | accept at parse layer | Clamping to ≥5s with tracing::warn! is Engine's responsibility (Plan 02); parser accepts all valid u64 |
| T-03-01-03 | Tampering | Unknown key in KNOWN_PROVIDER_FIELD_KEYS | accept | Forward-compat warn-and-ignore (D-38); typos warn but don't error — intentional design |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan (pure struct extension + const additions) |
</threat_model>

<verification>
1. `cargo test --lib config` passes (Task 1 unit tests)
2. `cargo build --lib` clean, no warnings
3. `grep -rn "DEFAULT_REFRESH_INTERVAL_SECS" src/provider/ | grep "pub const"` returns exactly 4 lines
4. `grep -n "refresh_interval" src/config.rs` returns hits in KNOWN_PROVIDER_FIELD_KEYS and ProviderConfig
5. `cargo test refresh_interval_config_parse` passes (5 integration tests)
6. `cargo test` full suite passes — existing tests unchanged (engine behavior not modified)
7. Negative regression: `grep -rn "\-\-experimental-gemini" src/` returns 0 matches (D-63)
</verification>

<success_criteria>
- ProviderConfig gains `refresh_interval: Option<u64>` parseable from TOML; absent = None (not 0).
- KNOWN_PROVIDER_FIELD_KEYS includes "refresh_interval" preventing false D-38 warns.
- All four provider modules export `pub const DEFAULT_REFRESH_INTERVAL_SECS: u64 = 15`.
- 5 new integration tests green; all prior tests unaffected.
- Engine behavior is byte-identical to Phase 2 (no cache logic yet).
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-01-SUMMARY.md` when done.
</output>
