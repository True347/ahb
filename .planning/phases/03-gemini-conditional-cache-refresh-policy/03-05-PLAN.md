---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - tests/cache_stale_on_error.rs
  - tests/no_walltime_in_adapter.rs
autonomous: true
requirements:
  - TUI-03
  - ADP-05

must_haves:
  truths:
    - "IntermittentFailureProvider drives a 3-tick sequence: success → Stale (Network) → success; RowOutcome shapes match expected at each tick (D-71 three-state)"
    - "Engine with IntermittentFailureProvider: tick 1 = Fresh, tick 2 = Stale{stale_age_secs >= 0}, tick 3 = Fresh (cache updated)"
    - "Engine with SchemaDrift error + prior cache hit: RowOutcome::Failed(SchemaDrift) — NOT Stale (Q2 non-transient)"
    - "No Timestamp::now() call under src/engine/ in the cache write site (BL-01 extension)"
    - "tests/no_walltime_in_adapter.rs scan extended to cover src/engine/ for defensive guardrail"
    - "Existing Phase 1/2 integration tests all still pass (no regressions)"
  artifacts:
    - path: "tests/cache_stale_on_error.rs"
      provides: "3-tick stale sequence + non-transient cache-bypass test"
      contains: "IntermittentFailureProvider"
    - path: "tests/no_walltime_in_adapter.rs"
      provides: "extended scan to src/engine/"
      contains: "src/engine"
  key_links:
    - from: "tests/cache_stale_on_error.rs"
      to: "src/engine/mod.rs"
      via: "Engine::refresh_all called with advancing now timestamps"
      pattern: "refresh_all"
    - from: "tests/cache_stale_on_error.rs"
      to: "src/engine/cache.rs"
      via: "RowOutcome::Fresh/Stale/Failed variants asserted"
      pattern: "RowOutcome"
---

<objective>
Belt-and-suspenders integration coverage for the stale-on-error flow: an IntermittentFailureProvider drives a 3-tick sequence verifying the D-71 three-state timeline. Extends no_walltime_in_adapter.rs to cover src/engine/ defensively.

Purpose: Confirms the Phase 3 cache + stale logic is correct end-to-end at the engine level, independent of TUI rendering. Also closes the BL-01 coverage gap on the engine cache write site.

Output: New tests/cache_stale_on_error.rs, updated tests/no_walltime_in_adapter.rs.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-RESEARCH.md

<interfaces>
<!-- Key types extracted from codebase + Plan 02/03 additions -->

From src/engine/mod.rs (after Plan 02) — Engine::new:
```rust
pub fn new(cfg: Config, secrets: Secrets) -> Self
```
Engine internally owns cache + refresh_intervals. Tests use Config with refresh_interval = Some(small_value) to control TTL without time.sleep.

From src/engine/cache.rs (after Plan 02):
```rust
pub enum RowOutcome {
    Fresh(ProviderState),
    Stale { state: ProviderState, stale_age_secs: u64 },
    Failed(ProviderError),
}
pub(crate) fn is_transient(err: &ProviderError) -> bool { ... }
```

From src/engine/fanout.rs (lines 111-163) — existing test provider pattern:
```rust
struct OkProvider;
struct SlowProvider { delay: Duration }
struct PanicProvider;
```
IntermittentFailureProvider follows this pattern using AtomicU64 call counter.

From src/model.rs — ProviderState construction (for Ok return in test):
```rust
ProviderState {
    id: ProviderId,
    windows: vec![HpWindow { ... }],
    fetched_at: jiff::Timestamp,  // must come from ctx.now — BL-01
    source: Cow::Borrowed("intermittent"),
}
```

From src/provider/mod.rs — FetchCtx:
```rust
pub struct FetchCtx<'a> {
    pub now: jiff::Timestamp,
    pub secrets: &'a Secrets,
}
```

From tests/no_walltime_in_adapter.rs (lines 30-40) — current scan dirs:
Currently covers src/provider/ and src/tui/widgets/ only.
src/engine/ is NOT scanned today — extend defensively.

From 03-RESEARCH.md § Q7 — IntermittentFailureProvider design:
```rust
struct IntermittentFailureProvider {
    call_count: AtomicU64,
    fail_every_nth: u64,   // fail on call n where n % fail_every_nth == 0
}
```
For 3-tick test: use fail_every_nth = 2 (fails on call 0, succeeds on call 1, fails on call 2)
OR: use a sequence-based approach (fails on exactly call 1 — i.e., the second call).

Recommended: use a Vec&lt;bool&gt; of outcomes (fail = true) protected by AtomicU64 index.
Tick 1 (call 0): Ok  → RowOutcome::Fresh
Tick 2 (call 1): Err(Network)  → RowOutcome::Stale (cache hit from tick 1)
Tick 3 (call 2): Ok  → RowOutcome::Fresh (cache updated; stale gone)

Clock advance: use Engine::refresh_all(now + duration) to simulate time advancing past TTL.
Set refresh_interval = Some(1) in config (clamped to 5s — so advance now by 6s between ticks).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/cache_stale_on_error.rs with IntermittentFailureProvider and 3-tick sequence</name>
  <read_first>
    - src/engine/fanout.rs lines 111-163: OkProvider / SlowProvider / PanicProvider pattern — copy structure for IntermittentFailureProvider
    - src/engine/mod.rs (after Plan 02): Engine::new signature + refresh_all signature
    - src/engine/cache.rs (after Plan 02): RowOutcome variants
    - 03-CONTEXT.md § D-71: three-state timeline (tick1=Fresh, tick2=Stale, tick3=Fresh-again)
    - 03-RESEARCH.md § Q7: IntermittentFailureProvider design
    - 03-RESEARCH.md § Q4: Engine internal owns cache; tests use short refresh_interval in Config
    - src/model.rs: ProviderState / HpWindow / ResetInfo construction pattern
    - tests/engine_row_order.rs or tests/no_walltime_in_adapter.rs: understand test file include pattern (how they import crate types)
  </read_first>
  <files>tests/cache_stale_on_error.rs</files>
  <behavior>
    - Test `engine_fresh_stale_fresh_three_tick_sequence`:
      - Tick 1 (now = T0): IntermittentFailureProvider returns Ok → RowOutcome::Fresh
      - Tick 2 (now = T0 + 6s, TTL=5s elapsed): IntermittentFailureProvider returns Err(Network) → RowOutcome::Stale { stale_age_secs: 6 } (within rounding tolerance: assert stale_age_secs >= 5 && <= 7)
      - Tick 3 (now = T0 + 12s): IntermittentFailureProvider returns Ok → RowOutcome::Fresh (cache updated; state from tick 3)
    - Test `engine_non_transient_error_does_not_stale_despite_cache`:
      - Tick 1: Ok → cache populated
      - Tick 2 (TTL elapsed): Err(Unavailable) → RowOutcome::Failed (NOT Stale — Unavailable is non-transient per Q2)
    - Test `engine_no_cache_with_transient_error_produces_failed`:
      - First ever call: Err(Network) with no prior cache → RowOutcome::Failed (no cache to serve)
    - Test `engine_cache_hit_within_ttl_returns_fresh_without_fetch`:
      - Two rapid calls (now does NOT advance past TTL): second call returns Fresh from cache, IntermittentFailureProvider fetch() is called only once (verify via call_count AtomicU64)
  </behavior>
  <action>
    Create tests/cache_stale_on_error.rs:

    1. Define IntermittentFailureProvider in a helper module or inline:
       ```rust
       use std::sync::atomic::{AtomicUsize, Ordering};
       use std::sync::Arc;

       struct ScriptedProvider {
           /// Sequence of outcomes: true = fail with Network, false = succeed
           script: Vec<bool>,
           call_idx: AtomicUsize,
       }
       ```
       impl Provider for ScriptedProvider:
       - id() → ProviderId::Mock
       - fetch(ctx): let idx = call_idx.fetch_add(1, Ordering::SeqCst); if idx >= script.len(), succeed (default). If script[idx] is true, return Err(ProviderError::Network { source: NetworkErr("simulated".into()) }). Else return Ok(ProviderState { id: ProviderId::Mock, windows: [...], fetched_at: ctx.now, source: Cow::Borrowed("scripted") }).

       Use a Vec&lt;bool&gt; script: [false, true, false] for the 3-tick test (false=succeed, true=fail).

    2. For each test: build Config with `mock: ProviderConfig { enabled: true, refresh_interval: Some(5) }`. Engine::new clamps Some(5) to 5s (≥5, no clamp needed — 5 is exactly the minimum). Advance `now` by 6s between ticks to exceed TTL.

    3. Three-tick test:
       ```
       let t0: jiff::Timestamp = "2026-05-25T12:00:00Z".parse()?;
       let t1 = t0 + jiff::Span::new().seconds(6);
       let t2 = t0 + jiff::Span::new().seconds(12);
       let results0 = engine.refresh_all(t0).await;  // tick 1: script[0]=false → Ok
       let results1 = engine.refresh_all(t1).await;  // tick 2: script[1]=true  → Err(Network) → Stale
       let results2 = engine.refresh_all(t2).await;  // tick 3: script[2]=false → Ok → Fresh
       ```
       Assert results0[0].1 matches RowOutcome::Fresh(_).
       Assert results1[0].1 matches RowOutcome::Stale { stale_age_secs, .. } where stale_age_secs is >= 5 (≤ 8 for tolerance).
       Assert results2[0].1 matches RowOutcome::Fresh(_).

    4. For `engine_cache_hit_within_ttl_returns_fresh_without_fetch` — use ScriptedProvider with script [false, true]. Call refresh_all(t0) twice WITHOUT advancing now (both calls at t0 — TTL of 5s not elapsed). The second call should return Fresh from cache WITHOUT calling fetch again, so script[1]=true never fires. Verify: extract call_idx after two calls → should be 1 (only one actual fetch).

    5. Use `#[tokio::test]` attribute on each async test function. Import: `use ahb::engine::{Engine, cache::RowOutcome};`, `use ahb::config::{Config, ProviderConfig, Providers};`, `use ahb::model::{ProviderError, ProviderId, ProviderState, HpWindow, ResetInfo, NetworkErr};`, `use ahb::provider::{Provider, FetchCtx};`, `use ahb::secrets::Secrets;`.

    Note on Arc&lt;dyn Provider&gt; in Engine::new: Engine::new takes a Config and builds providers internally. Tests cannot inject ScriptedProvider directly unless Engine exposes a test constructor. Check if Engine::new in Phase 3 has a test-only constructor, or if tests need to use a different approach.

    If Engine::new doesn't accept custom providers: The RESEARCH says "internal own; tests use config-level refresh_interval override." This means tests drive Engine through Config. But IntermittentFailureProvider requires injection. Resolution: Engine should expose a `#[cfg(test)] pub fn new_with_providers(providers: Vec<Arc<dyn Provider>>, cfg: Config) -> Self` constructor for testing, similar to how fanout.rs test helpers are defined in-module. Add this test constructor to src/engine/mod.rs if it doesn't exist, as part of this plan. The test constructor bypasses the provider-from-config logic but still builds cache + refresh_intervals from cfg.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test cache_stale_on_error 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 tests in cache_stale_on_error.rs pass. D-71 three-state timeline verified programmatically.</done>
</task>

<task type="auto">
  <name>Task 2: Extend no_walltime_in_adapter.rs scan to cover src/engine/ and run full regression suite</name>
  <read_first>
    - tests/no_walltime_in_adapter.rs lines 30-40: current scan dirs and grep pattern
    - 03-RESEARCH.md § Q8: cache fetched_at uses state.fetched_at (from FetchCtx::now) — src/engine/ should not call Timestamp::now() directly
    - 03-CONTEXT.md: authorized Timestamp::now() callsites are in src/main.rs and src/tui/mod.rs only; engine should use the `now` parameter passed to refresh_all
  </read_first>
  <files>tests/no_walltime_in_adapter.rs</files>
  <action>
    1. Read the current tests/no_walltime_in_adapter.rs to understand the grep structure.

    2. Add `"src/engine"` to the list of scan directories in the test. The existing test uses something like `for dir in &["src/provider", "src/tui/widgets"]` — extend to include "src/engine".

    3. Add a doc-comment explaining why src/engine/ is now scanned: "Phase 3 added cache write logic in src/engine/mod.rs. Cache entries use state.fetched_at (already stamped from FetchCtx::now) — no new Timestamp::now() calls should appear here. This scan is a defensive guardrail per 03-RESEARCH.md § Q8."

    4. Run the full test suite to confirm no Phase 1/2 regressions:
       - tests/engine_row_order.rs
       - tests/cli_walking_skeleton.rs
       - tests/exit_codes.rs
       - tests/json_format_round_trip.rs
       - tests/detailed_format.rs
       - tests/no_walltime_in_adapter.rs (updated)
       - tests/refresh_interval_config_parse.rs (from Plan 01)
       - tests/cache_stale_on_error.rs (from Task 1)
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test 2>&1 | tail -30</automated>
  </verify>
  <done>Full `cargo test` suite passes. no_walltime_in_adapter.rs scans src/engine/ and finds 0 Timestamp::now() calls there. All Phase 1/2 tests pass unmodified. All Phase 3 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test provider → Engine | ScriptedProvider returns synthetic ProviderState with ctx.now as fetched_at — BL-01 preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Timestamp::now() appearing in src/engine/ cache write | mitigate | Extended no_walltime_in_adapter.rs scan covers src/engine/ — any such call will fail the test |
| T-03-05-02 | Tampering | is_transient widening (Unavailable → true) | mitigate | Test `engine_non_transient_error_does_not_stale_despite_cache` explicitly asserts Unavailable produces RowOutcome::Failed not Stale |
| T-03-05-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan |
</threat_model>

<verification>
1. `cargo test cache_stale_on_error` passes (4 tests: 3-tick sequence, non-transient bypass, no-cache fail, TTL-hit no-fetch)
2. `cargo test no_walltime_in_adapter` passes with src/engine/ in scan scope
3. `cargo test` full suite passes (zero regressions across all Phase 0/1/2/3 tests)
4. `grep -rn "Timestamp::now()" src/engine/` returns 0 matches (BL-01 engine guardrail)
5. Positive: Phase 3 SC-4 verified — stale fallback appears in RowOutcome::Stale during transient error with prior cache
6. Positive: D-71 three-state timeline verified programmatically across 3 ticks
</verification>

<success_criteria>
- 4 integration tests in tests/cache_stale_on_error.rs pass, covering the D-71 three-state timeline.
- tests/no_walltime_in_adapter.rs extended to scan src/engine/.
- Full cargo test suite passes with zero regressions.
- Phase 3 SC-4 ("stale Ns ago indicator instead of blanking the row") verified at engine level.
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-05-SUMMARY.md` when done.
</output>
