---
phase: 03-gemini-conditional-cache-refresh-policy
plan: "05"
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/engine/mod.rs
  - tests/cache_stale_on_error.rs
  - tests/no_walltime_in_adapter.rs
autonomous: true
requirements:
  - TUI-03
  - ADP-05

must_haves:
  truths:
    - "IntermittentFailureProvider drives a 3-tick sequence: success → Stale (Network) → success; RowOutcome shapes match expected at each tick (D-71 three-state)"
    - "Engine with IntermittentFailureProvider: tick 1 = Fresh, tick 2 = Stale{stale_age_secs >= 0}, tick 3 = Fresh (cache updated)"
    - "Engine with SchemaDrift error + prior cache hit: RowOutcome::Failed(SchemaDrift) — NOT Stale (Q2 non-transient)"
    - "No Timestamp::now() call under src/engine/ in the cache write site (BL-01 extension)"
    - "tests/no_walltime_in_adapter.rs scan extended to cover src/engine/ for defensive guardrail"
    - "Setting one provider to a short interval (5s) and another to a long interval (600s) at t=10s fetches only the short-interval provider — the long-interval provider is served from cache (SC-3 multi-provider polling cadence)"
    - "Existing Phase 1/2 integration tests all still pass (no regressions)"
  artifacts:
    - path: "src/engine/mod.rs"
      provides: "#[cfg(test)] pub fn new_with_providers(providers, cfg) test constructor"
      contains: "new_with_providers"
    - path: "tests/cache_stale_on_error.rs"
      provides: "3-tick stale sequence + non-transient cache-bypass test + multi-provider cadence test"
      contains: "ScriptedProvider"
    - path: "tests/no_walltime_in_adapter.rs"
      provides: "extended scan to src/engine/"
      contains: "src/engine"
  key_links:
    - from: "tests/cache_stale_on_error.rs"
      to: "src/engine/mod.rs"
      via: "Engine::new_with_providers called with ScriptedProvider + advancing now timestamps"
      pattern: "new_with_providers"
    - from: "tests/cache_stale_on_error.rs"
      to: "src/engine/cache.rs"
      via: "RowOutcome::Fresh/Stale/Failed variants asserted"
      pattern: "RowOutcome"
---

<objective>
Belt-and-suspenders integration coverage for the stale-on-error flow: a ScriptedProvider drives a 3-tick sequence verifying the D-71 three-state timeline. A separate multi-provider cadence test verifies SC-3: different refresh_interval values per provider produce the expected polling cadence. Extends no_walltime_in_adapter.rs to cover src/engine/ defensively.

Purpose: Confirms the Phase 3 cache + stale logic is correct end-to-end at the engine level, independent of TUI rendering. Also closes the BL-01 coverage gap on the engine cache write site and verifies SC-3 multi-provider interval discrimination.

Output: Updated src/engine/mod.rs (test-only constructor), new tests/cache_stale_on_error.rs, updated tests/no_walltime_in_adapter.rs.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/03-gemini-conditional-cache-refresh-policy/03-RESEARCH.md

<interfaces>
<!-- Key types extracted from codebase + Plan 02/03 additions -->

From src/engine/mod.rs (after Plan 02) — Engine::new:
```rust
pub fn new(cfg: Config, secrets: Secrets) -> Self
```
Engine internally owns cache + refresh_intervals. Tests use Config with refresh_interval = Some(small_value) to control TTL without time.sleep.

From src/engine/cache.rs (after Plan 02):
```rust
pub enum RowOutcome {
    Fresh(ProviderState),
    Stale { state: ProviderState, stale_age_secs: u64 },
    Failed(ProviderError),
}
pub(crate) fn is_transient(err: &ProviderError) -> bool { ... }
```

From src/engine/fanout.rs (lines 111-163) — existing test provider pattern:
```rust
struct OkProvider;
struct SlowProvider { delay: Duration }
struct PanicProvider;
```
ScriptedProvider follows this pattern using AtomicUsize call counter + Vec&lt;bool&gt; script.

From src/model.rs — ProviderState construction (for Ok return in test):
```rust
ProviderState {
    id: ProviderId,
    windows: vec![HpWindow { ... }],
    fetched_at: jiff::Timestamp,  // must come from ctx.now — BL-01
    source: Cow::Borrowed("scripted"),
}
```

From src/provider/mod.rs — FetchCtx:
```rust
pub struct FetchCtx<'a> {
    pub now: jiff::Timestamp,
    pub secrets: &'a Secrets,
}
```

From tests/no_walltime_in_adapter.rs (lines 30-40) — current scan dirs:
Currently covers src/provider/ and src/tui/widgets/ only.
src/engine/ is NOT scanned today — extend defensively.

From 03-RESEARCH.md § Q7 — ScriptedProvider design using Vec&lt;bool&gt; sequence:
Tick 1 (call 0): Ok  → RowOutcome::Fresh
Tick 2 (call 1): Err(Network)  → RowOutcome::Stale (cache hit from tick 1)
Tick 3 (call 2): Ok  → RowOutcome::Fresh (cache updated; stale gone)

Clock advance: use Engine::refresh_all(now + duration) to simulate time advancing past TTL.
Set refresh_interval = Some(5) in config (≥5 minimum; advance now by 6s between ticks).

For SC-3 multi-provider cadence test:
Two providers (ProviderId::Mock mapped to "A" and a second provider mapped to "B"):
- Provider A: refresh_interval = Some(5)  → TTL = 5s
- Provider B: refresh_interval = Some(600) → TTL = 600s
At t=10s (both TTLs expired for first call): both fetched (both Fresh).
At t=11s (A's 5s TTL expired again, B's 600s TTL NOT expired):
  - Only A's fetch() is called (call_count A = 2)
  - B is served from cache (call_count B = 1)
Verify via AtomicUsize call_count on each ScriptedProvider.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test constructor to Engine and create tests/cache_stale_on_error.rs with 5 tests</name>
  <read_first>
    - src/engine/mod.rs (after Plan 02): Engine struct fields — understand cache + refresh_intervals to wire into the test constructor
    - src/engine/fanout.rs lines 111-163: OkProvider / SlowProvider / PanicProvider pattern — copy structure for ScriptedProvider
    - src/engine/cache.rs (after Plan 02): RowOutcome variants
    - 03-CONTEXT.md § D-71: three-state timeline
    - 03-RESEARCH.md § Q7: ScriptedProvider design
    - src/model.rs: ProviderState / HpWindow / ResetInfo construction pattern
    - tests/engine_row_order.rs or tests/no_walltime_in_adapter.rs: understand test file import pattern
  </read_first>
  <files>src/engine/mod.rs, tests/cache_stale_on_error.rs</files>
  <behavior>
    Test `engine_fresh_stale_fresh_three_tick_sequence`:
    - Tick 1 (now = T0): ScriptedProvider returns Ok → RowOutcome::Fresh
    - Tick 2 (now = T0 + 6s, TTL=5s elapsed): ScriptedProvider returns Err(Network) → RowOutcome::Stale { stale_age_secs in [5,8] range }
    - Tick 3 (now = T0 + 12s): ScriptedProvider returns Ok → RowOutcome::Fresh (cache updated)

    Test `engine_non_transient_error_does_not_stale_despite_cache`:
    - Tick 1: Ok → cache populated
    - Tick 2 (TTL elapsed): Err(Unavailable) → RowOutcome::Failed (NOT Stale)

    Test `engine_no_cache_with_transient_error_produces_failed`:
    - First ever call: Err(Network) with no prior cache → RowOutcome::Failed

    Test `engine_cache_hit_within_ttl_returns_fresh_without_fetch`:
    - Two rapid calls at T0 (TTL not elapsed): second call returns Fresh from cache; ScriptedProvider fetch() called only once (call_count == 1)

    Test `engine_multi_provider_different_intervals_only_stale_provider_fetched` (SC-3):
    - Provider A (TTL=5s) and Provider B (TTL=600s); both fetched at T0
    - At T0 + 6s (A's TTL expired, B's not): only A's fetch() is called (A call_count = 2, B call_count = 1)
    - B returned as Fresh from cache; A returned as Fresh (successful re-fetch)
  </behavior>
  <action>
    === Changes to src/engine/mod.rs ===

    Add a test-only constructor that bypasses provider-from-config logic. Place it inside `#[cfg(test)]`:
    ```rust
    #[cfg(test)]
    pub fn new_with_providers(
        providers: Vec<std::sync::Arc<dyn crate::provider::Provider>>,
        cfg: crate::config::Config,
        secrets: crate::secrets::Secrets,
    ) -> Self {
        // Build refresh_intervals from cfg exactly as Engine::new does,
        // but accept the providers vec directly instead of building from config.
        // Cache built the same way: Cache::builder().max_capacity(8).build().
        // ...
    }
    ```
    The constructor reuses the same refresh_interval clamping + cache-building logic from Engine::new. Extract this shared logic into a private helper `fn build_refresh_intervals(cfg: &Config) -> HashMap<ProviderId, Duration>` if it reduces duplication — otherwise inline it. Either approach is acceptable.

    === Create tests/cache_stale_on_error.rs ===

    1. Define ScriptedProvider inline in the test file:
       ```rust
       struct ScriptedProvider {
           id: ProviderId,
           script: Vec<bool>,          // true = fail with Network, false = succeed
           call_idx: AtomicUsize,
       }
       ```
       impl Provider for ScriptedProvider:
       - id() → self.id
       - fetch(ctx): let idx = call_idx.fetch_add(1, Ordering::SeqCst); if idx >= script.len(), succeed. If script[idx] is true, return Err(ProviderError::Network { source: NetworkErr("simulated".into()) }). Else return Ok(ProviderState { id: self.id, windows: [one HpWindow with some content], fetched_at: ctx.now, source: Cow::Borrowed("scripted") }).

    2. For each test: build Engine using `Engine::new_with_providers(providers, cfg, Secrets::default())`. Config has `mock: ProviderConfig { enabled: true, refresh_interval: Some(5) }` (TTL = 5s, no clamping needed since ≥5).

    3. Three-tick test:
       ```
       let t0: jiff::Timestamp = "2026-05-25T12:00:00Z".parse()?;
       let t1 = t0 + jiff::Span::new().seconds(6);
       let t2 = t0 + jiff::Span::new().seconds(12);
       ```
       script = [false, true, false] for tick1=Ok, tick2=Err(Network), tick3=Ok.

    4. For `engine_cache_hit_within_ttl_returns_fresh_without_fetch`: script = [false, true]. Call refresh_all(t0) twice without advancing now. Verify call_idx == 1 after both calls (second call served from cache, second script entry never reached).

    5. For SC-3 multi-provider cadence test (`engine_multi_provider_different_intervals_only_stale_provider_fetched`):
       - Create two ScriptedProviders: provider_a with ProviderId::Mock (or a suitable distinct ID) and provider_b with a different ProviderId.
       - provider_a config: refresh_interval = Some(5) (TTL 5s)
       - provider_b config: refresh_interval = Some(600) (TTL 600s)
       - Both scripts = [false, false, false] (always succeed)
       - Call refresh_all(t0): both fetched; call_count_a = 1, call_count_b = 1.
       - Call refresh_all(t0 + 6s): A's TTL (5s) elapsed → A fetched again; B's TTL (600s) NOT elapsed → B served from cache.
       - Assert: call_count_a == 2, call_count_b == 1 (B was NOT called the second time).
       - Assert both results are RowOutcome::Fresh (A re-fetched successfully, B from cache).
       Note: Two distinct ProviderId values are needed. Use Mock for provider_a; if the codebase has a second test-only ProviderId value, use it for provider_b. If not, check whether ScriptedProvider can be constructed with the same ProviderId (two instances) — if ProviderId is an enum, there may only be one Mock variant. In that case, use a different real variant (Claude, Codex) for provider_b with a matching config entry.

    6. Use `#[tokio::test]` attribute on each async test function. Import: `use ahb::engine::{Engine, cache::RowOutcome};`, `use ahb::config::{Config, ProviderConfig, Providers};`, `use ahb::model::{ProviderError, ProviderId, ProviderState, HpWindow, ResetInfo, NetworkErr};`, `use ahb::provider::{Provider, FetchCtx};`, `use ahb::secrets::Secrets;`, `use std::sync::atomic::{AtomicUsize, Ordering};`, `use std::borrow::Cow;`.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test cache_stale_on_error 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cargo test cache_stale_on_error::engine_fresh_stale_fresh_three_tick_sequence exits 0
    - cargo test cache_stale_on_error::engine_non_transient_error_does_not_stale_despite_cache exits 0
    - cargo test cache_stale_on_error::engine_no_cache_with_transient_error_produces_failed exits 0
    - cargo test cache_stale_on_error::engine_cache_hit_within_ttl_returns_fresh_without_fetch exits 0
    - cargo test cache_stale_on_error::engine_multi_provider_different_intervals_only_stale_provider_fetched exits 0
    - grep -F "new_with_providers" src/engine/mod.rs returns >= 1 match
    - grep -rn "Timestamp::now()" src/engine/ returns 0 matches (BL-01 engine scope)
  </acceptance_criteria>
  <done>All 5 tests in cache_stale_on_error.rs pass. D-71 three-state timeline verified programmatically. SC-3 multi-provider polling cadence verified. src/engine/mod.rs has #[cfg(test)] new_with_providers constructor.</done>
</task>

<task type="auto">
  <name>Task 2: Extend no_walltime_in_adapter.rs scan to cover src/engine/ and run full regression suite</name>
  <read_first>
    - tests/no_walltime_in_adapter.rs lines 30-40: current scan dirs and grep pattern
    - 03-RESEARCH.md § Q8: cache fetched_at uses state.fetched_at (from FetchCtx::now) — src/engine/ should not call Timestamp::now() directly
    - 03-CONTEXT.md: authorized Timestamp::now() callsites are in src/main.rs and src/tui/mod.rs only; engine should use the `now` parameter passed to refresh_all
  </read_first>
  <files>tests/no_walltime_in_adapter.rs</files>
  <action>
    1. Read the current tests/no_walltime_in_adapter.rs to understand the grep structure.

    2. Add `"src/engine"` to the list of scan directories in the test. The existing test uses something like `for dir in &["src/provider", "src/tui/widgets"]` — extend to include "src/engine".

    3. Add a doc-comment explaining why src/engine/ is now scanned: "Phase 3 added cache write logic in src/engine/mod.rs. Cache entries use state.fetched_at (already stamped from FetchCtx::now) — no new Timestamp::now() calls should appear here. This scan is a defensive guardrail per 03-RESEARCH.md § Q8."

    4. Run the full test suite to confirm no Phase 1/2 regressions:
       - tests/engine_row_order.rs
       - tests/cli_walking_skeleton.rs
       - tests/exit_codes.rs
       - tests/json_format_round_trip.rs
       - tests/detailed_format.rs
       - tests/no_walltime_in_adapter.rs (updated)
       - tests/refresh_interval_config_parse.rs (from Plan 01)
       - tests/cache_stale_on_error.rs (from Task 1)
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - cargo test no_walltime_in_adapter exits 0 (with src/engine/ now in scan scope)
    - cargo test engine_row_order exits 0
    - cargo test cli_walking_skeleton exits 0
    - cargo test exit_codes exits 0
    - cargo test json_format_round_trip exits 0
    - cargo test (full suite) exits 0 — zero regressions
    - grep -F "src/engine" tests/no_walltime_in_adapter.rs returns >= 1 match (scan dir added)
    - grep -rn "Timestamp::now()" src/engine/ returns 0 matches (BL-01 confirmed)
  </acceptance_criteria>
  <done>Full `cargo test` suite passes. no_walltime_in_adapter.rs scans src/engine/ and finds 0 Timestamp::now() calls there. All Phase 1/2 tests pass unmodified. All Phase 3 tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test provider → Engine | ScriptedProvider returns synthetic ProviderState with ctx.now as fetched_at — BL-01 preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Timestamp::now() appearing in src/engine/ cache write | mitigate | Extended no_walltime_in_adapter.rs scan covers src/engine/ — any such call will fail the test |
| T-03-05-02 | Tampering | is_transient widening (Unavailable → true) | mitigate | Test `engine_non_transient_error_does_not_stale_despite_cache` explicitly asserts Unavailable produces RowOutcome::Failed not Stale |
| T-03-05-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs in this plan |
</threat_model>

<verification>
1. `cargo test cache_stale_on_error` passes (5 tests: 3-tick sequence, non-transient bypass, no-cache fail, TTL-hit no-fetch, multi-provider cadence)
2. `cargo test no_walltime_in_adapter` passes with src/engine/ in scan scope
3. `cargo test` full suite passes (zero regressions across all Phase 0/1/2/3 tests)
4. `grep -rn "Timestamp::now()" src/engine/` returns 0 matches (BL-01 engine guardrail)
5. Positive: Phase 3 SC-3 verified — ScriptedProvider B with 600s interval is NOT called on second tick at t=6s
6. Positive: Phase 3 SC-4 verified — stale fallback appears in RowOutcome::Stale during transient error with prior cache
7. Positive: D-71 three-state timeline verified programmatically across 3 ticks
</verification>

<success_criteria>
- 5 integration tests in tests/cache_stale_on_error.rs pass, covering D-71 three-state timeline + SC-3 cadence.
- tests/no_walltime_in_adapter.rs extended to scan src/engine/.
- src/engine/mod.rs gains #[cfg(test)] new_with_providers constructor.
- Full cargo test suite passes with zero regressions.
- Phase 3 SC-3 ("different refresh_interval values produce expected polling cadence") verified at engine level.
- Phase 3 SC-4 ("stale Ns ago indicator instead of blanking the row") verified at engine level.
</success_criteria>

<output>
Create `.planning/phases/03-gemini-conditional-cache-refresh-policy/03-05-SUMMARY.md` when done.
</output>
