---
phase: 01-engine-claude-tui-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/main.rs
  - src/cli/mod.rs
  - src/cli/tty.rs
  - src/config.rs
  - src/templates/default-config.toml
  - src/engine/mod.rs
  - src/engine/fanout.rs
  - src/engine/events.rs
  - src/provider/claude/mod.rs
  - src/provider/claude/jsonl.rs
  - src/provider/claude/window.rs
  - src/provider/mod.rs
autonomous: true
requirements:
  - CORE-01
  - CORE-05
  - CFG-01
  - CFG-02
  - CFG-04
  - SEC-04
  - ADP-01
  - ADP-02
tags: [rust, cli, walking-skeleton, engine, claude, jsonl, config, tokio, jiff, tracing]

must_haves:
  truths:
    - "Running `AHB` on this machine (which has Claude Code installed and `~/.claude/projects/**/*.jsonl` populated) prints exactly one `claude` HP-bar line whose percent comes from real cluster-anchored cache_creation_input_tokens summation (D-33 amended per L1) and whose countdown is `(session_start + 5h) - ctx.now`."
    - "First-run on a clean machine (no `~/.config/ahb/config.toml`): AHB creates the default config from the embedded template, prints `initialized {path} — enable providers and rerun` to stdout, and exits 0 (D-37)."
    - "Disabling Claude in config (`enabled = false` for every provider) makes `AHB` print the empty-state line `no providers configured` and exit 0 — unconfigured providers are silently skipped, not flagged as failures (CFG-04 + UI-SPEC empty state)."
    - "`AHB | cat` emits zero ANSI escape bytes (CORE-05 + UI-SPEC Color color-off path 5: non-TTY)."
    - "Adding a deliberately-broken Claude config (point at a non-existent `~/.claude/projects` directory by overriding `home_dir`) causes AHB to print an ERROR row ending in a next-step hint (UI-SPEC error copy `claude  ERROR: ~/.claude/projects not found — is Claude Code installed?`), not crash. The engine never propagates an Err out of `refresh_all`."
    - "Engine `refresh_all` fans out via `tokio::task::JoinSet` with per-adapter `tokio::time::timeout(DEFAULT_PER_PROVIDER_TIMEOUT = 2s, p.fetch(ctx))` (D-28 + D-29); the engine maintains a `HashMap<task::Id, ProviderId>` so a `JoinError::is_panic()` becomes a `(ProviderId, Err(ProviderError::Internal))` pair, not a lost task (RESEARCH Pitfall L4)."
    - "`src/main.rs` is the only file that calls `jiff::Timestamp::now()`; `provider/claude/**/*.rs` use `ctx.now` exclusively (acceptance grep enforces)."
  artifacts:
    - path: "src/engine/mod.rs"
      provides: "Engine struct with `pub async fn refresh_all(&self) -> Vec<(ProviderId, Result<ProviderState, ProviderError>)>` + constructor `Engine::new(config: Config, secrets: Secrets) -> Self`"
      contains: "pub struct Engine"
    - path: "src/engine/fanout.rs"
      provides: "JoinSet fan-out with timeout + panic-to-Internal conversion"
      contains: "pub async fn refresh_all_inner"
    - path: "src/engine/events.rs"
      provides: "EngineEvent enum + EVENT_BUFFER const for Plan 03's mpsc plumbing"
      contains: "pub enum EngineEvent"
    - path: "src/provider/claude/mod.rs"
      provides: "ClaudeProvider implementing Provider trait; constructor takes `home_dir: &Path` for test injection"
      contains: "pub struct ClaudeProvider"
    - path: "src/provider/claude/jsonl.rs"
      provides: "Streaming JSONL parser tolerating trailing-line truncation (D-35); deserializes the 4 token fields (input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens) as `u64` with `#[serde(default)]` and only sums the cache_creation field (L1). Plan 02's drift detector uses a separate `serde_json::Value` re-parse path to distinguish missing-vs-zero — Plan 01 schema does NOT change."
      contains: "pub fn read_assistant_entries"
    - path: "src/provider/claude/window.rs"
      provides: "5h cluster anchor + `find_active_cluster` + `percent_remaining`; `pub const CLAUDE_5H_TOKEN_LIMIT: u64 = 44_000` with doc comment 'best-effort estimate; revisit quarterly' (D-44)"
      contains: "pub const CLAUDE_5H_TOKEN_LIMIT: u64 = 44_000"
    - path: "src/config.rs"
      provides: "Config + Providers + ProviderConfig structs; `pub fn load_or_init(path) -> anyhow::Result<Config>` + `pub fn default_path() -> anyhow::Result<PathBuf>`; D-37 first-run path; D-38 unknown-key warn"
      contains: "pub fn load_or_init"
    - path: "src/templates/default-config.toml"
      provides: "Embedded TOML template with 3 providers all `enabled = false` and markdown comments"
      contains: "[providers.claude]"
    - path: "src/cli/tty.rs"
      provides: "ColorMode enum + should_colorize(ColorMode, json_mode) decision function"
      contains: "pub fn should_colorize"
  key_links:
    - from: "src/provider/claude/window.rs"
      to: "cache_creation_input_tokens"
      via: "u64 summation across cluster"
      pattern: "cache_creation_input_tokens"
    - from: "src/engine/fanout.rs"
      to: "tokio::task::JoinSet"
      via: "spawn + join_next loop with HashMap<task::Id, ProviderId> bookkeeping"
      pattern: "JoinSet::new"
    - from: "src/main.rs"
      to: "ahb::engine::Engine::refresh_all"
      via: "single .await before render_text rendering loop"
      pattern: "engine.refresh_all"
    - from: "src/config.rs"
      to: "directories::ProjectDirs::from"
      via: "(\"\", \"\", \"ahb\") three-arg form"
      pattern: "ProjectDirs::from"
---

<objective>
Walking Skeleton: a `cargo run --release` of `AHB` (no args) on this machine prints exactly one `claude` HP-bar line computed from real `~/.claude/projects/**/*.jsonl`. This plan delivers the thinnest end-to-end vertical slice that exercises every architectural decision recorded in SKELETON.md — config load, engine fan-out (with 1 adapter), Claude adapter (file glob + streaming JSONL parser + 5h cluster anchor + cache_creation_input_tokens summation per L1), `Vec<Result<...>>` aggregation, and the existing `render_text::compact_line` Phase 0 renderer (extended to N rows + per-row error variant + color toggle).

Purpose: Prove the spine works end-to-end with real data BEFORE Plan 02 adds keyring/Secret hardening and Plan 03 adds the TUI surface. Every later plan plugs into THIS plan's engine without rewriting the contract.

Output: A working `AHB` binary that, on a Claude-Code-equipped machine with `[providers.claude] enabled = true`, prints `claude  ████████░░ {pct}% • resets in {Xh{MM}m}` (real numbers). On a clean machine, first-run inits the config and exits 0.

**Task structure (3 tasks):** Task 1a delivers the engine spine + Cargo.toml + config + CLI scaffolding against the existing MockProvider (7 files). Task 1b adds the Claude adapter (4 files) and wires Engine to consume both Mock and Claude. Task 3 wires main.rs dispatch + render extensions + integration tests against real Claude data (6 files). The split keeps each task within executor cognitive load while preserving all coverage from the original combined Task 1.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/CLAUDE.md
@/home/chasel/REPO/AIHPBar/.planning/PROJECT.md
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/STATE.md
@/home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-RESEARCH.md
@/home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-PATTERNS.md
@/home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-UI-SPEC.md
@/home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-SKELETON.md

<interfaces>
<!-- Phase 0 LOCKED types — extracted from src/. Executor MUST use these signatures verbatim. -->

From src/model.rs (UNCHANGED — do NOT modify):
```
pub enum ProviderId { Claude, Codex, Gemini, Mock }   // #[serde(rename_all = "snake_case")]
pub type HpUnit = f32;
pub enum BarColor { Red, Yellow, Green }
pub struct ResetInfo { pub resets_at: jiff::Timestamp }
pub struct HpWindow { pub label: Cow<'static,str>, pub percent_remaining: HpUnit,
                      pub reset: ResetInfo, pub bar_color: Option<BarColor> }
pub struct ProviderState { pub id: ProviderId, pub windows: Vec<HpWindow>,
                           pub fetched_at: jiff::Timestamp, pub source: Cow<'static,str> }
pub enum ProviderError { Unconfigured, Unavailable{reason:String}, SchemaDrift{missing:Vec<String>},
                         Network{source:NetworkErr}, RateLimited{retry_after:Option<jiff::Span>},
                         Internal{source:anyhow::Error} }
impl From<anyhow::Error> for ProviderError { /* yields Self::Internal { source } */ }
```

From src/provider/mod.rs (UNCHANGED contract; Task 1b adds `pub mod claude;` line):
```
pub struct FetchCtx<'a> { pub now: jiff::Timestamp, pub secrets: &'a Secrets }   // Copy
#[async_trait]
pub trait Provider: Send + Sync + 'static {
    fn id(&self) -> ProviderId;
    async fn fetch(&self, ctx: &FetchCtx<'_>) -> Result<ProviderState, ProviderError>;
}
```

From src/cli/render_text.rs (Phase 0 EXISTING — extend, do NOT rewrite):
```
pub const BAR_WIDTH: usize = 10;
pub fn compact_line(state: &ProviderState, now: &jiff::Timestamp, ascii: bool) -> String
// Phase 0 single-row debug_assert that windows.len() == 1 — Task 3 widens compact_line
// (or wraps it) so a multi-row CLI session works; preserve the byte-exact format string
// "{label}  {bar} {pct}% {sep} resets in {countdown}".
//
// Also: filled_cells and format_countdown are PROMOTED to `pub(crate) fn` in Task 3 so
// Plan 03's TUI widget can re-import them without duplication or scoped clippy allow drift.
```

From src/main.rs (Phase 0 — Task 3 replaces the body but PRESERVES `install_phase0_panic_hook` as the first line per D-27):
```
fn install_phase0_panic_hook() { /* take_hook + set_hook composition — DO NOT remove */ }
#[tokio::main(flavor = "current_thread")] async fn main() -> anyhow::Result<()>
```

From src/secrets.rs (Phase 0 stub — UNCHANGED in this plan; Plan 02 widens):
```
pub struct Secrets;   // Default + Clone + Debug
```

Wall-clock injection rule (Phase 0 acceptance contract — extended here to claude.rs):
- `jiff::Timestamp::now()` is called ONLY from `src/main.rs` (currently 1 callsite at line 68).
- All adapters MUST use `ctx.now`; acceptance grep over `src/provider/` MUST return zero `Timestamp::now` matches outside comments.

Lint floor (inherited from src/lib.rs):
```
#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]
#![warn(clippy::pedantic)]
```
Every new `src/**/*.rs` MUST start with these two attributes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Engine spine + Cargo.toml dep upgrade + Config + CLI/TTY scaffolding (against existing MockProvider)</name>
  <files>Cargo.toml, src/lib.rs, src/engine/mod.rs, src/engine/fanout.rs, src/engine/events.rs, src/config.rs, src/templates/default-config.toml, src/cli/tty.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/Cargo.toml (existing 9 prod + 1 dev dep with phase-comment convention to preserve)
    - /home/chasel/REPO/AIHPBar/src/lib.rs (module exports + lint floor)
    - /home/chasel/REPO/AIHPBar/src/provider/mod.rs (FetchCtx + Provider trait + `pub mod mock;` pattern — Task 1a uses existing MockProvider as Engine's only adapter; Task 1b adds Claude)
    - /home/chasel/REPO/AIHPBar/src/provider/mock.rs (EXACT analog Engine will spawn into JoinSet — exercise the spine end-to-end against this in Task 1a's tests)
    - /home/chasel/REPO/AIHPBar/src/model.rs (Phase 0 LOCKED types — read so executor does not redefine)
    - /home/chasel/REPO/AIHPBar/src/cli/render_text.rs (filled_cells analog for scoped clippy cast pattern — Task 1a does NOT modify, just patterns)
    - /home/chasel/REPO/AIHPBar/src/secrets.rs (Phase 0 stub shape — module-doc evolution pattern to mirror in config.rs)
    - /home/chasel/REPO/AIHPBar/clippy.toml (disallowed-types — may need entries added for future TUI but NOT in this task)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-CONTEXT.md (D-28, D-29, D-36, D-37, D-38, D-39)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-RESEARCH.md sections "Pattern 1: Engine fan-out via JoinSet", "Pattern 4: TTY-aware color decision", "Pitfall L4", "Code Examples 1"
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-PATTERNS.md sections covering engine/, config.rs, cli/tty.rs
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-UI-SPEC.md (compact line layout + empty state copy)
  </read_first>
  <behavior>
    - Cargo.toml: 8 new prod deps added with phase-comments (`ratatui = "0.30"`, `tokio` features upgraded to `["rt-multi-thread","macros","fs","time","signal","sync"]`, `keyring-core = "1"`, `zeroize = "1.8"`, `glob = "0.3"`, `directories = "6"`, `toml = "0.8"`, `tracing = "0.1"`, `tracing-subscriber = { version="0.3", features=["env-filter"] }`); 4 new dev-deps (`tempfile`, `assert_cmd`, `predicates`, `regex`); NO direct `crossterm` entry (PITFALLS.md double-version hazard — comment in Cargo.toml explains).
    - `engine::Engine::new(config: Config, secrets: Secrets)` constructs an engine holding `providers: Vec<Arc<dyn Provider>>`. In Task 1a this only consults `cfg.providers.mock.enabled` (added as a power-user knob — `[providers.mock]` is NOT in the default template, set by hand) and pushes `Arc::new(MockProvider)` if true. Claude / Codex / Gemini wiring is in Task 1b / Phase 2/3; for Phase 1 Task 1a, a `tracing::debug!` notes that other providers are not yet implemented when their `enabled = true` flag is set. (Task 1b extends this to push `Arc::new(ClaudeProvider::new(...))` when `cfg.providers.claude.enabled`.)
    - `engine::Engine::refresh_all(&self, now: jiff::Timestamp) -> Vec<(ProviderId, Result<ProviderState, ProviderError>)>` constructs a `FetchCtx { now, secrets: &self.secrets }`, then delegates to `fanout::refresh_all_inner(&self.providers, &ctx, DEFAULT_PER_PROVIDER_TIMEOUT)`.
    - `engine::fanout::refresh_all_inner` uses `JoinSet` + `tokio::time::timeout(per_provider_timeout, p.fetch(...))` per D-28+D-29, maintains `HashMap<tokio::task::Id, ProviderId>` keyed by `set.spawn(...).id()`, and on `Err(JoinError)` where `je.is_panic()` produces `(id_map[&je.id()], Err(ProviderError::Internal { source: anyhow::anyhow!("adapter panicked: {pid:?}") }))`. Returns `Vec<(ProviderId, Result<ProviderState, ProviderError>)>` matching Phase 0 signature.
    - `engine::events`: declares `pub enum EngineEvent { Refresh(Vec<(ProviderId, Result<ProviderState, ProviderError>)>), TickError { source: anyhow::Error }, Shutdown }` + `pub const EVENT_BUFFER: usize = 64;` (used by Plan 03; declared here so Plan 03 does not add to engine/).
    - `config::Config { providers: Providers }` with `Providers { claude: ProviderConfig, codex: ProviderConfig, gemini: ProviderConfig, mock: ProviderConfig }` and `ProviderConfig { enabled: bool }`, all `#[derive(Debug, Clone, Default, serde::Deserialize)]`, NO `deny_unknown_fields` (D-38). Mock is included so Plan 02's panic-injection test can opt in via config; the default template does NOT mention mock (power-user-only knob). `pub fn default_path() -> anyhow::Result<PathBuf>` returns `directories::ProjectDirs::from("", "", "ahb").ok_or_else(...)?.config_dir().join("config.toml")` (D-39). `pub fn load_or_init(path: &Path) -> anyhow::Result<LoadOutcome>` where `pub enum LoadOutcome { Initialized(PathBuf), Loaded(Config) }`: on missing → `create_dir_all(parent)` + `write(path, DEFAULT_CONFIG)` (DEFAULT_CONFIG via `include_str!("templates/default-config.toml")`) + `println!("initialized {} — enable providers and rerun", path.display())` + return `Initialized(path)` (D-37; main.rs decides to exit). On present → read+parse via `toml::from_str`; then `toml::Value` pre-pass to detect unknown top-level keys under `[providers.<known>]` and emit `tracing::warn!("unrecognized config key '{key}' — see README")` for each (D-38). Returns `Loaded(Config)`.
    - `src/templates/default-config.toml`: verbatim content from CONTEXT `<specifics>` 6th bullet — three `[providers.<name>]` tables (claude, codex, gemini), all `enabled = false`, the markdown-comment header `# AHB — AI HP Bar config. Enable the providers you have subscriptions for.` then a blank-comment line then `# Run \`AHB\` after editing — uncovered providers are silently skipped (not flagged as failures).`. Mock is NOT in the template.
    - `cli::tty::ColorMode { Auto, Always, Never }` clap ValueEnum + `pub fn should_colorize(cli_flag: ColorMode, json_mode: bool, is_tty: bool, no_color_env: bool) -> bool` implementing the priority chain `--json → --color=never → --color=always → NO_COLOR → IsTerminal → default true` (RESEARCH Pattern 4 verbatim). Also provide a thin wrapper `pub fn should_colorize_env(cli: ColorMode, json: bool) -> bool` that reads env + stdout TTY state and delegates — keeps the pure fn testable without env manipulation. cli::mod.rs and Cli struct are added in Task 3 (CLI dispatch wiring) — Task 1a just creates `cli/tty.rs` and a minimal `cli/mod.rs` re-export (`pub mod tty; pub use tty::ColorMode;`).
    - `lib.rs`: add `pub mod engine;` and `pub mod config;` exports + `pub mod cli;` if not present already (Phase 0 already has it).
    - All new `*.rs` start with `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)] #![warn(clippy::pedantic)]` (PATTERNS.md Crate-root lint floor section). Any `unwrap`/`expect`/`panic` outside tests must be scoped `#[allow]` with a comment.
    - Tests (inline `#[cfg(test)] mod tests`): per file
      * `engine/fanout.rs`: test that a panicking adapter (using a one-shot `struct PanicProvider;` inside test) becomes `(ProviderId::X, Err(Internal))` not a lost task; test that a slow adapter (`async { sleep(Duration::from_secs(10)).await; ... }`) returns `Err(Unavailable { reason })` after 100ms test-only timeout.
      * `engine/mod.rs`: test that `Engine::new` consuming a config with `[providers.mock] enabled = true` produces an engine whose `refresh_all` returns exactly one row for `ProviderId::Mock` with `Ok(state)` (proves the spine end-to-end against MockProvider before Task 1b plugs Claude in).
      * `config.rs`: test `load_or_init` against `tempfile::tempdir()` — missing path returns `LoadOutcome::Initialized(p)` and the file actually exists on disk afterwards; present path with sample TOML returns `Loaded(_)`. Unknown-key test: TOML containing `[providers.claude]\nenabled = true\nbogus = 1` — `Loaded(_)` returned, `bogus` key triggers `tracing::warn` (capture via `tracing_subscriber` test layer or simpler: scan parsed TOML keys against `["claude","codex","gemini","mock"]` for top-level + `["enabled"]` for nested; assert at least one unknown key detected).
      * `cli/tty.rs`: test all 6 paths from RESEARCH Pattern 4 truth table on the pure `should_colorize(cli_flag, json, is_tty, no_color)` fn (json_mode=true → false; Never → false; Always → true; Auto+no_color=true → false; Auto+no_color=false+is_tty=false → false; Auto+no_color=false+is_tty=true → true) — no env manipulation needed.
  </behavior>
  <action>Implement Task 1a per the behavior block. (1) Cargo.toml: add 8 prod deps + 4 dev-deps with per-line phase comments matching the Phase 0 convention (CONTEXT Established Patterns #5); set `tokio.features` to the exact list `["rt-multi-thread","macros","fs","time","signal","sync"]`; keep `default-features = false`. Do NOT add `crossterm` (PATTERNS Shared `Disallowed-types`). (2) Create the 7 source files listed in `files_modified` (excluding existing Phase 0 files). (3) Apply the lint-floor 2-line preamble at the top of every new `*.rs`. (4) Use the EXACT type names, signatures, constant values, serde field names, and crate paths named in the behavior block (`DEFAULT_PER_PROVIDER_TIMEOUT = Duration::from_secs(2)` per D-29, `EVENT_BUFFER = 64`, ProjectDirs `from("", "", "ahb")` per D-39, `pub enum LoadOutcome { Initialized(PathBuf), Loaded(Config) }`). (5) `Providers` struct INCLUDES `mock: ProviderConfig` so Plan 02 can opt mock in via config — the template `default-config.toml` does NOT mention mock. (6) Inline tests on each new file per behavior block — every business-logic file ships its own tests in the same commit; use `tempfile` for filesystem tests; `Secrets::default()` + frozen `jiff::Timestamp` for FetchCtx construction (mock.rs::tests pattern); test `Engine::new` + `refresh_all` end-to-end against the existing `MockProvider`.</action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -20 &amp;&amp; cargo test --lib 2>&1 | tail -30 &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'JoinSet::new' src/engine/fanout.rs` returns at least 1
    - Source: `grep -c 'tokio::time::timeout' src/engine/fanout.rs` returns at least 1
    - Source: `grep -c 'DEFAULT_PER_PROVIDER_TIMEOUT' src/engine/fanout.rs src/engine/mod.rs` returns at least 1 (defined exactly once)
    - Source: `grep -c 'is_panic' src/engine/fanout.rs` returns at least 1 (Pitfall L4 fix)
    - Source: `grep -F 'ProjectDirs::from("", "", "ahb")' src/config.rs` returns the call
    - Source: `grep -F 'include_str!' src/config.rs` returns a line referencing `templates/default-config.toml`
    - Source: `grep -F 'initialized' src/config.rs` returns a line containing the D-37 literal `enable providers and rerun`
    - Source: `grep -F 'deny_unknown_fields' src/config.rs` returns nothing (D-38: warn + ignore, not deny)
    - Source: `grep -F 'pub enum LoadOutcome' src/config.rs` returns the enum definition
    - Source: `grep -c 'mock: ProviderConfig' src/config.rs` returns at least 1 (Providers struct includes mock for Plan 02 opt-in)
    - Source: `grep -c 'crossterm' Cargo.toml` returns zero (PITFALLS.md double-version hazard)
    - Source: `grep -F 'rt-multi-thread' Cargo.toml` returns the tokio features line containing all six features
    - Source: `grep -c '#\\!\\[deny(clippy::unwrap_used' src/engine/mod.rs src/engine/fanout.rs src/engine/events.rs src/config.rs src/cli/tty.rs` returns 5 (one per new file in Task 1a)
    - Test: `cargo test --lib engine::fanout 2>&1 | grep -E 'test result: ok\\. [1-9]'` passes
    - Test: `cargo test --lib engine:: 2>&1 | grep -E 'test result: ok\\. [1-9]'` passes (Engine::refresh_all against MockProvider end-to-end)
    - Test: `cargo test --lib config 2>&1 | grep -E 'test result: ok\\. [1-9]'` passes
    - Test: `cargo test --lib cli::tty 2>&1 | grep -E 'test result: ok\\. [1-9]'` passes
    - CI: `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - CI: `cargo build --release` exits 0
  </acceptance_criteria>
  <done>Task 1a delivers the engine spine + config + cli/tty scaffolding running against the existing MockProvider. The crate compiles in release mode with all eight new prod deps + four new dev-deps wired; `Engine::new` consumes a `Config` and produces an engine that fans out via `JoinSet` (timeout + panic recovery via `task::Id` map per Pitfall L4); `config::load_or_init` returns the `LoadOutcome` enum (no internal exit calls — main.rs decides); `cli::tty::should_colorize` is the pure decision fn with all 6 paths tested. Clippy clean with `-D warnings`. No `crossterm`, no `deny_unknown_fields`. Task 1b plugs the real Claude adapter into this spine next.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Claude adapter implementation + wire into Engine</name>
  <files>src/provider/mod.rs, src/provider/claude/mod.rs, src/provider/claude/jsonl.rs, src/provider/claude/window.rs, src/engine/mod.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/src/provider/mod.rs (output of Phase 0 — Task 1b adds `pub mod claude;` line)
    - /home/chasel/REPO/AIHPBar/src/provider/mock.rs (EXACT analog for ClaudeProvider per PATTERNS.md — import order, async_trait skeleton, ctx.now usage, Cow::Borrowed source, frozen-clock test pattern)
    - /home/chasel/REPO/AIHPBar/src/model.rs (Phase 0 LOCKED types — read so executor does not redefine)
    - /home/chasel/REPO/AIHPBar/src/cli/render_text.rs (format_countdown analog for window.rs jiff math; filled_cells analog for scoped clippy cast pattern)
    - /home/chasel/REPO/AIHPBar/src/engine/mod.rs (Task 1a output — extend `Engine::new` to also push `Arc::new(ClaudeProvider::new(...))` when `cfg.providers.claude.enabled`)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-CONTEXT.md (D-32, D-33 AMENDED, D-35, D-44)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-RESEARCH.md sections "Pitfall L1", "Pitfall L5", "Pitfall L8", "Code Examples 2 + 3 + 4"
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-PATTERNS.md sections covering provider/claude/
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-UI-SPEC.md (error row format with required next-step hint)
  </read_first>
  <behavior>
    - `provider::claude::ClaudeProvider { base_path: PathBuf, token_limit: u64 }` with `pub fn new(home_dir: &Path, token_limit: u64) -> Self { Self { base_path: home_dir.join(".claude").join("projects"), token_limit } }` (RESEARCH Example 4 verbatim). `impl Provider`: `id() -> ProviderId::Claude`; `fetch(&self, ctx)` → glob `{base_path}/**/*.jsonl` (returns `Err(ProviderError::Unavailable { reason: "~/.claude/projects not found — is Claude Code installed?".into() })` if `!base_path.exists()` so error row carries the UI-SPEC literal), call `jsonl::read_assistant_entries` on each, sort merged Vec by `timestamp`, call `window::find_active_cluster(&sorted, ctx.now)`, build a single `HpWindow` (label `"claude-5h"`, percent computed via `window::percent_remaining(cluster.used_tokens, self.token_limit)`, reset `ResetInfo { resets_at: cluster.reset_at }`, `bar_color: None` so renderer derives from percent), wrap in `ProviderState { id: Claude, windows, fetched_at: ctx.now, source: Cow::Borrowed("claude-jsonl") }`. Empty cluster (no JSONL files OR no assistant messages) → return `ProviderError::Unavailable { reason: "no claude sessions found in ~/.claude/projects — run Claude Code at least once".into() }` so error row has next-step hint.
    - `provider::claude::jsonl`: declares `JsonlEntry` envelope enum `#[serde(tag = "type")] { Assistant(AssistantEntry), #[serde(other)] Other }` (handles `user`, `file-history-snapshot`, future variants), `AssistantEntry { timestamp: jiff::Timestamp, message: ClaudeMessage }`, `ClaudeMessage { #[serde(default)] usage: Option<Usage> }`, `Usage { #[serde(default)] input_tokens: u64, #[serde(default)] output_tokens: u64, #[serde(default)] cache_creation_input_tokens: u64, #[serde(default)] cache_read_input_tokens: u64 }` (RESEARCH Example 2 — deserializes all 4 fields as `u64` with `#[serde(default)]`. **Important**: Plan 02's drift detector does NOT widen these to `Option<u64>` — it uses a separate `serde_json::Value` re-parse path to distinguish missing-vs-zero, preserving Plan 01's `u64` schema. This avoids cross-plan ripple to Plan 01's inline tests.). `pub fn read_assistant_entries(path: &Path) -> Vec<AssistantEntry>`: BufReader.lines().peekable(); per-line `serde_json::from_str::<JsonlEntry>` — `Ok(Assistant(a))` push; `Ok(Other)` skip; `Err(e) if is_last` silent (truncated trailing line per D-35); `Err(e)` non-last → `tracing::warn!`. File open failure → `tracing::warn!` + return `Vec::new()`. NEVER `?` propagate at this layer (adapter wraps). Glob discovery helper: `pub fn discover_session_files(base: &Path) -> Vec<PathBuf>` using `glob::glob_with` with `MatchOptions { case_sensitive: true, require_literal_separator: false, require_literal_leading_dot: false }`; pattern is `{base}/**/*.jsonl` constructed via `base.join("**/*.jsonl")` then `to_string_lossy()`. Module doc note: "follows symlinks by default (L8); acceptable for `~/.claude/projects`."
    - `provider::claude::window`: `pub const CLAUDE_5H_TOKEN_LIMIT: u64 = 44_000;` with doc comment `/// Best-effort estimate (Pro tier ~44k tokens / 5h window). Anthropic does not publish exact numbers; revisit quarterly. Max5 / Max20 subscribers will see undercounted bars — Phase 2 may add a plan_tier knob (CFG-03). Source: tokenmix.ai 2026 + ccusage community measurements.` `pub const FIVE_HOURS: jiff::Span = jiff::Span::new().hours(5);` (or build at call site if const-context disallows). `pub struct Cluster { pub session_start: jiff::Timestamp, pub reset_at: jiff::Timestamp, pub used_tokens: u64 }`. `pub fn find_active_cluster(sorted_msgs: &[jsonl::AssistantEntry], _now: jiff::Timestamp) -> Option<Cluster>` per D-33 amended: walk newest→oldest, find first gap > 5h, slice from there to end (or from index 0 if no gap); `session_start = cluster.first()?.timestamp`; `reset_at = session_start.checked_add(FIVE_HOURS).ok()?` (use `target.since((jiff::Unit::Hour, *other)).unwrap_or_else(|_| Span::new())` pattern from render_text.rs for any other Span work — Phase 0-03 deviation 1). `used_tokens = cluster.iter().map(|m| m.message.usage.as_ref().map(|u| u.cache_creation_input_tokens).unwrap_or(0)).sum()` (D-33 AMENDED per L1 — ONLY cache_creation_input_tokens, NOT input_tokens + output_tokens). `pub fn percent_remaining(used: u64, limit: u64) -> f32` returns `0.0` for `limit==0`, else `((limit.saturating_sub(used)) as f32 / limit as f32 * 100.0).clamp(0.0, 100.0)` — scope the `as f32` cast in a `#[allow(clippy::cast_precision_loss)]` helper fn per render_text.rs::filled_cells pattern.
    - `provider::mod.rs`: add `pub mod claude;` after the existing `pub mod mock;` line.
    - `engine::mod.rs` extension: in `Engine::new`, after the Task 1a `cfg.providers.mock.enabled` branch, ALSO check `cfg.providers.claude.enabled` and push `Arc::new(ClaudeProvider::new(&dirs::home_dir().unwrap_or_default(), CLAUDE_5H_TOKEN_LIMIT))`. (Codex / gemini flags continue to emit `tracing::debug!` only.)
    - All new `*.rs` start with `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)] #![warn(clippy::pedantic)]`.
    - Tests (inline `#[cfg(test)] mod tests`): per file
      * `provider/claude/window.rs`: test cluster anchor with synthetic AssistantEntry vec — single cluster, gap > 5h splits, empty input returns None, percent_remaining edge cases (limit=0 → 0.0, used=limit → 0.0, used=0 → 100.0). Also assert `used_tokens == 41630` for a single-entry fixture with `cache_creation_input_tokens: 41630` (proves the typed `u64` summation path).
      * `provider/claude/jsonl.rs`: test `read_assistant_entries` against a `tempfile::NamedTempFile` containing the verbatim envelope from RESEARCH Example 3 (one Assistant entry with `cache_creation_input_tokens: 41630`, one Other `type:"user"`, plus a truncated trailing line `{"type":"assist` — assert exactly one AssistantEntry returned with `entry.message.usage.as_ref().unwrap().cache_creation_input_tokens == 41630` and `.timestamp` parses).
      * `provider/claude/mod.rs`: test `ClaudeProvider::new` + `fetch` against a `tempfile::tempdir()` fake-home containing one fixture JSONL — assert `Ok(state)` returned with `state.windows.len() == 1`, `state.id == ProviderId::Claude`, `state.source == "claude-jsonl"`. Also test the missing-directory path returns `Err(Unavailable { reason })` with the verbatim UI-SPEC literal.
  </behavior>
  <action>Implement Task 1b per the behavior block. (1) Create the 4 new files under `src/provider/claude/` + extend `src/provider/mod.rs` with `pub mod claude;`. (2) Use the EXACT type names, signatures, constant values, serde field names, and error literals from the behavior block (`CLAUDE_5H_TOKEN_LIMIT = 44_000`, sum `cache_creation_input_tokens` ONLY per D-33 amended + L1, error literal `~/.claude/projects not found — is Claude Code installed?` per UI-SPEC). (3) Keep `Usage` fields as `u64` with `#[serde(default)]` — Plan 02 uses a separate `serde_json::Value` re-parse for drift detection, not a schema change. (4) Copy patterns from `src/provider/mock.rs` (Provider impl skeleton + frozen-clock test) and `src/cli/render_text.rs::format_countdown` (jiff `since((Unit::Hour, *now))` + `Span::new()` fallback). (5) Extend `Engine::new` in `src/engine/mod.rs` to push `ClaudeProvider` when `cfg.providers.claude.enabled` is true. (6) Apply the lint-floor 2-line preamble at the top of every new `*.rs`. (7) Inline tests on each new file per behavior block; use `tempfile` for filesystem tests; `Secrets::default()` + frozen `jiff::Timestamp` for FetchCtx construction (mock.rs::tests pattern).</action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -20 &amp;&amp; cargo test --lib 2>&1 | tail -30 &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'cache_creation_input_tokens' src/provider/claude/window.rs` returns at least 1
    - Source: `grep -v '^[[:space:]]*//' src/provider/claude/window.rs src/provider/claude/jsonl.rs src/provider/claude/mod.rs | grep -E '\binput_tokens\s*\+\s*output_tokens\b'` returns nothing (D-33 amended — broken formula not used)
    - Source: `grep -v '^[[:space:]]*//' src/provider/claude/mod.rs src/provider/claude/jsonl.rs src/provider/claude/window.rs | grep -E 'Timestamp::now\b'` returns nothing (wall-clock injection rule)
    - Source: `grep -c 'CLAUDE_5H_TOKEN_LIMIT' src/provider/claude/window.rs` returns at least 1
    - Source: `grep -F 'pub const CLAUDE_5H_TOKEN_LIMIT: u64 = 44_000' src/provider/claude/window.rs` returns the const definition line
    - Source: `grep -F 'pub mod claude' src/provider/mod.rs` returns the line
    - Source: `grep -c '#\\!\\[deny(clippy::unwrap_used' src/provider/claude/mod.rs src/provider/claude/jsonl.rs src/provider/claude/window.rs` returns 3 (one per new file in Task 1b)
    - Source: `grep -c 'ClaudeProvider::new' src/engine/mod.rs` returns at least 1 (Engine wires Claude when cfg.providers.claude.enabled)
    - Test: `cargo test --lib provider::claude 2>&1 | grep -E 'test result: ok\\. [1-9]'` passes
    - Test: `cargo test --lib engine 2>&1 | grep -E 'FAILED' | wc -l` returns 0 (Task 1a engine tests still green after Engine::new extension)
    - CI: `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - CI: `cargo build --release` exits 0
  </acceptance_criteria>
  <done>Task 1b adds the real Claude adapter into the engine spine. ClaudeProvider implements Provider with file-glob → streaming JSONL parse → cluster anchor → percent calc, returning Ok(state) on healthy data and Err(Unavailable { reason }) with the UI-SPEC next-step-hint literal on broken paths. Engine::new now pushes ClaudeProvider when `cfg.providers.claude.enabled`. Usage schema stays `u64` (Plan 02's drift detector uses `serde_json::Value` re-parse, not a schema change). Clippy clean; no `Timestamp::now` under `src/provider/`; no `input_tokens + output_tokens` summation. Task 3 wires main.rs to dispatch this end-to-end against real data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire main.rs dispatch + render_text multi-row + per-row error rendering + integration tests for real Claude data path</name>
  <files>src/main.rs, src/cli/mod.rs, src/cli/render_text.rs, tests/cli_walking_skeleton.rs, tests/first_run_init.rs, tests/no_walltime_in_adapter.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/src/main.rs (Phase 0 layout — `install_phase0_panic_hook()` MUST remain the first line per D-27 contract; `_ = cli.color;` line 65 to remove; `jiff::Timestamp::now()` callsite at line 68 stays as the only wall-clock read)
    - /home/chasel/REPO/AIHPBar/src/cli/mod.rs and /home/chasel/REPO/AIHPBar/src/cli/render_text.rs (Phase 0 `compact_line` byte-exact format string — extend, don't rewrite; `debug_assert_eq!(state.windows.len(), 1)` must be relaxed)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-UI-SPEC.md (Layout & Interaction Contract — CLI compact-line section verbatim error-row format `{label}␣␣ERROR: {one-line reason}` with mandatory next-step hint; empty-state literal `no providers configured` + body `add at least one provider to ~/.config/ahb/config.toml — see README`; color thresholds Green≥30 / Yellow 10≤..<30 / Red <10)
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-RESEARCH.md sections "Pattern 4: TTY-aware color decision", "Pitfall L7: tracing-subscriber initialization races the panic hook", "Code Example 4"
    - /home/chasel/REPO/AIHPBar/.planning/phases/01-engine-claude-tui-scaffold/01-PATTERNS.md sections "main.rs (modified)" and "Integration tests"
    - The output of Task 1a + 1b: src/engine/mod.rs, src/cli/tty.rs, src/config.rs, src/provider/claude/mod.rs (for the constructors / function names this task wires together)
  </read_first>
  <behavior>
    - `main.rs` body order is contractually fixed: (1) `install_phase0_panic_hook()` first line (D-27 preserved verbatim); (2) `tracing_subscriber::fmt().with_env_filter(tracing_subscriber::EnvFilter::from_default_env()).init()` second (RESEARCH Pitfall L7 — panic hook uses `eprintln!` so no actual race, but ordering is canonical); (3) `Cli::parse()`; (4) `config_path = ahb::config::default_path()?` then `let cfg = match ahb::config::load_or_init(&config_path)? { LoadOutcome::Initialized(_) => { /* println already printed inside load_or_init */ std::process::exit(0) } LoadOutcome::Loaded(c) => c };` (D-37 first-run path); (5) `let secrets = Secrets::default()` (Phase 1 stub — Plan 02 replaces with `ahb::secrets::init()?`); (6) `let engine = ahb::engine::Engine::new(cfg, secrets);`; (7) dispatch on `cli.command`: `None` → `cli::run_compact(&engine, cli.ascii, cli.color).await`; `Some(Command::Tui)` → stub returning `Err(anyhow!("AHB tui will be wired in Plan 03"))` (Plan 03 replaces). The `#[tokio::main(flavor = "current_thread")]` attribute upgrades to `#[tokio::main]` (default multi-thread runtime) because Engine fan-out now wants `rt-multi-thread`.
    - Add `Command` enum to `cli::mod.rs`: `#[derive(clap::Subcommand)] pub enum Command { /// Launch the fixed-frame TUI. Tui }`. Extend `Cli` struct (move from main.rs to cli::mod.rs): `pub struct Cli { #[arg(long)] pub ascii: bool, #[arg(long, value_enum, default_value_t = ColorMode::Auto)] pub color: ColorMode, #[command(subcommand)] pub command: Option<Command> }` with the existing `#[command(version, about = "AHB — AI HP Bar — multi-CLI subscription session usage at a glance")]`. main.rs imports `ahb::cli::Cli` instead of redefining it.
    - `cli::run_compact(engine: &Engine, ascii: bool, color_flag: ColorMode) -> anyhow::Result<()>`: calls `now = jiff::Timestamp::now()` (the second and only other wall-clock callsite — only main.rs and this main-adjacent CLI dispatcher; acceptance grep tightens to `src/provider/` only), calls `engine.refresh_all(now).await`, then iterates `Vec<(ProviderId, Result<ProviderState, ProviderError>)>`. If the vec is empty → `println!("no providers configured")` and `println!("add at least one provider to ~/.config/ahb/config.toml — see README")` per UI-SPEC empty-state copy; exit 0. Else: for each `(id, result)` print one line — `Ok(state)` → `compact_line(&state, &now, ascii)` (existing Phase 0 fn, multi-window-relaxed per below); `Err(e)` → `format_error_row(id, &e, ascii)` (new helper in render_text.rs) emitting `{label_padded}  ERROR: {one-line reason}` per UI-SPEC, where `one-line reason` is `e.to_string()` (the Display impl already enforces a one-line form because `ProviderError` variants use `thiserror::Error #[error(...)]` strings without `\n`); UI-SPEC binding: the reason MUST end with a next-step hint — Plan 01 satisfies this by ensuring every `ProviderError::Unavailable { reason }` produced by ClaudeProvider has a hint baked in (already verified in Task 1b).
    - `compact_line`: relax the Phase 0 `debug_assert_eq!(state.windows.len(), 1)` to `debug_assert!(!state.windows.is_empty())` and render only `windows[0]` for now (Phase 2's `--detailed` will print all windows). The byte-exact Phase 0 format string `"{label}  {bar} {pct}% {sep} resets in {countdown}"` is unchanged.
    - **Promote `filled_cells` and `format_countdown` to `pub(crate) fn`** in `src/cli/render_text.rs` so Plan 03's TUI widget can re-import without duplication. This is a 2-line visibility bump on existing functions; no behavior change. (Resolves WARNING #3 — eliminates the duplication-vs-allow-drift fork.)
    - `format_error_row(id: ProviderId, err: &ProviderError, ascii: bool) -> String`: format `"{label}  ERROR: {message}"` where the label is derived from `ProviderId` via a small helper `pub(crate) fn id_label(id: ProviderId) -> &'static str { match id { Claude => "claude", Codex => "codex", Gemini => "gemini", Mock => "mock" } }` (so Plan 02's SchemaDrift sentinel + future-provider drift use the same label source — resolves WARNING #5: no hard-coded `"claude"` in `format_error_row`'s SchemaDrift special-case). `message = err.to_string()`. The `ascii` parameter is currently unused but reserved (kept for symmetry with `compact_line` and so signature does not change when Plan 02 adds the schema-drift sentinel rendering).
    - Color application: when `should_colorize(color_flag, false)` returns true, wrap the bar fill segment in ANSI escape sequences via `owo-colors` (already a Phase 0 dep) — fg color derived from percent per UI-SPEC: `pct >= 30 -> Green`, `10 <= pct < 30 -> Yellow`, `pct < 10 -> Red`. Empty cells are `Color::DarkGray` (Secondary role). The bullet `•` and "resets in" prose stay default fg (Dominant role). Implementation hint: wrap the `bar` segment build with `if color_on { format!("\u{1b}[32m{}\u{1b}[0m\u{1b}[90m{}\u{1b}[0m", filled_str, empty_str) }` style escape sequences (or use `owo-colors::OwoColorize` trait — `filled_str.green().to_string()` etc.); pick whichever keeps lint floor clean. NEVER pull both `owo-colors` and `ratatui::Style` into the CLI path (UI-SPEC Color source note — double-escape hazard).
    - Three new integration test files under `tests/`:
      * `tests/cli_walking_skeleton.rs`: spins up a `tempfile::tempdir()` as fake HOME, writes `~/.claude/projects/proj-a/<uuid>.jsonl` with the verbatim RESEARCH Example 3 assistant envelope (timestamp set 1 hour ago so cluster anchor + reset countdown are deterministic), writes a fake `~/.config/ahb/config.toml` with `[providers.claude] enabled = true`, then runs `assert_cmd::Command::cargo_bin("ahb").env("HOME", tmp.path()).env("XDG_CONFIG_HOME", tmp.path().join(".config"))` and asserts: stdout matches regex `^claude\s+\S{10}\s+\d{1,3}%\s+\S\s+resets in \d+h\d{2}m$` (Unicode bar) OR `^claude\s+[#-]{10}\s+\d{1,3}%\s+\|\s+resets in \d+h\d{2}m$` (when `--ascii` passed). Also asserts piping (`AHB | cat` equivalent: `.assert().stdout(predicates::str::is_match(...))` already runs without a TTY) produces no ANSI escape bytes (assertion: stdout `predicates::str::contains("\x1b[").not()`).
      * `tests/first_run_init.rs`: empty tmp HOME, no config — run `AHB`, assert exit code 0, stdout contains `initialized ` and ` — enable providers and rerun`, and a file actually exists at `{tmp}/.config/ahb/config.toml` after the run. (D-37)
      * `tests/no_walltime_in_adapter.rs`: file-system grep test using `std::process::Command` to invoke `grep -RE 'Timestamp::now' src/provider/` (or pure Rust `walkdir` if `grep` portability is a concern; prefer the Rust path to keep the test cross-OS). Filter out comments by reading line-by-line and skipping lines whose `trim_start` begins with `//`. Assert zero non-comment hits across `src/provider/`. (Wall-clock-injection rule binding.)
    - `tests/` uses `[dev-dependencies]` `assert_cmd`, `predicates`, `tempfile`, `regex` (regex only by `tests/secret_leak.rs` in Plan 02; declare all four now since Cargo.toml already added them in Task 1a).
    - Final smoke (manual + automated): `cargo run --release` against the real `~/.claude/projects/` on this machine prints exactly one `claude` line (if config has `[providers.claude] enabled = true`) or the empty-state pair of lines (if no providers enabled).
  </behavior>
  <action>Wire the CLI dispatch glue per the behavior block. (1) Move `Cli` struct out of `src/main.rs` into `src/cli/mod.rs` (re-use the `cli::tty::ColorMode` already created in Task 1a — re-exported via `cli::ColorMode` for ergonomic import). (2) Add `Command` subcommand enum with one `Tui` variant. (3) Rewrite `main()` body in the exact order documented in the behavior block: panic hook → tracing init → Cli::parse → `default_path` → `load_or_init` (matching `LoadOutcome` variants) → `Secrets::default()` (Plan 02 will replace with `init()?`) → `Engine::new(cfg, secrets)` → dispatch. Upgrade `#[tokio::main(flavor = "current_thread")]` to plain `#[tokio::main]` (multi-thread, leveraging the `rt-multi-thread` feature added in Task 1a). (4) Implement `cli::run_compact` calling `engine.refresh_all(now).await`, iterating results, calling `compact_line` for Ok / new `format_error_row` for Err, OR printing the two UI-SPEC empty-state lines if the vec is empty. (5) Extend `cli::render_text.rs`: (a) relax the `windows.len() == 1` debug_assert to `!windows.is_empty()`; (b) **promote `filled_cells` and `format_countdown` to `pub(crate) fn`** (visibility bump only) so Plan 03 TUI can re-import; (c) add `pub(crate) fn id_label(id: ProviderId) -> &'static str` helper; (d) add `pub fn format_error_row(id: ProviderId, err: &ProviderError, ascii: bool) -> String` using `id_label(id)` (NOT hard-coded `"claude"`); (e) add color application around the bar segment (use `owo-colors` already in deps; map percent → Green/Yellow/Red per UI-SPEC thresholds; empty cells DarkGray; gate the entire color application on `should_colorize` decision passed in as a `color_on: bool` param). The Phase 0 byte-exact tests must still pass — they call `compact_line(..., ascii)` without color; introduce a new `compact_line_colored(state, now, ascii, color_on)` if needed and have `compact_line` delegate `compact_line_colored(..., false)` to preserve existing test signatures. (6) Create three integration test files under `tests/` per behavior block; use the fixture-construction pattern from PATTERNS.md "Integration tests" section. (7) Delete the `_ = cli.color;` placeholder line — Task 3 actually wires color.</action>
  <verify>
    <automated>cargo test --tests 2>&1 | tail -30 &amp;&amp; cargo build --release 2>&1 | tail -10 &amp;&amp; ls -la "$HOME/.claude/projects" 2>/dev/null &amp;&amp; (./target/release/ahb 2>&1 | head -3) || true</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `./target/release/ahb 2>&1` exits with code 0 or 2 (0 = healthy or first-run; 2 = future SEC-01 keyring-missing — irrelevant in Plan 01 since Plan 02 wires keyring; in Plan 01 the binary should exit 0)
    - Behavior: On this developer machine (where `~/.claude/projects/-home-chasel-REPO-AIHPBar/*.jsonl` already exists), after manually setting `[providers.claude] enabled = true` in `~/.config/ahb/config.toml`, `./target/release/ahb` stdout matches regex `^claude\s+\S{10}\s+\d{1,3}%\s+.\s+resets in \d+h\d{2}m$` (one row, real numbers)
    - Behavior: `./target/release/ahb | cat` produces stdout containing zero `\x1b[` byte sequences (CORE-05 + UI-SPEC color-off path 5)
    - Behavior: With an empty config (all `enabled = false`), `./target/release/ahb` prints exactly `no providers configured\nadd at least one provider to ~/.config/ahb/config.toml — see README\n` and exits 0
    - Source: `grep -c 'install_phase0_panic_hook' src/main.rs` returns at least 1; the call must be the first line of `main()`'s body (verify by `awk '/^async fn main/,0' src/main.rs | head -10 | grep -F 'install_phase0_panic_hook'`)
    - Source: `grep -F 'tokio::main(flavor' src/main.rs` returns nothing (upgraded away from current_thread)
    - Source: `grep -c '#\[tokio::main\]' src/main.rs` returns 1
    - Source: `grep -c 'Timestamp::now' src/main.rs src/cli/mod.rs` returns at least 1 (still allowed in CLI entry layer)
    - Source: `grep -v '^[[:space:]]*//' src/provider/**/*.rs | grep -E 'Timestamp::now\b'` returns nothing (rule preserved through Task 3)
    - Source: `grep -F 'no providers configured' src/cli/render_text.rs src/cli/mod.rs` returns at least 1 (UI-SPEC empty-state literal present)
    - Source: `grep -F 'enable providers and rerun' src/config.rs` returns the D-37 literal (preserved from Task 1a)
    - Source: `grep -F 'ERROR:' src/cli/render_text.rs` returns at least 1 (error row format)
    - Source: `grep -E 'pub\(crate\) fn (filled_cells|format_countdown)' src/cli/render_text.rs` returns 2 (visibility-promoted for Plan 03 TUI re-use — WARNING #3 resolution)
    - Source: `grep -E 'pub\(crate\) fn id_label' src/cli/render_text.rs` returns 1 (label helper for format_error_row + future SchemaDrift sentinel — WARNING #5 resolution)
    - Test: `cargo test --test cli_walking_skeleton 2>&1 | grep -E 'test result: ok'` passes
    - Test: `cargo test --test first_run_init 2>&1 | grep -E 'test result: ok'` passes
    - Test: `cargo test --test no_walltime_in_adapter 2>&1 | grep -E 'test result: ok'` passes
    - Test: `cargo test 2>&1 | grep -E 'test result: ok' | wc -l` returns at least 6 (lib tests from Task 1a + 1b + 3 integration tests + Phase 0 tests still passing)
    - CI: `cargo clippy --all-targets --all-features -- -D warnings` exits 0
    - CI: `cargo build --release` exits 0
  </acceptance_criteria>
  <done>`./target/release/ahb` is a runnable Walking Skeleton: on this machine with Claude Code installed and config opted-in, it prints one real `claude` HP-bar line; on first run it auto-creates config and exits cleanly; with no enabled providers it prints the empty-state pair; with `| cat` it emits zero ANSI bytes; with a broken claude config it prints an ERROR row ending in a next-step hint instead of crashing. `filled_cells` + `format_countdown` are `pub(crate)` so Plan 03 TUI can re-import them without duplication. `format_error_row` derives label via `id_label(id)` helper (no hard-coded strings). All integration tests pass; clippy clean; the wall-clock-injection rule holds across all of `src/provider/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → adapter | `~/.claude/projects/**/*.jsonl` is user-owned but written by an external process (Claude Code); content shape can change without notice (ADP-03 sentinel covers, Plan 02). |
| TOML config → engine | `~/.config/ahb/config.toml` is user-owned; user can put arbitrary keys / values (D-38 warn+ignore tolerates) |
| Glob pattern → filesystem | Symlinks within `~/.claude/projects/` (e.g., dotfile-sync setups) are followed by default (L8 documented in module doc) |
| stdout → downstream consumer | `AHB | cat` pipes to another process; ANSI escapes MUST be suppressed when stdout is not a TTY (CORE-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | JSONL parser (`provider/claude/jsonl.rs`) | mitigate | `serde_json::from_str` on each line; malformed lines `tracing::warn!` + skip; trailing-line truncation silent (D-35); NEVER `?` propagate at IO layer — adapter wraps so a corrupted file yields a single warn-line, not a crash |
| T-01-02 | Information disclosure | JSONL content rendering | accept | Renderer only paints provider labels (closed enum `snake_case`) + integers + countdown text — JSONL `message.content` is NEVER rendered; ANSI escape injection via embedded prompt cannot reach the renderer because we never read content fields. Limited to Phase 1 scope (Plan 02 schema-drift sentinel preserves this property) |
| T-01-03 | DoS | Engine fan-out | mitigate | `tokio::time::timeout(2s, p.fetch(ctx))` on every adapter (D-29); `JoinSet::abort_all()` available if needed for cancellation; in Phase 1 the single Claude adapter cannot stall the engine for more than 2s |
| T-01-04 | DoS | Glob expansion | mitigate | Symlink behavior documented in module doc (L8); cycle protection is `glob`'s responsibility; user-owned `~/.claude/projects/` is not an attack surface for a tool the user runs themselves |
| T-01-05 | Tampering | TOML config parsing | mitigate | `toml::from_str` (mature parser); unknown-key warn+ignore (D-38) — forward-compat without strict-mode crash surface; no `include` or external-reference features used |
| T-01-06 | Information disclosure | stdout when piped | mitigate | `should_colorize` returns false when `!IsTerminal::is_terminal(&io::stdout())`; integration test `cli_walking_skeleton` asserts zero `\x1b[` bytes in piped stdout (CORE-05) |
| T-01-07 | Tampering | Path traversal in config | accept | Config has no user-supplied paths in Phase 1; provider table headers are hardcoded (`claude`/`codex`/`gemini`/`mock`); ClaudeProvider's `base_path` is built from `dirs::home_dir()` not from config; Plan 02/Phase 3 may introduce auth_source paths and that's their scope |
| T-01-08 | Repudiation | tracing log output | accept | Logs go to stderr (or `RUST_LOG` consumer); they are best-effort observability, not an audit trail. Per UI-SPEC Color, tracing output is not in the binding rendering surface |
</threat_model>

<verification>

After all three tasks complete, perform whole-plan acceptance:

```bash
# (1) Static gates (re-runnable)
grep -v '^[[:space:]]*//' src/provider/claude/window.rs src/provider/claude/jsonl.rs src/provider/claude/mod.rs | grep -E '\binput_tokens\s*\+\s*output_tokens\b'   # must be empty
grep -v '^[[:space:]]*//' src/provider/**/*.rs | grep -E 'Timestamp::now\b'                                                                                   # must be empty
grep -c 'crossterm' Cargo.toml                                                                                                                                # must be 0
grep -F 'pub const CLAUDE_5H_TOKEN_LIMIT: u64 = 44_000' src/provider/claude/window.rs                                                                          # must hit
grep -F 'ProjectDirs::from("", "", "ahb")' src/config.rs                                                                                                      # must hit
grep -F 'enable providers and rerun' src/config.rs                                                                                                            # must hit
grep -F 'no providers configured' src/cli/render_text.rs src/cli/mod.rs                                                                                       # must hit at least once
grep -F 'install_phase0_panic_hook' src/main.rs                                                                                                               # must hit (line 1 of main body)
grep -E 'pub\(crate\) fn (filled_cells|format_countdown)' src/cli/render_text.rs                                                                              # must return 2 (WARNING #3)
grep -E 'pub\(crate\) fn id_label' src/cli/render_text.rs                                                                                                     # must return 1 (WARNING #5)

# (2) Build + lint + tests
cargo build --release
cargo clippy --all-targets --all-features -- -D warnings
cargo test
cargo test --tests

# (3) Runtime smoke (on this dev machine where ~/.claude/projects already exists)
./target/release/ahb            # should print one claude row OR the empty-state pair
./target/release/ahb | cat      # output MUST contain zero \x1b[ sequences
./target/release/ahb --ascii    # bar uses #/- and pipe |
NO_COLOR=1 ./target/release/ahb # zero \x1b[ sequences
```

</verification>

<success_criteria>

Plan 01 success when:
- [ ] Task 1a delivers engine spine + config + cli/tty scaffolding, all tests passing against MockProvider
- [ ] Task 1b adds ClaudeProvider into the engine spine, all Claude-specific tests passing
- [ ] Task 3 wires main.rs dispatch + integration tests against real Claude data
- [ ] Walking Skeleton runs end-to-end against real `~/.claude/projects/` data with `[providers.claude] enabled = true`
- [ ] First-run on clean machine auto-creates config, prints D-37 literal, exits 0
- [ ] All-disabled config prints empty-state pair, exits 0
- [ ] Broken claude path config prints ERROR row with next-step hint, does NOT crash
- [ ] `AHB | cat` produces zero ANSI escape bytes; `NO_COLOR=1 AHB` ditto; `AHB --color=never` ditto
- [ ] `AHB --ascii` uses `#/-/|` glyphs (byte-exact via existing Phase 0 tests)
- [ ] Engine `refresh_all` uses `JoinSet` + per-adapter timeout + panic recovery via `task::Id` map (Pitfall L4)
- [ ] No `jiff::Timestamp::now()` anywhere under `src/provider/`
- [ ] No `input_tokens + output_tokens` summation anywhere (L1 binding)
- [ ] `CLAUDE_5H_TOKEN_LIMIT = 44_000` const exists with the "revisit quarterly" doc comment (D-44)
- [ ] `filled_cells` + `format_countdown` are `pub(crate) fn` (WARNING #3 — Plan 03 TUI re-uses without duplication)
- [ ] `format_error_row` derives label via `id_label(id)` helper (WARNING #5 — no hard-coded `"claude"`)
- [ ] All Plan 01 inline tests + 3 integration tests pass (`cargo test` green)
- [ ] `cargo clippy --all-targets --all-features -- -D warnings` exits 0
- [ ] Cargo.toml has 8 new prod deps + 4 new dev deps with per-line phase comments; tokio features include all six (`rt-multi-thread, macros, fs, time, signal, sync`)
- [ ] `crossterm` does NOT appear in `Cargo.toml` (PATTERNS Shared `Disallowed-types` enforcement)

</success_criteria>

<output>
Create `.planning/phases/01-engine-claude-tui-scaffold/01-01-SUMMARY.md` when done following the standard summary template (tags, requires, provides, affects, tech-stack, patterns established, key-files, key-decisions).
</output>
</content>
</invoke>