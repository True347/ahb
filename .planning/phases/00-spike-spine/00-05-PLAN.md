---
phase: 00-spike-spine
plan: 05
type: execute
wave: 4
depends_on: ["00-03", "00-04"]
files_modified:
  - .planning/research/GEMINI_SPIKE.md
autonomous: false
requirements: [ADP-00]
tags: [smoke-test, charset, verification, phase-gate]

must_haves:
  truths:
    - "All 5 Phase 0 success criteria from ROADMAP § Phase 0 are verifiably met (memo, binary, tests, charset, CI)"
    - "Charset verification per D-26 is recorded in GEMINI_SPIKE.md § 7 with actual xxd output from THIS machine's binary"
    - "The Phase 0 binary's stdout bytes match the U+2588 / U+2591 / U+2022 expectations (proven via xxd, not eyeballed)"
    - "Visual eyeball check is performed in at least one terminal beyond the default (e.g., tmux if installable, OR documented best-effort carve-out)"
    - "CI matrix run on the final tree is green across ubuntu-latest, macos-latest, windows-latest (the Windows run becomes the Windows Terminal proxy proof per Pitfall 4)"
  artifacts:
    - path: ".planning/research/GEMINI_SPIKE.md"
      provides: "Updated § 7 Charset verification with actual byte+visual evidence from this run"
      contains: "xxd"
  key_links:
    - from: "Phase 0 binary (target/release/ahb)"
      to: ".planning/research/GEMINI_SPIKE.md § 7"
      via: "captured xxd output"
      pattern: "e2 96 88"
    - from: "CI run on windows-latest"
      to: "Windows Terminal charset proxy proof"
      via: "CI green status"
      pattern: "windows-latest"
---

<objective>
Phase 0 end-to-end smoke + charset verification. Confirm all 5 Phase 0 success criteria from ROADMAP are met, capture the actual byte-level + visual-eyeball charset proof in `GEMINI_SPIKE.md` § 7 (the placeholder section Plan 04 created), and verify the CI workflow turns green on the final tree.

**Purpose:** Phase 0 gate. Without this plan, "Phase 0 complete" is only structurally true — this plan binds the structural completion to actual empirical evidence that the spine works in a real terminal, on real CI, on three OSes.

**Output:** Updated `GEMINI_SPIKE.md` § 7 with real captured evidence. Phase 0 checklist confirmed.

**This is NOT a code plan.** It's a verification + documentation pass.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/PROJECT.md
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md
@/home/chasel/REPO/AIHPBar/.planning/research/GEMINI_SPIKE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full local verification suite + capture xxd byte proof</name>
  <files>/tmp/ahb-out.txt, /tmp/ahb-ascii.txt (temp verification artifacts, not committed)</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/ROADMAP.md § Phase 0 Success Criteria (5 items, lines 28-33)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Pitfall 4: Verifying charset rendering" (lines 400-413) — byte-level methodology
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-15, D-16, D-18, D-25, D-26 — charset + bar + verification expectations
  </read_first>
  <action>
    Run the following commands on the developer's machine and capture output. Bash tool working directory is NOT guaranteed across calls in this environment, so EVERY invocation prefixes `cd /home/chasel/REPO/AIHPBar &&` and EVERY binary path is absolute (`/home/chasel/REPO/AIHPBar/target/release/ahb`) — B-3 binding fix. The executor (Claude) can run all of these autonomously — they're shell commands against the project root, no terminal-emulator-specific behavior required:

    1. **Build verification:**
       ```
       cd /home/chasel/REPO/AIHPBar && cargo build --release 2>&1 | tail -5
       ```
       Expect: builds clean, exits 0.

    2. **Test verification:**
       ```
       cd /home/chasel/REPO/AIHPBar && cargo test --all-targets 2>&1 | tail -20
       ```
       Expect: All tests green. At least 8 tests total (4 from model.rs + 2 from provider/mod.rs + 4 from mock.rs + 4 from render_text.rs ≈ 14). Capture the `test result: ok. N passed` line.

    3. **Clippy verification:**
       ```
       cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5
       ```
       Expect: zero warnings, exit 0.

    4. **Binary smoke (Unicode default):**
       ```
       /home/chasel/REPO/AIHPBar/target/release/ahb
       ```
       Expect a line matching `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` (regex tolerates wall-clock drift — see Plan 03 W-3). Exit 0.

    5. **Binary smoke (ASCII fallback):**
       ```
       /home/chasel/REPO/AIHPBar/target/release/ahb --ascii
       ```
       Expect a line matching `^mock-session  ######---- 60% \| resets in [0-9]+h[0-9]{2}m$`.

    6. **Help + version smoke:**
       ```
       /home/chasel/REPO/AIHPBar/target/release/ahb --help
       /home/chasel/REPO/AIHPBar/target/release/ahb --version
       ```
       Expect: clap-formatted help including `--ascii` + `--color` flags + the D-01 description. Version line `ahb 0.0.1`.

    7. **Byte-level charset proof (the critical one for D-26 + Pitfall 4):**
       ```
       /home/chasel/REPO/AIHPBar/target/release/ahb > /tmp/ahb-out.txt
       xxd /tmp/ahb-out.txt | head -3
       ```
       Capture verbatim xxd output. The first ~16 bytes after the label "mock-session  " should be `e2 96 88` repeated 6 times then `e2 96 91` repeated 4 times. Around byte offset ~50 you should see `e2 80 a2` (U+2022 middle dot).

       Also run the ASCII byte check:
       ```
       /home/chasel/REPO/AIHPBar/target/release/ahb --ascii > /tmp/ahb-ascii.txt
       xxd /tmp/ahb-ascii.txt | head -3
       ```
       The bar portion should be all ASCII bytes (`23` = `#` ×6, `2d` = `-` ×4) and the separator should be `7c` (`|` pipe).

       SAVE THIS XXD OUTPUT — it gets pasted into Plan 04's memo in Task 2 below.

    8. **Cargo.lock sanity:**
       ```
       cd /home/chasel/REPO/AIHPBar && grep -E '^name = "(jiff|tokio|owo-colors|ratatui)"' Cargo.lock | sort -u
       ```
       Expect: jiff appears (any 0.2.x), tokio appears (any 1.52.x), owo-colors appears (any 4.x). `ratatui` should NOT appear (Phase 0 doesn't pull it per RESEARCH § Phase 0 dep minimalism). If ratatui appears, something accidentally pulled it — investigate before declaring Phase 0 done.

    9. **Best-effort tmux check (if installable):**
       ```
       which tmux
       ```
       - If found: `tmux new-session -d 'cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb; sleep 3' && sleep 1 && tmux capture-pane -p -t 0 ; tmux kill-server` — capture what tmux rendered.
       - If not found: skip; record "tmux not installed on dev box; visual eyeball deferred per Pitfall 4 best-effort carve-out". Per RESEARCH line 410 the developer can `pacman -S tmux` (Arch) but is not required to install just for the spike — CI Windows runner is the binding charset proof for the cross-platform claim.

    Aggregate ALL captured output into a temp scratch file or paste-buffer. Task 2 below pastes the xxd output into the memo.
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo build --release 2>&1 | grep -E "(error|Compiling ahb v)" | grep -E -v "warning:" && cd /home/chasel/REPO/AIHPBar && cargo test --all-targets 2>&1 | grep -E "test result: ok" && cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings 2>&1 | grep -E -v "warning" | tail -3 && /home/chasel/REPO/AIHPBar/target/release/ahb | xxd | grep -E "e2 96 88" && /home/chasel/REPO/AIHPBar/target/release/ahb | xxd | grep -E "e2 96 91" && /home/chasel/REPO/AIHPBar/target/release/ahb | xxd | grep -E "e2 80 a2" && /home/chasel/REPO/AIHPBar/target/release/ahb --ascii | grep -qP '^mock-session  ######---- 60% \| resets in [0-9]+h[0-9]{2}m$'</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/chasel/REPO/AIHPBar && cargo build --release` exits 0.
    - `cd /home/chasel/REPO/AIHPBar && cargo test --all-targets` shows at least one `test result: ok. N passed` line with N ≥ 8.
    - `cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings` exits 0.
    - `/home/chasel/REPO/AIHPBar/target/release/ahb` output matches the regex `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` (countdown is dynamic since it computes from wall-clock; expect close to `2h00m` but a few seconds drift is acceptable for the regex; the byte verification covers the structure).
    - `/home/chasel/REPO/AIHPBar/target/release/ahb --ascii` outputs a line matching `^mock-session  ######---- 60% \| resets in [0-9]+h[0-9]{2}m$`.
    - xxd of stdout shows bytes `e2 96 88` (U+2588) appearing 6 times, `e2 96 91` (U+2591) appearing 4 times, `e2 80 a2` (U+2022) appearing once.
    - ratatui NOT in Cargo.lock (Phase 0 dep minimalism check).
    - Tmux check performed (executed if installed, documented as deferred if not).
    - All binary invocations use absolute path `/home/chasel/REPO/AIHPBar/target/release/ahb` and all cargo invocations are prefixed with `cd /home/chasel/REPO/AIHPBar &&` (B-3).
  </acceptance_criteria>
  <done>
    Local verification complete. xxd output captured. All 5 Phase 0 ROADMAP success criteria are empirically met on this machine. Output stored for Task 2 to embed in memo. B-3 absolute-path discipline enforced — no reliance on Bash tool cwd persistence.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human eyeballs the binary output in their terminal — visual sanity check</name>
  <what-built>
    Task 1 ran the binary + captured xxd bytes — that proves the BYTES are right.

    Task 2 asks the human (Chasel) to actually LOOK at the output in their terminal and confirm it renders correctly. Per D-26 + Pitfall 4 definition of "renders correctly":
    (a) U+2588 displays as a single column-wide solid block ←
    (b) U+2591 displays as a single column-wide light shade ←
    (c) U+2022 doesn't consume two columns ←
    (d) no U+FFFD replacement character ←
    (e) no "tofu" rectangle ←

    Byte verification alone can't catch (a)-(e) — a terminal could have correct bytes but render them as monospace-double-width or with wrong fonts.
  </what-built>
  <how-to-verify>
    The HUMAN must:

    1. Open a fresh terminal window in the developer's preferred emulator (alacritty / kitty / gnome-terminal / iterm2 / xterm — whichever is normal).
    2. Run: `/home/chasel/REPO/AIHPBar/target/release/ahb`
    3. Visually confirm:
       - The bar is 10 cells wide total (count: 6 solid blocks + 4 light shades = 10)
       - Each block is the same width as each shade (no double-width glyphs)
       - The `•` middle dot is one column wide (not two)
       - No replacement char (`�`), no empty rectangle ("tofu"), no `?` or wrong glyph
       - Overall the line looks like the locked literal: `mock-session  ██████░░░░ 60% • resets in 2h00m` — visually (countdown may drift to 1h59m or similar — that's fine; bar + separator are what matter)
    4. If tmux is installed, ALSO run inside tmux: `tmux new-window '/home/chasel/REPO/AIHPBar/target/release/ahb; read'` and confirm same.
    5. (Best-effort, defer if no Windows machine): on a Windows box with Windows Terminal, run the binary and visually confirm. Skip if no Windows box at hand — the CI matrix run on `windows-latest` will catch byte-level rendering correctness when Phase 0 is pushed.

    If anything looks wrong:
    - **(a)/(b) wrong width:** likely a font issue — note the emulator + font name. Suggested fix: switch to a font with full Unicode coverage (e.g., DejaVu Sans Mono, Noto Sans Mono, JetBrains Mono).
    - **(c) double-width middle dot:** known issue in some terminal+font combos. Workaround: use `--ascii` mode (D-18). Document in memo.
    - **(d) replacement char:** font lacks the codepoint entirely — `--ascii` is the fallback.
    - **(e) tofu:** same as (d).

    Record findings as a list:
    - "Native terminal (<emulator name>): rendered correctly | ISSUE: <describe>"
    - "tmux: rendered correctly | not installed; skipped"
    - "Windows Terminal: best-effort — verified manually | deferred to CI windows-latest proof"
  </how-to-verify>
  <resume-signal>
    Reply with:
    - "verified" + the three-line findings list (native / tmux / windows-terminal) — Task 3 will paste these into the memo.
    - OR "needs-fix" + description of what rendered wrong + which terminal — at minimum the issue should be recorded; doesn't necessarily block Phase 0 (`--ascii` exists as the explicit fallback per D-18).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update .planning/research/GEMINI_SPIKE.md § 7 with actual captured evidence</name>
  <files>.planning/research/GEMINI_SPIKE.md</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/research/GEMINI_SPIKE.md (current state from Plan 04 — has § 7 Charset verification placeholder with TBD entries)
    - Task 1 captured xxd output (from prior tool output in this plan run)
    - Task 2 captured visual findings (from human resume signal)
  </read_first>
  <action>
    Edit `.planning/research/GEMINI_SPIKE.md` and REPLACE the `## Charset verification (per D-26)` section's placeholder content with the actual captured evidence from Tasks 1 + 2.

    Before edit, the section (from Plan 04) has placeholder text like:
    ```
    Performed on dev box terminal: <alacritty / kitty / ...>
    Byte-level (always doable per Pitfall 4):
        $ ./target/release/ahb > /tmp/ahb-out.txt
        $ xxd /tmp/ahb-out.txt | head -3
        <paste actual xxd output here>
    Expected bytes present: ... <YES | NO>
    ```

    After edit, the section should have ACTUAL captured values:

    ```markdown
    ## Charset verification (per D-26)

    Performed on dev box terminal: <ACTUAL terminal name from Task 2 resume signal>
    Date: 2026-05-22 (or actual date if different)

    ### Byte-level proof (binding control per Pitfall 4)

    Unicode default mode:
    ```
    $ /home/chasel/REPO/AIHPBar/target/release/ahb > /tmp/ahb-out.txt
    $ xxd /tmp/ahb-out.txt | head -3
    <PASTE actual xxd output from Task 1 here — typically 3 lines of hex dump>
    ```

    Bytes present: U+2588 (`e2 96 88`) ×6 ✓, U+2591 (`e2 96 91`) ×4 ✓, U+2022 (`e2 80 a2`) ×1 ✓.

    ASCII fallback mode:
    ```
    $ /home/chasel/REPO/AIHPBar/target/release/ahb --ascii > /tmp/ahb-ascii.txt
    $ xxd /tmp/ahb-ascii.txt | head -3
    <PASTE actual ASCII-mode xxd output from Task 1>
    ```

    Bytes confirmed: `23` (`#`) ×6, `2d` (`-`) ×4, `7c` (`|`) ×1 — all pure ASCII.

    ### Visual eyeball (per Pitfall 4 definition of "renders correctly")

    | Environment | Result | Notes |
    |-------------|--------|-------|
    | Native: <emulator name from Task 2> | ✓ renders correctly / ✗ <issue> | From Task 2 finding |
    | tmux | ✓ verified / ⊘ not installed; skipped per Pitfall 4 best-effort | From Task 2 finding |
    | screen | ⊘ not installed; skipped — Pitfall 4 carve-out | n/a |
    | Windows Terminal | ⊘ best-effort — deferred; CI matrix run on `windows-latest` (Plan 01 § ci.yml) provides byte-level proof | CI run on the final tree is the binding cross-platform check |

    Definition met (Pitfall 4 lines 408-413):
    (a) U+2588 single column-wide solid block — <YES/NO>
    (b) U+2591 single column-wide light shade — <YES/NO>
    (c) U+2022 does not consume two columns — <YES/NO>
    (d) no U+FFFD replacement character — <YES/NO>
    (e) no "tofu" rectangle — <YES/NO>

    Overall verdict: <"charset verified" | "issue documented; --ascii fallback exists per D-18">
    ```

    Replace placeholder fields with actual data:
    - Terminal name: from Task 2 resume signal (e.g., "alacritty")
    - xxd output: verbatim from Task 1 capture
    - YES/NO answers: from Task 2 visual eyeball
    - tmux row: based on whether Task 1 step 9 found `tmux` installed and what `tmux capture-pane` showed

    If Task 2 reported any rendering issue (e.g., "needs-fix: middle dot double-width in iterm2 with Menlo font"), record it in the table's Notes column and note in the overall verdict that `--ascii` is the documented fallback (per D-18 — this is exactly the case ASCII mode exists for; not a Phase 0 blocker).

    Per CONTEXT D-26, the option to split this section to a separate `CHARSET_NOTE.md` is at Claude's discretion. Since the section is small (~30-40 lines), keep it co-located in GEMINI_SPIKE.md.
  </action>
  <verify>
    <automated>grep -A 30 '^## Charset verification' .planning/research/GEMINI_SPIKE.md | grep -E -c '(e2 96 88|e2 96 91|e2 80 a2)' | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/research/GEMINI_SPIKE.md` § 7 Charset verification contains literal xxd hex bytes `e2 96 88`, `e2 96 91`, `e2 80 a2` (the proof bytes pasted from Task 1's capture).
    - The section no longer contains TBD placeholders like `<paste actual xxd output here>` or `<emulator name>` — all replaced with actual captured values.
    - Visual eyeball table has 4 rows (native, tmux, screen, Windows Terminal) each with a verdict.
    - Overall verdict line is non-empty.
    - `git diff .planning/research/GEMINI_SPIKE.md` shows additions to § 7 (the only change).
  </acceptance_criteria>
  <done>
    `GEMINI_SPIKE.md` § 7 now has real captured byte+visual evidence (not placeholders). D-26 is fully discharged. Phase 0 success criterion #4 (ROADMAP "Charset decision is recorded ... and verified to render correctly") is now met with audit trail.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Confirm Phase 0 success-criteria checklist + write phase summary</name>
  <files>(verification-only — no file modifications in this task)</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/ROADMAP.md § Phase 0 (5 success criteria, lines 28-33)
    - /home/chasel/REPO/AIHPBar/.planning/research/GEMINI_SPIKE.md (final state after Task 3)
    - All 4 prior plan SUMMARYs if they exist (00-01-SUMMARY.md through 00-04-SUMMARY.md)
  </read_first>
  <action>
    Walk through the 5 Phase 0 success criteria from ROADMAP.md and confirm each is met. This is a check-and-document pass — no new artifacts created beyond confirmation.

    **Phase 0 success criterion #1:** `research/GEMINI_SPIKE.md` memo exists with explicit go/no-go decision and kill criteria.
       - Verify: `test -f .planning/research/GEMINI_SPIKE.md && grep -E '^decision: (go|no-go)' .planning/research/GEMINI_SPIKE.md && grep -q '^## Kill criteria' .planning/research/GEMINI_SPIKE.md`
       - Plan 04 + Plan 05 Task 3 produced this.

    **Phase 0 success criterion #2:** `cargo build --release` produces a single static binary; running `AHB` prints one mocked HP bar line via the locked Provider trait + ProviderState/ResetInfo/HpUnit/ProviderError types.
       - Verify: `cd /home/chasel/REPO/AIHPBar && cargo build --release && test -x /home/chasel/REPO/AIHPBar/target/release/ahb && /home/chasel/REPO/AIHPBar/target/release/ahb | head -1` matches the regex `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$`.
       - Plan 03 produced this; Plan 05 Task 1 re-verified.
       - "Single static binary" + "no OpenSSL/native-tls" claim: Phase 0 has no HTTP deps so this is trivially true. Phase 4 will tighten with `--release` strip+lto.

    **Phase 0 success criterion #3:** `cargo test` runs green against MockProvider-based unit tests; confirms model.rs is `Serialize` + `Deserialize` + `Send + Sync` + dyn-safe.
       - Verify: `cd /home/chasel/REPO/AIHPBar && cargo test --all-targets 2>&1 | grep -E 'test result: ok'`. All test result lines say `ok`.
       - Compile-time `assert_impl_all!` invocations prove Send + Sync + dyn-safe (Plan 02 + Plan 03).
       - serde round-trip proven by `provider_state_serde_roundtrip` (Plan 02 — including Cow source field per W-2) and `provider_state_serde_roundtrip_full` (Plan 03).

    **Phase 0 success criterion #4:** Charset decision is recorded (ASCII default vs emoji opt-in) and verified to render correctly in tmux, screen, and Windows Terminal.
       - Charset decision: documented in CONTEXT.md D-15..D-19 + GEMINI_SPIKE.md § 7. Default = Unicode block (D-15), `--ascii` opt-in fallback (D-18), no emoji in v1 (D-19).
       - Rendering verification: Plan 05 Tasks 1+2+3 captured byte-level proof + visual eyeball. tmux/Windows-Terminal best-effort per Pitfall 4; CI matrix run on `windows-latest` is the binding Windows proof.

    **Phase 0 success criterion #5:** Repo scaffolded with MSRV pin (≥1.88), clippy config (`unwrap_used = deny` for adapter/render paths), and a CI skeleton that runs `cargo build` + `cargo test` + `cargo clippy`.
       - MSRV pin: `cd /home/chasel/REPO/AIHPBar && grep '^rust-version = "1.88"$' Cargo.toml` succeeds (Plan 01).
       - Clippy config: `cd /home/chasel/REPO/AIHPBar && grep '^allow-unwrap-in-tests = true' clippy.toml` succeeds + `cd /home/chasel/REPO/AIHPBar && grep '#!\[deny(clippy::unwrap_used' src/main.rs` succeeds (Plan 01 + Plan 03).
       - CI: `.github/workflows/ci.yml` exists with 3-OS matrix running build + test + clippy (Plan 01).
       - CI green status: this CANNOT be verified locally — requires pushing to GitHub. Note that the LOCAL equivalent (`cargo build && cargo test && cargo clippy --all-targets -- -D warnings`) is green on the dev box per Plan 05 Task 1. When Phase 0 is committed + pushed, the GH Actions run is the final proof. If the developer hasn't pushed yet at the moment Plan 05 runs, mark this as "local checks green; awaiting first push for CI proof".

    Produce a Phase 0 verification report (in agent response, not committed) summarizing each of the 5 criteria with PASS/PASS-AWAITING-PUSH/FAIL status and brief evidence.

    **No commit in this task** — Plan 05's only file change was Task 3's update to GEMINI_SPIKE.md, which will be committed by the orchestrator at phase close. Plan 05 produces a SUMMARY.md (via the output line below) that documents the verification.
  </action>
  <verify>
    <automated>test -f .planning/research/GEMINI_SPIKE.md && grep -E '^decision: (go|no-go)' .planning/research/GEMINI_SPIKE.md && cd /home/chasel/REPO/AIHPBar && cargo build --release 2>&1 | tail -3 && cd /home/chasel/REPO/AIHPBar && cargo test --all-targets 2>&1 | grep -E 'test result: ok' && cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings 2>&1 | tail -3 && cd /home/chasel/REPO/AIHPBar && grep -q '^rust-version = "1.88"$' Cargo.toml && cd /home/chasel/REPO/AIHPBar && grep -q '^allow-unwrap-in-tests = true' clippy.toml && test -f /home/chasel/REPO/AIHPBar/.github/workflows/ci.yml && grep -q 'windows-latest' /home/chasel/REPO/AIHPBar/.github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - All 5 ROADMAP § Phase 0 success criteria have PASS or PASS-AWAITING-PUSH status with documented evidence.
    - No FAIL status for any of the 5 criteria.
    - Plan 05's only file modification is Task 3's update to GEMINI_SPIKE.md § 7 — no other files changed by this plan (Tasks 1, 2, 4 are verification-only).
  </acceptance_criteria>
  <done>
    Phase 0 verification report produced. All 5 ROADMAP success criteria confirmed met. Phase 0 ready for git commit (orchestrator handles) and for Phase 1 planning to begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 03 binary output → Plan 05 captured evidence | Captured xxd output is committed to memo; same anonymization concerns as Plan 04 apply, but Phase 0 binary output is hardcoded fixture data (no PII). |
| Visual eyeball human report → committed memo content | Human-reported terminal name + finding is recorded; trust assumes Chasel is reporting truthfully (no adversary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-19 | Tampering | Charset verification might be claimed without actually being performed | mitigate | Task 1 captures xxd bytes mechanically (not subjective); Task 2 has a blocking human checkpoint that requires the developer to actually look at the terminal; Task 3 pastes the captured xxd into the memo as evidence trail. Without Task 1 output, Task 3 cannot fill the placeholder. |
| T-00-20 | Info disclosure | Captured xxd output could leak terminal-specific environment data | accept | Phase 0 binary output is hardcoded fixture: `mock-session  ██████░░░░ 60% • resets in <countdown>`. No PII, no system info. xxd of this output reveals only the fixed bytes — safe to commit. |
| T-00-21 | DoS | Test suite hanging would block Phase 0 close | mitigate | All tests are unit-level + inline + use injected clocks. tokio test timeout (default 60s) protects against runaway. Plan 05 Task 1 verify command pipes through tail/grep — won't block on test output volume. |
| T-00-22 | Repudiation | Phase 0 "complete" claim might be made without empirical proof | mitigate | Plan 05 acceptance criteria require concrete grep checks against built artifacts. SUMMARY.md output of Plan 05 documents which evidence backs each Phase 0 success criterion. |

Severity: All LOW. Plan 05 is verification, not creation; the only artifact change is § 7 of an existing memo, and that change is auditable via git diff.
</threat_model>

<verification>
After all four tasks complete:
1. `cd /home/chasel/REPO/AIHPBar && cargo build --release && cargo test --all-targets && cargo clippy --all-targets -- -D warnings` all exit 0 from project root.
2. `/home/chasel/REPO/AIHPBar/target/release/ahb` prints the Unicode line (regex match per W-3); `--ascii` flag prints ASCII line.
3. `xxd` confirms byte sequences `e2 96 88`, `e2 96 91`, `e2 80 a2` present.
4. `.planning/research/GEMINI_SPIKE.md` § 7 contains the actual captured xxd output (not placeholder text).
5. All 5 ROADMAP § Phase 0 success criteria are verifiably met (PASS or PASS-AWAITING-PUSH).
6. No `tests/` directory at project root (per RESEARCH Pitfall 3 — tests live inline in src/).
7. ratatui NOT in Cargo.lock (per Phase 0 dep minimalism).
</verification>

<success_criteria>
- All 5 Phase 0 ROADMAP success criteria are documented as met with concrete evidence.
- `GEMINI_SPIKE.md` § 7 contains real byte+visual proof (replacing placeholders from Plan 04).
- Phase 0 is ready to be marked complete; Phase 1 planning can begin.
- D-26 (charset verification) and the spirit of D-15..D-19 (charset design) are jointly discharged.
- B-3 absolute-path discipline enforced — all binary/cargo invocations stable across Bash tool cwd resets.
</success_criteria>

<output>
Create `.planning/phases/00-spike-spine/00-05-SUMMARY.md` when done.

The SUMMARY.md should include the Phase 0 verification report from Task 4 — i.e., the 5-criteria checklist with PASS/AWAITING-PUSH status + evidence per criterion. This is the artifact future agents will reference when answering "was Phase 0 actually complete or did it ship gaps?".
</output>
