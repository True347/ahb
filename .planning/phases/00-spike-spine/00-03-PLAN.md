---
phase: 00-spike-spine
plan: 03
type: execute
wave: 3
depends_on: ["00-02"]
files_modified:
  - src/provider/mock.rs
  - src/cli/mod.rs
  - src/cli/render_text.rs
  - src/main.rs
  - src/lib.rs
autonomous: true
requirements: [ADP-00]
tags: [rust, cli, mock, render, bar, skeleton]

must_haves:
  truths:
    - "Running `cargo run --release` from project root prints exactly one line that matches `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` (with two spaces after label, fixed 10-cell bar, U+2022 separator; countdown is regex-matched because wall-clock drift during a multi-minute build can shift `2h00m` → `1h59m` between build end and run)"
    - "The line is produced by invoking MockProvider via the locked `Provider` trait (NOT via a hardcoded `println!`) — per CONTEXT specifics third bullet"
    - "ASCII fallback works: `cargo run --release -- --ascii` prints a line matching `^mock-session  ######---- 60% \\| resets in [0-9]+h[0-9]{2}m$`"
    - "Phase 0 panic hook is installed before any provider code, composing with `take_hook()`/`set_hook()` so Phase 1's ratatui::init() will wrap it cleanly"
    - "Bar width is fixed at 10 cells per D-16 (snapshot stability + alignment)"
    - "Charset bytes confirmed: `cargo run --release | xxd | grep -E '2588|2591'` matches (U+2588 = e2 96 88, U+2591 = e2 96 91)"
  artifacts:
    - path: "src/provider/mock.rs"
      provides: "MockProvider implementing Provider trait + inline tests for shape + serde round-trip + dyn-safety"
      min_lines: 50
      contains: "impl Provider for MockProvider"
    - path: "src/cli/render_text.rs"
      provides: "compact_line(state, now, ascii) -> String + format_countdown helper"
      min_lines: 25
      contains: "pub fn compact_line"
    - path: "src/cli/mod.rs"
      provides: "Module declaration exporting render_text"
      contains: "pub mod render_text"
    - path: "src/main.rs"
      provides: "Phase 0 panic hook + tokio::main + clap parser + MockProvider invocation + render"
      min_lines: 30
      contains: "install_phase0_panic_hook"
  key_links:
    - from: "src/main.rs"
      to: "MockProvider::fetch"
      via: "&dyn Provider through the locked trait"
      pattern: "Provider"
    - from: "src/main.rs install_phase0_panic_hook"
      to: "std::panic::take_hook + std::panic::set_hook"
      via: "chained hook composition for Phase 1 ratatui wrap"
      pattern: "take_hook"
    - from: "src/cli/render_text.rs compact_line"
      to: "BAR_WIDTH const = 10"
      via: "fixed-width bar per D-16"
      pattern: "BAR_WIDTH"
---

<objective>
Wire the runtime spine: MockProvider implementing the locked trait, a text renderer producing the exact CONTEXT-D-25 HP bar line, and a `main.rs` that ties them together with the Phase 0 panic-hook contract. After this plan, `cargo run --release` prints a line matching `mock-session  ██████░░░░ 60% • resets in <Xh00m>` — the user-visible Phase 0 success criterion #2 from ROADMAP (with regex-style countdown match to tolerate wall-clock drift).

**Purpose:** Prove the spine works end-to-end through the locked trait. Critically — NO `println!` shortcut. The bar value must flow `MockProvider::fetch -> ProviderState -> render_text::compact_line -> println!` so every Phase 1+ adapter has the same path proven.

**Output:** A runnable binary that exits 0 and prints exactly one line of HP bar text through the Provider trait, plus `--ascii` flag support, plus Phase 0 panic hook in place for Phase 1 to compose with.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/PROJECT.md
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md
@/home/chasel/REPO/AIHPBar/.planning/research/ARCHITECTURE.md
@/home/chasel/REPO/AIHPBar/.planning/research/PITFALLS.md
@/home/chasel/REPO/AIHPBar/Cargo.toml
@/home/chasel/REPO/AIHPBar/src/model.rs
@/home/chasel/REPO/AIHPBar/src/provider/mod.rs
@/home/chasel/REPO/AIHPBar/src/secrets.rs

<interfaces>
<!-- Types/trait this plan consumes from Plan 02. -->

From src/model.rs:
- `pub enum ProviderId { Claude, Codex, Gemini, Mock }`
- `pub struct ProviderState { pub id: ProviderId, pub windows: Vec<HpWindow>, pub fetched_at: jiff::Timestamp, pub source: Cow<'static, str> }`  // W-2: Cow, not &'static str
- `pub struct HpWindow { pub label: Cow<'static, str>, pub percent_remaining: HpUnit, pub reset: ResetInfo, pub bar_color: Option<BarColor> }`
- `pub struct ResetInfo { pub resets_at: jiff::Timestamp }`
- `pub type HpUnit = f32;  // 0.0..=100.0`
- `pub enum ProviderError { Unconfigured, Unavailable, SchemaDrift, Network, RateLimited, Internal(anyhow::Error) }`

From src/provider/mod.rs:
- `pub struct FetchCtx<'a> { pub now: jiff::Timestamp, pub secrets: &'a Secrets }`
- `#[async_trait] pub trait Provider: Send + Sync + 'static { fn id(&self) -> ProviderId; async fn fetch(&self, ctx: &FetchCtx<'_>) -> Result<ProviderState, ProviderError>; }`

From src/secrets.rs:
- `pub struct Secrets;  // with Default`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write src/provider/mock.rs (MockProvider returning the D-25 fixture)</name>
  <files>src/provider/mock.rs, src/provider/mod.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Code Examples — MockProvider" (lines 482-551) — verbatim impl + tests
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-25 — MockProvider output shape (label = "mock-session", percent_remaining = 60.0, resets_at = now + 2h)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Anti-Patterns to Avoid" (lines 318-323) — must use ctx.now, not jiff::Timestamp::now() inside fetch
    - /home/chasel/REPO/AIHPBar/src/model.rs (Plan 02 types — note: ProviderState.source is now Cow<'static, str> per W-2)
    - /home/chasel/REPO/AIHPBar/src/provider/mod.rs (Plan 02 trait + FetchCtx)
  </read_first>
  <behavior>
    - Test 1: `MockProvider.fetch(&ctx).await` with `ctx.now = "2026-05-22T12:00:00Z"` returns `Ok(state)` where `state.id == ProviderId::Mock`, `state.windows.len() == 1`, `state.windows[0].label == "mock-session"`, `state.windows[0].percent_remaining` is 60.0 (within f32::EPSILON), and `state.windows[0].reset.resets_at == ctx.now + jiff::Span::new().hours(2)`.
    - Test 2: `state.fetched_at == ctx.now` (mock uses injected clock, NOT `Timestamp::now()`).
    - Test 3: `state` round-trips through `serde_json::to_string` + `from_str` losslessly (re-asserts model.rs serde across the full ProviderState built by the mock).
    - Test 4: Compile-time `assert_impl_all!(MockProvider: Send, Sync);` AND re-assert `assert_impl_all!(Box<dyn Provider>: Send, Sync);` here too (defense in depth — if Plan 02 dropped this and we don't, both checks must pass).
    - Test 5: `MockProvider.id() == ProviderId::Mock` (one-line sanity test).
  </behavior>
  <action>
    Per RESEARCH § Code Examples lines 482-551, write `src/provider/mock.rs` containing:

    1. Module imports:
       - `use std::borrow::Cow;`
       - `use async_trait::async_trait;`
       - `use crate::model::{ProviderId, ProviderState, HpWindow, ResetInfo, ProviderError};`
       - `use crate::provider::{FetchCtx, Provider};`

    2. `pub struct MockProvider;` — unit struct. Doc comment: `/// Phase 0 placeholder provider. Returns a hardcoded HpWindow per CONTEXT D-25 so the spine can be exercised before any real adapter exists. Phase 1 will gate this behind a config-only flag; production builds will not invoke MockProvider unless the user explicitly enables it. NOT cfg(test)-only — production-reachable for Phase 0 cargo run.`

    3. `#[async_trait] impl Provider for MockProvider` with:
       - `fn id(&self) -> ProviderId { ProviderId::Mock }`
       - `async fn fetch(&self, ctx: &FetchCtx<'_>) -> Result<ProviderState, ProviderError>` body:
         - Compute `resets_at` as `ctx.now + jiff::Span::new().hours(2)` — CRITICAL: use `ctx.now`, NOT `jiff::Timestamp::now()` (per RESEARCH § Anti-Patterns line 322). This is the clock-injection contract.
         - Return `Ok(ProviderState { id: ProviderId::Mock, windows: vec![ HpWindow { label: Cow::Borrowed("mock-session"), percent_remaining: 60.0, reset: ResetInfo { resets_at }, bar_color: None } ], fetched_at: ctx.now, source: Cow::Borrowed("mock") })` — note `source: Cow::Borrowed("mock")` per W-2 (Plan 02's `ProviderState.source` is `Cow<'static, str>`, not `&'static str`).
       - Per D-25 the label is literally `"mock-session"`, percent is `60.0`, the offset is `+2h`. These values are NOT placeholders — they're the contract that Plan 03 Task 2 (render_text) and the smoke test will verify byte-by-byte.

    4. NO `unwrap()`, NO `expect()`, NO `panic!()` in the fetch body — clippy.toml enforces this for adapter code. The body should be a single `Ok(...)` expression returning the constructed `ProviderState`. Span arithmetic via `+ jiff::Span::new().hours(2)` is infallible.

    5. Inline `#[cfg(test)] mod tests { ... }` per RESEARCH lines 510-550, but extend with the 5 tests in <behavior>:
       - `mock_returns_expected_shape` — full shape assertion per Test 1.
       - `mock_uses_injected_clock` — assert `state.fetched_at == ctx.now` (NOT recent wall-clock now). Pick a fixed past timestamp like `"2020-01-01T00:00:00Z"` so the test would obviously fail if `fetch` used `Timestamp::now()` instead.
       - `provider_state_serde_roundtrip_full` — serialize the mock's output and assert it deserializes back equal.
       - `mock_id_returns_mock` — `assert_eq!(MockProvider.id(), ProviderId::Mock);`
       - Module-level `assert_impl_all!(MockProvider: Send, Sync);` and `assert_impl_all!(Box<dyn Provider>: Send, Sync);` — both compile-time.

       In tests, `unwrap()` is allowed via the clippy.toml `allow-unwrap-in-tests = true` setting because tests live in inline `#[cfg(test)] mod tests` (per Pitfall 3 — this is exactly why tests stay inline rather than in `tests/`).

    6. Update `src/provider/mod.rs` (Plan 02 file) to declare the submodule. Add the line `pub mod mock;` near the top, after the existing trait+FetchCtx declarations. This makes `MockProvider` reachable as `ahb::provider::mock::MockProvider` from `src/main.rs`.
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | tail -5 && cargo test --lib provider::mock 2>&1 | tail -15 && cargo clippy --lib --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/provider/mock.rs` exists.
    - `cargo build --lib` exits 0.
    - `cargo test --lib provider::mock` runs at least 4 tests and all pass (the `mock_returns_expected_shape`, `mock_uses_injected_clock`, `provider_state_serde_roundtrip_full`, `mock_id_returns_mock`).
    - `cargo clippy --lib --all-targets -- -D warnings` exits 0.
    - `grep -q 'impl Provider for MockProvider' src/provider/mock.rs` succeeds.
    - `grep -q 'Cow::Borrowed("mock-session")' src/provider/mock.rs` succeeds (label exact).
    - `grep -q 'percent_remaining: 60.0' src/provider/mock.rs` succeeds (value exact).
    - `grep -q 'source: Cow::Borrowed("mock")' src/provider/mock.rs` succeeds (W-2 compliance — source uses Cow, not bare `&'static str`).
    - `grep -q 'pub mod mock' src/provider/mod.rs` succeeds (submodule exported).
    - `grep -E 'jiff::Timestamp::now\(\)' src/provider/mock.rs` returns NOTHING (the file must NOT call wall-clock now inside fetch — clock injection contract).
    - Filter test count: `cargo test --lib provider::mock 2>&1 | grep -E 'test result: ok\. ([4-9]|[0-9][0-9])' | head -1` finds at least 4-passing.
  </acceptance_criteria>
  <done>
    MockProvider implements Provider, returns the D-25 fixture, honors clock-injection via `ctx.now`. Inline tests prove shape + serde round-trip + dyn-safety. `src/provider/mod.rs` exports the submodule. The spine has its first concrete implementer.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write src/cli/render_text.rs (compact_line + format_countdown) + src/cli/mod.rs</name>
  <files>src/cli/mod.rs, src/cli/render_text.rs, src/lib.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Code Examples — cli/render_text.rs — bar builder" (lines 611-641) — reference implementation (NOT verbatim — see B-1 correction below)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md `<specifics>` section (lines 130-136) — HP bar literal shape including U+2022 middle dot
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-15, D-16, D-18 — charset + width + ASCII fallback
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Pitfall 4: Verifying charset rendering" (lines 400-413) — byte verification approach
    - /home/chasel/REPO/AIHPBar/src/lib.rs (current state — has model, provider, secrets exported)
  </read_first>
  <behavior>
    - Test 1: `compact_line(&state, &now, ascii=false)` with the MockProvider's fixture returns EXACTLY the string `"mock-session  ██████░░░░ 60% • resets in 2h00m"` (two spaces after label, 6 filled + 4 empty cells, U+2022 separator U+2022, "2h00m" format).
    - Test 2: `compact_line(&state, &now, ascii=true)` returns `"mock-session  ######---- 60% | resets in 2h00m"` (`#` filled, `-` empty, ASCII pipe separator).
    - Test 3: Bar width is fixed at 10 cells regardless of percent: a state with 0% renders 0 filled / 10 empty; 100% renders 10 filled / 0 empty; 60% renders 6/4; 67% renders 7/3 (rounded).
    - Test 4: Countdown format for `+2h00m exactly` returns `"2h00m"`; for `+0h15m` returns `"0h15m"` (always zero-padded minutes).
    - Test 5: Byte-level — the produced (non-ASCII) string contains bytes `e2 96 88` (U+2588) and `e2 96 91` (U+2591) and `e2 80 a2` (U+2022 middle dot). Verified by `compact_line(...).as_bytes().windows(3).any(|w| w == [0xe2, 0x96, 0x88])`.
  </behavior>
  <action>
    1. Create `src/cli/mod.rs` containing one line: `pub mod render_text;`. This makes the render module reachable as `ahb::cli::render_text` for the binary entry and any future integration tests.

    2. Create `src/cli/render_text.rs`. The RESEARCH § Code Examples block (lines 611-641) is a REFERENCE — translate the structure, NOT verbatim. The key deviation is in `format_countdown` (see step B below). Build the file as follows:

       File-level constant at top: `pub const BAR_WIDTH: usize = 10;  // CONTEXT D-16 — fixed for snapshot stability + multi-provider alignment in --compact mode`

       Imports: `use crate::model::ProviderState;`

       (A) Function `pub fn compact_line(state: &ProviderState, now: &jiff::Timestamp, ascii: bool) -> String`:
       - Per CONTEXT specifics first bullet, Phase 0 mock returns exactly one window — assert with `debug_assert_eq!(state.windows.len(), 1, "Phase 0 mock returns exactly one window; multi-window rendering is Phase 1");`
       - Let `w = &state.windows[0];`
       - Clamp percent: `let pct = w.percent_remaining.clamp(0.0, 100.0);`
       - Compute filled cells: `let filled = (pct * BAR_WIDTH as f32 / 100.0).round() as usize;`
       - Build bar:
         - If `ascii`: `format!("{}{}", "#".repeat(filled), "-".repeat(BAR_WIDTH - filled))` — D-18 charset.
         - Else: `format!("{}{}", "\u{2588}".repeat(filled), "\u{2591}".repeat(BAR_WIDTH - filled))` — D-15 charset (use `\u{...}` escapes in source rather than literal block chars, so the source file stays ASCII-clean for grep/diff readability).
       - Compute countdown: `let countdown = format_countdown(now, &w.reset.resets_at);`
       - Compute separator: `let sep = if ascii { '|' } else { '\u{2022}' };` per CONTEXT specifics bullet 1.
       - Final format: `format!("{label}  {bar} {pct}% {sep} resets in {countdown}", label = w.label, pct = pct.round() as u32)` — two spaces after label per CONTEXT specifics (matches `mock-session  ██████░░░░ 60% • resets in 2h00m`).

       (B) Function `fn format_countdown(now: &jiff::Timestamp, target: &jiff::Timestamp) -> String`:
       - **B-1 correction — DO NOT translate verbatim from RESEARCH lines 611-641.** RESEARCH around line 636 uses `target.since(*now).unwrap_or_default()`. That is incorrect for two reasons:
         (i) `clippy::unwrap_used` is denied for the renderer path (this file is built under the same crate-wide deny attributes; even `unwrap_or_default` is fine for clippy, but the planner's correction is about *semantics*, not lint), and
         (ii) `jiff::Span::default()` is NOT identical to `jiff::Span::new()` for the purposes of countdown formatting — `Span::new()` is the documented zero-identity Span with all fields explicitly zeroed; `Span::default()` derivation has historically been a source of confusion in jiff issue threads. To keep behavior unambiguous, **use `.since(*now).unwrap_or_else(|_| jiff::Span::new())` — NOT `.unwrap_or_default()`. The RESEARCH example uses `unwrap_or_default()` which is not the same as `Span::new()` (Span has fields that need explicit construction); the planner's correction stands.**
       - Implementation: `let span = target.since(*now).unwrap_or_else(|_| jiff::Span::new());`
       - Extract hours and minutes: `let h = span.get_hours(); let m = span.get_minutes();`
       - Format: `format!("{h}h{m:02}m")` — minutes zero-padded to 2 digits.

       NOTE on the jiff::Span API: `Span::get_hours` and `Span::get_minutes` exist per jiff 0.2 docs. If during implementation these turn out to be named differently (e.g., `get_hours()` returns `i64`, may need cast), adjust per actual signatures but keep behavior identical.

    3. Inline `#[cfg(test)] mod tests { ... }` containing the 5 tests from <behavior>. For each test, construct a `ProviderState` directly (not via MockProvider) so the test is independent of mock.rs. Use a fixed `jiff::Timestamp` for `now` and `now + 2h` for `resets_at` so countdown is deterministic.

       For Test 5 (byte check), use `.as_bytes().windows(3).any(|w| w == &[0xe2, 0x96, 0x88][..])` for the filled-block check, and the analogous for U+2591 and U+2022.

    4. Update `src/lib.rs` to add `pub mod cli;` AFTER the existing `pub mod model;`, `pub mod provider;`, `pub mod secrets;` lines. Final `src/lib.rs`:
       ```
       // crate root — modules added in plans 02 and 03
       pub mod model;
       pub mod provider;
       pub mod secrets;
       pub mod cli;
       ```

    5. Do NOT add color (owo-colors) styling in this task — Phase 0 mock output is uncolored per RESEARCH § Phase 0 dep minimalism. owo-colors is in Cargo.toml so it's available, but the call site is Plan 03 Task 3 (main.rs) and even there owo-colors is wired only as a no-op pass-through for Phase 0. Coloring meaningful tokens (red/yellow/green bar segments) is Phase 1/2 work per CONTEXT deferred ideas.
  </action>
  <verify>
    <automated>cargo build --lib 2>&1 | tail -5 && cargo test --lib cli::render_text 2>&1 | tail -15 && cargo clippy --lib --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/cli/mod.rs` exists and contains `pub mod render_text;`.
    - `src/cli/render_text.rs` exists.
    - `src/cli/render_text.rs` contains `pub const BAR_WIDTH: usize = 10;`.
    - `src/cli/render_text.rs` contains `pub fn compact_line` taking `(&ProviderState, &jiff::Timestamp, bool)` and returning `String`.
    - `src/cli/render_text.rs` does NOT contain literal `█` or `░` Unicode chars (uses `\u{2588}` / `\u{2591}` escapes per action item).
    - **B-1 binding check:** `grep -q 'unwrap_or_else' src/cli/render_text.rs && ! grep -q 'unwrap_or_default' src/cli/render_text.rs` — `format_countdown` MUST use `.unwrap_or_else(|_| jiff::Span::new())` and MUST NOT use `.unwrap_or_default()` anywhere in the file.
    - `cargo build --lib` exits 0.
    - `cargo test --lib cli::render_text` runs at least 4 tests, all pass.
    - The Unicode test (`compact_line(...) == "mock-session  ██████░░░░ 60% • resets in 2h00m"`) passes — this is the binding success criterion #2 from ROADMAP.
    - The ASCII test (`compact_line(..., true) == "mock-session  ######---- 60% | resets in 2h00m"`) passes.
    - `cargo clippy --lib --all-targets -- -D warnings` exits 0.
    - `grep -q 'pub mod cli;' src/lib.rs` succeeds.
  </acceptance_criteria>
  <done>
    Renderer produces the byte-exact CONTEXT D-25 output line in both Unicode and ASCII modes. BAR_WIDTH = 10 enforced. Countdown formatting deterministic via `Span::new()` fallback (B-1). Unit tests prove the byte sequence (e2 96 88, e2 96 91, e2 80 a2 all verified). Phase 0 success criterion #2 is now achievable — Task 3 wires it to the binary.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Rewrite src/main.rs with panic hook + clap + MockProvider + render wire-up</name>
  <files>src/main.rs</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Code Examples — Phase 0 main.rs — full skeleton" (lines 554-608) — reference main.rs (NOT verbatim — see owo-colors carve-out below)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Pattern 2: Phase 0 Panic Hook (deferred-handoff contract)" (lines 288-314) — panic hook composition contract
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Pitfall 5: ratatui::init() panic-hook composition is order-sensitive" (lines 415-430)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-17, D-18, D-19, D-25, D-27
    - /home/chasel/REPO/AIHPBar/src/cli/render_text.rs (Task 2 — compact_line)
    - /home/chasel/REPO/AIHPBar/src/provider/mock.rs (Task 1 — MockProvider)
    - /home/chasel/REPO/AIHPBar/src/main.rs (Plan 01 placeholder — to be overwritten)
  </read_first>
  <action>
    Rewrite `src/main.rs` based on RESEARCH § Code Examples lines 554-608, preserving the file-level lint attributes from Plan 01 Task 2. **W-4 owo-colors carve-out:** REMOVE the `use owo_colors::{OwoColorize, Stream::Stdout}` import line from the translated main.rs — Phase 0 renderer is uncolored (the renderer doesn't take a color mode parameter, see Task 2). The owo-colors crate stays in Cargo.toml so Phase 1 can use it without dep churn, but it is NOT imported in any Phase 0 source file. Any unused import would fail `cargo clippy --all-targets -- -D warnings`.

    1. File-level attributes (FIRST lines of file, preserved from Plan 01):
       - `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]`
       - `#![warn(clippy::pedantic)]`

       These attributes mean the rest of main.rs cannot use `unwrap()`, `expect()`, or `panic!()` — adapter-quality code from day one.

    2. Imports (DO NOT include `use owo_colors::...` — W-4):
       - `use clap::Parser;`
       - `use ahb::cli::render_text;`
       - `use ahb::provider::Provider;` (trait import — required for `mock.fetch(...).await` method resolution)
       - `use ahb::provider::FetchCtx;`
       - `use ahb::provider::mock::MockProvider;`
       - `use ahb::secrets::Secrets;`

    3. `#[derive(clap::Parser)] struct Cli` per RESEARCH lines 568-578:
       - `#[command(version, about = "AHB — AI HP Bar — multi-CLI subscription session usage at a glance")]` — `version` auto-derives from Cargo.toml; `about` matches D-01 description.
       - Field `#[arg(long)] ascii: bool` — D-18, `--ascii` flag.
       - Field `#[arg(long, value_enum, default_value_t = ColorMode::Auto)] color: ColorMode` — D-17, `--color=auto|always|never`. NOTE: Phase 0 binary does NOT actually colorize anything (the renderer doesn't take a color mode parameter), but the CLI surface accepts the flag per D-17. Phase 1/2 will wire it through. Document with a comment: `/// Color mode (Phase 0 accepts but does not yet apply — Phase 1 wires render coloring).`

    4. `#[derive(Copy, Clone, clap::ValueEnum)] enum ColorMode { Auto, Always, Never }` — clap auto-converts to `--color auto|always|never`.

    5. `fn install_phase0_panic_hook()` per RESEARCH § Pattern 2 lines 296-304:
       - `let original = std::panic::take_hook();`
       - `std::panic::set_hook(Box::new(move |info| { eprintln!("ahb panicked: {info}"); original(info); }));`
       - Doc comment per Pitfall 5: `/// Phase 0 panic hook. Composes via take_hook()+set_hook() so Phase 1's ratatui::init() can wrap it (ratatui takes the hook AFTER we install ours and chains: terminal-restore -> our stderr-print -> default). Order matters — see docs.rs/ratatui/latest/ratatui/fn.init.html.`

    6. `#[tokio::main(flavor = "current_thread")]` per RESEARCH line 591 + Cargo.toml feature set (`["rt", "macros"]` — no rt-multi-thread). The `current_thread` flavor is the smallest tokio runtime; Phase 0 has exactly one async await chain so multi-thread would be pure overhead.

    7. `async fn main() -> anyhow::Result<()>` body per RESEARCH lines 593-605:
       - `install_phase0_panic_hook();` — CRITICAL: first line, BEFORE any other code, per Pitfall 5.
       - `let cli = Cli::parse();` — parse clap args (single binding; read `cli.ascii` below).
       - `let secrets = Secrets::default();`
       - `let now = jiff::Timestamp::now();` — wall-clock at startup. This is the ONLY place wall-clock is read; everything downstream uses `ctx.now`.
       - `let ctx = FetchCtx { now, secrets: &secrets };`
       - `let mock = MockProvider;`
       - `let state = mock.fetch(&ctx).await.map_err(|e| anyhow::anyhow!("mock provider failed: {e}"))?;` — error mapping via anyhow `?` propagation (NOT unwrap).
       - `let line = render_text::compact_line(&state, &ctx.now, cli.ascii);`
       - `println!("{line}");` — the actual user-visible output.
       - `Ok(())`

    8. Per CONTEXT specifics second bullet, Phase 0 output is "exactly one line, exit code 0. No welcome header, no version banner, no help-hint footer. CLI is laconic." Do NOT add `eprintln!("starting...")`, do NOT print a header, do NOT print a footer. Just `println!("{line}");` once and return.

    9. Per CONTEXT specifics third bullet, the bar MUST come through `MockProvider::fetch -> ProviderState -> render_text::compact_line`. Do NOT add a fast-path that hardcodes the string. The whole point of Phase 0 is proving the spine. **B-2: this is enforced by two grep guards — see acceptance criteria.**

    10. Per CONTEXT.md `<deferred>` (D-19 explicitly): NO emoji, NO pace icons in Phase 0. The renderer in Task 2 already excludes these — main.rs does not need to do anything additional.

    11. No tests in main.rs (binary crates can technically have tests but RESEARCH § Project Structure puts all testing in `src/lib.rs` + module-level inline tests). Smoke testing of the binary output is the next plan's job (Plan 05).
  </action>
  <verify>
    <automated>cd /home/chasel/REPO/AIHPBar && cargo build --release 2>&1 | tail -5 && /home/chasel/REPO/AIHPBar/target/release/ahb 2>&1 | head -2 && cargo clippy --bin ahb --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/chasel/REPO/AIHPBar && cargo build --release` exits 0.
    - `/home/chasel/REPO/AIHPBar/target/release/ahb` exits 0 and prints EXACTLY one line.
    - **W-3 regex match (replaces strict byte-for-byte):** That line matches `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` — wall-clock countdown means after a multi-minute build, `2h00m` may shift to `1h59m`. Verify with: `cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb | grep -qP '^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$'`. The byte verification (xxd) covers the structural integrity of the bar/separator bytes — see next item.
    - xxd byte structure proof: `cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb | xxd | head -1` confirms the bytes `e2 96 88` (U+2588) appearing 6 times and `e2 96 91` (U+2591) appearing 4 times and `e2 80 a2` (U+2022) appearing once.
    - `cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb --ascii` prints a line matching `^mock-session  ######---- 60% \| resets in [0-9]+h[0-9]{2}m$`.
    - `cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb --help` shows `--ascii` and `--color` flags + the D-01 description line.
    - `cd /home/chasel/REPO/AIHPBar && /home/chasel/REPO/AIHPBar/target/release/ahb --version` shows `ahb 0.0.1` (from Cargo.toml version).
    - `cd /home/chasel/REPO/AIHPBar && cargo clippy --bin ahb --all-targets -- -D warnings` exits 0.
    - `grep -q 'install_phase0_panic_hook' src/main.rs` succeeds.
    - `grep -q 'take_hook' src/main.rs` succeeds (Pitfall 5 composition).
    - `grep -q 'flavor = "current_thread"' src/main.rs` succeeds (lean tokio).
    - **B-2 bypass-the-trait guards (replaces the ineffective `println!("\[..."` grep):**
      (a) `grep -q '\.fetch(' src/main.rs` — confirms the trait method is called.
      (b) `! grep -qE 'println!\([^)]*mock.session' src/main.rs` — confirms no hardcoded label-string `println!`. (Mock label is `"mock-session"`; the grep regex `mock.session` matches both `mock-session` and `mock_session` defensively, so an executor who hardcodes the line will fail this check.)
    - **W-4 owo-colors carve-out:** `! grep -q 'use owo_colors' src/main.rs && cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings` exits 0 — no `use owo_colors` import line in any Phase 0 source file; clippy is green (no unused-import warning).
    - The release binary path through the trait: `grep -q 'mock.fetch(&ctx).await' src/main.rs` succeeds — proves the call goes through the trait method.
    - No `.unwrap()` in main.rs adapter path: `! grep -E '\.unwrap\(\)' src/main.rs` (tests live elsewhere; main.rs is adapter-grade).
  </acceptance_criteria>
  <done>
    `cargo run --release` prints a line matching `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` through the locked Provider trait. Phase 0 panic hook installed in correct order for Phase 1 ratatui composition. `--ascii` flag works. Clippy clean (no unused `owo_colors` import — W-4). B-2 bypass-the-trait guards in place. B-3 absolute-path invocations stable across Bash tool calls. Phase 0 success criterion #2 from ROADMAP is met (and #4 / #5 are partially provable here pending Plan 05 charset eyeball + CI run).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cli arg parsing → application logic | clap-validated enum (ColorMode) and bool flag (--ascii) limit input surface to a closed set. |
| MockProvider → render → stdout | Internal trust boundary; no untrusted data crosses. |
| Phase 0 panic hook → Phase 1 ratatui::init() | Deferred contract — Phase 0 must compose via take_hook() so Phase 1 can wrap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-09 | DoS | Panic in `MockProvider::fetch` leaves terminal in bad state | mitigate | Phase 0 has no TUI (no raw mode, no altscreen), so a panic is harmless. `install_phase0_panic_hook` still composes via take_hook/set_hook so Phase 1 ratatui::init() will wrap correctly (Pitfall 5). MockProvider body has no panic surface (no unwrap, no division, no indexing). |
| T-00-10 | Tampering | Adapter (MockProvider) uses wall-clock `jiff::Timestamp::now()` internally, defeating clock injection | mitigate | Action item explicitly bans `Timestamp::now()` inside `fetch`; acceptance criteria includes grep check. Test `mock_uses_injected_clock` would fail loudly if violated. |
| T-00-11 | Info disclosure | `eprintln!("ahb panicked: {info}")` in panic hook could expose internal state | accept | Phase 0 has no secrets, no PII, no internal state worth hiding. Phase 1 will revisit when Secret<T> + keyring-core land. `info` is `std::panic::PanicInfo` which by default prints file:line + message — acceptable for a CLI tool. |
| T-00-12 | DoS | `--color=always` could pump ANSI escapes into a pipe (CORE-05 violation) | accept | Phase 0 binary doesn't actually colorize output — the `--color` flag is parsed but unused. The risk surfaces in Phase 1 when coloring is wired; ensure the consumer there checks `std::io::IsTerminal` and respects `NO_COLOR`. Phase 0 verifies clap parses the flag; semantic correctness is a Phase 1 plan task. |
| T-00-13 | Tampering | `cli.color` ignored value could mask future test failures | accept | We deliberately accept this flag at the CLI layer without wiring its semantics in Phase 0. Risk: a Phase 1 reviewer might think the flag works. Mitigation: doc comment on `Cli::color` field explicitly notes "Phase 0 accepts but does not yet apply" so the gap is documented in code. |

Severity: All LOW for Phase 0 Plan 03. The runtime surface is one async fetch returning a hardcoded ProviderState. No network, no filesystem (beyond the binary itself), no untrusted input.
</threat_model>

<verification>
After all three tasks complete:
1. `cd /home/chasel/REPO/AIHPBar && cargo build --release` exits 0.
2. `cd /home/chasel/REPO/AIHPBar && cargo test` exits 0 with at least 8 passing tests (4 from model.rs + 2 from provider/mod.rs + 4 from provider/mock.rs + 5 from cli/render_text.rs ≈ 15).
3. `cd /home/chasel/REPO/AIHPBar && cargo clippy --all-targets -- -D warnings` exits 0.
4. `/home/chasel/REPO/AIHPBar/target/release/ahb` prints exactly one line matching `^mock-session  ██████░░░░ 60% • resets in [0-9]+h[0-9]{2}m$` (W-3 regex match; byte-level structural proof via xxd in Plan 05).
5. `/home/chasel/REPO/AIHPBar/target/release/ahb --ascii` prints a line matching `^mock-session  ######---- 60% \| resets in [0-9]+h[0-9]{2}m$`.
6. `/home/chasel/REPO/AIHPBar/target/release/ahb --help` exits 0 with a clap-generated help including the D-01 description.
7. `/home/chasel/REPO/AIHPBar/target/release/ahb --version` prints `ahb 0.0.1`.
8. `/home/chasel/REPO/AIHPBar/target/release/ahb > /tmp/out.txt && xxd /tmp/out.txt | head -3` confirms bytes `e2 96 88` (filled) and `e2 96 91` (empty) and `e2 80 a2` (separator) appear in expected positions. (Plan 05 records this in the GEMINI_SPIKE memo per D-26.)
9. The bar value reaches stdout via the trait — `grep -c 'mock.fetch' src/main.rs` is at least 1; B-2 negative guard `! grep -qE 'println!\([^)]*mock.session' src/main.rs` holds.
</verification>

<success_criteria>
- Phase 0 ROADMAP success criterion #2 is met: `cargo build --release` produces a single binary; `./ahb` prints one mocked HP bar line via the locked Provider trait + ProviderState/ResetInfo/HpUnit/ProviderError types. ✓
- Phase 0 ROADMAP success criterion #3 partially met: `cargo test` runs green against MockProvider-based unit tests. (Plan 05 reaffirms the full success-criteria checklist.)
- D-15 (charset default U+2588 / U+2591), D-16 (10-cell width), D-17 (--color flag accepted), D-18 (--ascii fallback substitutes `#`/`-` and `|`), D-19 (no emoji), D-25 (mock fixture exact), D-27 (panic hook installed with take_hook/set_hook composition) all honored.
- `unwrap_used` clippy denial maintained — no adapter-path unwraps. Tests use unwrap freely via `allow-unwrap-in-tests`.
- B-1 (`Span::new()` fallback, no `unwrap_or_default`), B-2 (bypass-the-trait guards), B-3 (absolute-path verifies), W-3 (regex-match for countdown drift), W-4 (no owo-colors import in Phase 0 source) all enforced.
- The spine is now demonstrably working end-to-end.
</success_criteria>

<output>
Create `.planning/phases/00-spike-spine/00-03-SUMMARY.md` when done.
</output>
