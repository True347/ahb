---
phase: 00-spike-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitignore
  - LICENSE-MIT
  - LICENSE-APACHE
  - clippy.toml
  - .github/workflows/ci.yml
  - src/main.rs
  - src/lib.rs
autonomous: true
requirements: [ADP-00]
tags: [rust, cargo, ci, scaffold]

must_haves:
  truths:
    - "Cargo workspace exists at project root with name 'ahb' and bin 'ahb'"
    - "cargo build succeeds on a placeholder src/main.rs"
    - "cargo clippy -- -D warnings succeeds on a placeholder src/main.rs"
    - "CI workflow exists targeting ubuntu-latest, macos-latest, windows-latest"
    - "Cargo.lock is tracked (not in .gitignore) per D-07"
  artifacts:
    - path: "Cargo.toml"
      provides: "Crate manifest with locked deps + MSRV 1.88 + dual license + edition 2024"
      contains: "name = \"ahb\""
    - path: ".gitignore"
      provides: "Standard Rust gitignore; Cargo.lock NOT ignored"
      contains: "/target"
    - path: "LICENSE-MIT"
      provides: "MIT license text"
      min_lines: 18
    - path: "LICENSE-APACHE"
      provides: "Apache-2.0 license text"
      min_lines: 100
    - path: "clippy.toml"
      provides: "allow-unwrap-in-tests + disallowed-types for crossterm"
      contains: "allow-unwrap-in-tests = true"
    - path: ".github/workflows/ci.yml"
      provides: "3-OS matrix CI with build/test/clippy"
      contains: "windows-latest"
    - path: "src/lib.rs"
      provides: "Library entry — empty placeholder (re-exports added in Plan 02/03)"
    - path: "src/main.rs"
      provides: "Binary entry — placeholder that compiles + clippies clean"
  key_links:
    - from: "Cargo.toml"
      to: "src/main.rs"
      via: "[[bin]] default discovery"
      pattern: "fn main"
    - from: ".github/workflows/ci.yml"
      to: "cargo clippy --all-targets -- -D warnings"
      via: "CI step `run:` command"
      pattern: "cargo clippy"
---

<objective>
Scaffold the `ahb` Rust crate, lock all Phase 0 dependencies at exact versions from STACK.md, set up clippy + CI floor, and produce a compilable-but-placeholder binary. No domain code in this plan — only the foundation.

**Purpose:** Establish the floor that prevents any future commit from breaking the spine. Every later Phase 0 plan plugs into this scaffold.

**Output:** A Rust crate that `cargo build`, `cargo test`, and `cargo clippy -- -D warnings` all succeed on, with CI green across Linux/macOS/Windows.
</objective>

<execution_context>
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/workflows/execute-plan.md
@/home/chasel/REPO/AIHPBar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/chasel/REPO/AIHPBar/.planning/PROJECT.md
@/home/chasel/REPO/AIHPBar/.planning/ROADMAP.md
@/home/chasel/REPO/AIHPBar/.planning/STATE.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md
@/home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md
@/home/chasel/REPO/AIHPBar/.planning/research/STACK.md
@/home/chasel/REPO/AIHPBar/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write Cargo.toml + .gitignore + LICENSE files</name>
  <files>Cargo.toml, .gitignore, LICENSE-MIT, LICENSE-APACHE</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md (D-01..D-07 — scaffolding decisions)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Cargo.toml skeleton" (lines 708-740) — verbatim Cargo.toml template
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Phase 0 dep minimalism" (lines 140-145) — strict dep list
    - /home/chasel/REPO/AIHPBar/.planning/research/STACK.md — version pins cross-reference
    - /home/chasel/REPO/AIHPBar/CLAUDE.md "TL;DR — One-Line Picks" + "What NOT to Use" tables
  </read_first>
  <action>
    Create Cargo.toml at project root with these EXACT fields (per D-01..D-04 + RESEARCH "Cargo.toml skeleton"):

    `[package]` section: `name = "ahb"`, `version = "0.0.1"`, `edition = "2024"`, `rust-version = "1.88"`, `license = "MIT OR Apache-2.0"`, `description = "AHB — AI HP Bar — multi-CLI subscription session usage at a glance"` (em-dash U+2014, verbatim per D-01), `repository = "https://github.com/True347/ahb"`, `readme = "README.md"`, `keywords = ["claude", "codex", "gemini", "cli", "tui"]`, `categories = ["command-line-utilities"]`.

    `[dependencies]` — ONLY these crates at these exact versions (per RESEARCH § Phase 0 dep minimalism — do NOT add ratatui, reqwest, rusqlite, keyring-core, directories, tracing yet):
    - `clap = { version = "4.6", features = ["derive"] }`
    - `tokio = { version = "1.52", default-features = false, features = ["rt", "macros"] }` — lean features per RESEARCH Q5 + CLAUDE.md NOT-list (no `["full"]`)
    - `async-trait = "0.1"`
    - `serde = { version = "1", features = ["derive"] }`
    - `serde_json = "1"`
    - `jiff = { version = "0.2", features = ["serde"] }`
    - `anyhow = "1"`
    - `thiserror = "2"`
    - `owo-colors = { version = "4", features = ["supports-colors"] }` — per RESEARCH Q3 resolution of D-17 discretion

    `[dev-dependencies]` — only: `static_assertions = "1"`.

    Add an empty `[profile.release]` section as a forward-looking placeholder (Phase 4 will tune).

    Per D-07, create `.gitignore` containing: `/target`, `**/*.rs.bk`. CRITICAL — Cargo.lock MUST NOT appear (this is a binary crate; lock file is tracked).

    Create `LICENSE-MIT` with the standard MIT license text (copyright holder: "Chasel" or project name `ahb` contributors; year 2026). Create `LICENSE-APACHE` with the standard Apache-2.0 license text (the long form from apache.org). Per D-03, both files MUST exist — the SPDX `MIT OR Apache-2.0` requires BOTH.

    Do NOT run `cargo new` — manually write these files. `cargo new` creates an `src/main.rs` with `println!("Hello, world!")` which we'll overwrite in Task 2 anyway, and it auto-generates a generic Cargo.toml that doesn't match our exact spec.
  </action>
  <verify>
    <automated>test -f Cargo.toml && test -f .gitignore && test -f LICENSE-MIT && test -f LICENSE-APACHE && grep -q '^name = "ahb"$' Cargo.toml && grep -q '^edition = "2024"$' Cargo.toml && grep -q '^rust-version = "1.88"$' Cargo.toml && grep -q '^license = "MIT OR Apache-2.0"$' Cargo.toml && grep -q '^/target$' .gitignore && ! grep -q 'Cargo.lock' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` parses: `cargo metadata --offline 2>&1 | head -1` does not show TOML parse errors (it may fail later for missing deps, but parses).
    - `Cargo.toml` contains literal `name = "ahb"`, `edition = "2024"`, `rust-version = "1.88"`, `license = "MIT OR Apache-2.0"`.
    - `Cargo.toml` description starts with `AHB — AI HP Bar` (em-dash U+2014, three bytes `e2 80 94`).
    - `[dependencies]` contains exactly the 9 crates listed in the action, no more (no ratatui, reqwest, etc.).
    - `[dev-dependencies]` contains only `static_assertions`.
    - `.gitignore` contains `/target` and `**/*.rs.bk` and does NOT contain `Cargo.lock`.
    - `LICENSE-MIT` exists and contains "MIT License" text.
    - `LICENSE-APACHE` exists and contains "Apache License" text + "Version 2.0" string.
  </acceptance_criteria>
  <done>
    Cargo manifest, gitignore, and both license files are written. `Cargo.toml` reflects the exact dep + metadata spec from RESEARCH § Cargo.toml skeleton, honoring decisions D-01 through D-07.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write placeholder src/main.rs + src/lib.rs + clippy.toml</name>
  <files>src/main.rs, src/lib.rs, clippy.toml</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "clippy.toml — Phase 0 lint configuration" (lines 643-665)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § "Recommended Project Structure" (lines 236-261) — `lib.rs` recommendation
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-04 (MSRV) + D-06 (layout)
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md "Claude's Discretion" (lib.rs split, per RESEARCH = include)
  </read_first>
  <action>
    Create `src/main.rs` as a minimal placeholder that compiles + clippies clean. File-level attributes at top (per RESEARCH "clippy.toml" section + D-04):
    - `#![deny(clippy::unwrap_used, clippy::expect_used, clippy::panic)]`
    - `#![warn(clippy::pedantic)]`

    Body: a single `fn main() { /* phase 0 plan 01 placeholder — replaced in plan 03 */ }` — no `println!`, no `unwrap`. Phase 0 Plan 03 will rewrite this file with the full skeleton.

    Create `src/lib.rs` as an empty file with one line: `// crate root — modules added in plans 02 and 03`. The library entry is required so future integration tests under `tests/` can `use ahb::...` without `#[path]` hacks (per RESEARCH "lib.rs recommendation"). Plans 02 + 03 will add `pub mod ...` declarations.

    Create `clippy.toml` at project root per D-04 + RESEARCH § "clippy.toml — Phase 0 lint configuration":
    - `allow-unwrap-in-tests = true`
    - `allow-expect-in-tests = true`
    - `allow-panic-in-tests = true`
    - A `disallowed-types` entry blocking direct `crossterm::*` use, with `reason = "use owo-colors (Phase 0) or ratatui::crossterm (Phase 1+) — see PITFALLS.md double-version hazard"`

    Per RESEARCH Pitfall 3, do NOT create a `tests/` directory in Phase 0 — `allow-unwrap-in-tests` does NOT cover that directory (clippy issue #13981). All Phase 0 tests live inline in `src/` modules under `#[cfg(test)] mod tests { ... }`.
  </action>
  <verify>
    <automated>cargo build 2>&1 | tail -5 && cargo clippy --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build` exits 0.
    - `cargo clippy --all-targets -- -D warnings` exits 0.
    - `src/main.rs` first three non-blank lines are the three `#![...]` attributes from action.
    - `src/lib.rs` exists and is essentially empty.
    - `clippy.toml` contains `allow-unwrap-in-tests = true` (uncommented).
    - `clippy.toml` contains a `disallowed-types` array with at least one entry referencing `crossterm`.
    - No `tests/` directory exists at project root (per Pitfall 3).
  </acceptance_criteria>
  <done>
    Placeholder Rust source compiles + lints cleanly with the strict Phase 0 lint config. Subsequent plans can `cargo build` from this baseline.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write .github/workflows/ci.yml — 3-OS matrix</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-RESEARCH.md § ".github/workflows/ci.yml — 3-OS matrix" (lines 667-706) — verbatim YAML template
    - /home/chasel/REPO/AIHPBar/.planning/phases/00-spike-spine/00-CONTEXT.md D-05 — CI scope decision
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` exactly per RESEARCH § "3-OS matrix" template (lines 667-706):
    - `name: ci`
    - Triggers: `on: { push: { branches: [main] }, pull_request: ~ }` — push on main only (not every branch — avoids double-running on PR), pull_request unconditional
    - Single `test` job with matrix strategy: `fail-fast: false`, `os: [ubuntu-latest, macos-latest, windows-latest]`, `rust: [stable]` (no MSRV-specific test job in Phase 0 — CI proves stable+; MSRV correctness is enforced by `rust-version` field).
    - Steps:
      1. `actions/checkout@v6` (latest at research date 2026-05-22).
      2. Install toolchain via `actions-rust-lang/setup-rust-toolchain@v1` (per RESEARCH Q10 — bundles `Swatinem/rust-cache` + problem matchers; documented successor to dtolnay/rust-toolchain). `with:` `toolchain: ${{ matrix.rust }}`, `components: clippy, rustfmt`.
      3. `cargo build --all-targets`
      4. `cargo test --all-targets`
      5. `cargo clippy --all-targets -- -D warnings`

    Do NOT add `cargo fmt --check`, `cargo audit`, `cargo deny`, or `cargo build --release` to Phase 0 CI per D-05 — those are deferred to Phase 4 Distribution polish. The Phase 0 CI floor is build + test + clippy.

    Job name uses `${{ matrix.os }} / ${{ matrix.rust }}` so the GitHub Checks page reads "ubuntu-latest / stable" etc. — Chasel can identify failing OS at a glance.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -q 'windows-latest' .github/workflows/ci.yml && grep -q 'macos-latest' .github/workflows/ci.yml && grep -q 'ubuntu-latest' .github/workflows/ci.yml && grep -q 'actions-rust-lang/setup-rust-toolchain@v1' .github/workflows/ci.yml && grep -q 'fail-fast: false' .github/workflows/ci.yml && grep -q 'cargo clippy --all-targets -- -D warnings' .github/workflows/ci.yml && ! grep -q 'cargo fmt' .github/workflows/ci.yml && ! grep -q 'cargo audit' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists.
    - YAML contains literal `ubuntu-latest`, `macos-latest`, `windows-latest` (all three on one line of the matrix).
    - YAML contains `actions-rust-lang/setup-rust-toolchain@v1` (NOT `dtolnay/rust-toolchain`).
    - YAML contains `fail-fast: false` (so a Windows failure doesn't mask macOS).
    - YAML contains `cargo clippy --all-targets -- -D warnings`.
    - YAML does NOT contain `cargo fmt`, `cargo audit`, `cargo deny`, `cargo doc` — those are Phase 4 (per D-05).
    - File parses as valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (if `python3` available; otherwise skip but verify visually).
  </acceptance_criteria>
  <done>
    CI workflow ready. When pushed, it will exercise build/test/clippy on three OSes for every push to main and every PR. CI is the build-and-test floor that prevents any future plan from regressing the spine.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo registry → local build | crates.io fetches deps; pinned versions in Cargo.toml limit drift but supply-chain trust still applies. |
| GitHub Actions runner → cargo build | CI runner downloads + compiles crate; same crates.io trust as local. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | Cargo dependency resolution (slopsquatting risk) | mitigate | All Phase 0 deps independently verified against project STACK.md + RESEARCH § Package Legitimacy Audit (lines 161-178); only canonical/dtolnay/serde-org/well-known-author crates pinned at exact major versions. |
| T-00-02 | Tampering | GitHub Actions third-party action (`actions-rust-lang/setup-rust-toolchain@v1`) | accept | Action is widely used (cited in RESEARCH as documented successor); pinned to `@v1` (major version, accepts security patches). Phase 4 may pin to commit SHA when polishing distribution. |
| T-00-03 | DoS | Panic in placeholder `fn main()` | accept | Placeholder `fn main()` has empty body — no panic surface. Real panic-hook contract lands in Plan 03 per D-27. |
| T-00-04 | Info disclosure | Cargo.toml metadata exposes author email or paths | accept | Cargo.toml in this task contains no `authors = [...]` field (modern style — author info inferred from git). No PII written. |
| T-00-SC | Tampering | npm/pip/cargo installs | mitigate | All deps drawn from RESEARCH § Package Legitimacy Audit (10 packages, all approved). No `[SLOP]` or `[SUS]` packages. slopcheck unavailable per RESEARCH note — manual audit is the binding control. |

Severity assessment: All Phase 0 Plan 01 threats are LOW. No user input, no network calls beyond Cargo registry, no secrets handling, no panic surface. Empty threat models would be unacceptable; this register documents that Phase 0 scaffolding has minimal surface and explains why each STRIDE category is either mitigated or knowingly accepted.
</threat_model>

<verification>
After all three tasks complete:
1. `cargo build` exits 0.
2. `cargo test` exits 0 (no tests yet, so it just reports "0 passed").
3. `cargo clippy --all-targets -- -D warnings` exits 0.
4. `cargo metadata --offline --format-version 1 | head -c 200` shows `"name":"ahb"` and `"edition":"2024"`.
5. `.github/workflows/ci.yml` validates as YAML.
6. `Cargo.lock` is generated by `cargo build` and is NOT in `.gitignore`.
7. Inspect `Cargo.lock` once to confirm jiff resolved to 0.2.x, tokio to 1.52.x, owo-colors to 4.x (these match STACK.md).
</verification>

<success_criteria>
- All 8 files exist with the content specified.
- `cargo build`, `cargo test`, `cargo clippy --all-targets -- -D warnings` all exit 0 from project root.
- Cargo.toml depends on EXACTLY the 9 production crates + 1 dev crate listed (no ratatui, reqwest, rusqlite, etc. yet).
- CI workflow uses `actions-rust-lang/setup-rust-toolchain@v1` and tests on three OSes with `fail-fast: false`.
- D-01 through D-07 are honored: crate name `ahb`, edition 2024, MSRV 1.88, dual MIT/Apache license, 3-OS CI, single-binary layout, Cargo.lock tracked.
</success_criteria>

<output>
Create `.planning/phases/00-spike-spine/00-01-SUMMARY.md` when done.
</output>
